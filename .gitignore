# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Env
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite
.vite

# 로컬 버그 수정 문서 (노션 이관 예정 — git 추적 제외)
bugfix/

# 로컬 테스트용 얼굴 사진 (LFW 연구용 데이터셋 — 커밋·배포 금지)
test-data/

# 스토리 병행용 git worktree (로컬 체크아웃 — 리포에 담을 내용이 아니다)
.claude/worktrees/
