# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

치즈모아 — 유치원 행사 사진을 AI가 아이별 앨범으로 자동 분류 → 선생님이 검수 → **이벤트별 공개** → 학부모가 링크+비밀번호로 **무로그인** 조회·다운로드하는 **모바일웹** 서비스(MVP).

현재 단계: **스캐폴딩(CHMO-106) + 공용 컴포넌트 세트(CHMO-107) + MSW 목 레이어(CHMO-108·109) + 인증 플로우 실 UI(CHMO-110) + 모임 진입 실 UI(CHMO-111) + 모임 허브 실 UI(CHMO-112) + 이벤트·업로드·분석 실 UI(CHMO-113) + 앨범 그리드·앨범 상세/삭제 실 UI(CHMO-114) + 옮기기·이름전파 실 UI(CHMO-115) + 공개 전 검수·공개 실 UI(CHMO-116) + 학부모 뷰어 실 UI(CHMO-117) + 설정/프로필·통합 마무리(CHMO-118) 완료 — 전 화면 실 UI.** Vite+React18+TS 토대·Tailwind v3 디자인 시스템·전 라우트 뼈대 + 재사용 UI 세트(`src/components/ui/`, 데모 `/dev/components`) + 목 DB·픽스처·api-spec 전 엔드포인트 핸들러(`src/mocks/` — 인증·모임·이벤트·앨범·검수·공개·뷰어) + 인증 3화면(01·01-1·01-2 + **소셜 로그인 4종 병행**(카카오·구글·네이버·애플, CHMO-359) — 버튼은 fetch가 아닌 브라우저 이동으로 BE `/auth/social/{provider}` 시작(오리진은 `VITE_API_ORIGIN`, MSW 목은 내비게이션을 못 가로채 콜백 직행), BE가 FE `/auth/callback?code=일회용코드`(TTL 60초)로 복귀하면 `POST /auth/social/exchange`로 토큰 교환 — 응답이 기존 로그인과 동일 형태라 토큰 저장·리프레시 그대로. returnTo는 sessionStorage 위탁(외부 리다이렉트는 router state 유실). 실패는 BE가 `?error=`로 싣고 화면이 셋으로 갈라 안내한다(`OAUTH400` 미지원 프로바이더=다른 방법 유도·`OAUTH401` 인가 실패=재시도·`OAUTH502` 프로바이더 통신 실패=잠시 후 재시도, 미지 코드는 재시도로 수렴). 목의 일회용 코드도 실 BE처럼 **1회 소진**이라 시도마다 새 코드를 발급한다(고정 문자열이면 재사용 401 계약을 목이 못 지켜 콜백의 StrictMode 이중 교환 가드가 깨져도 로컬에선 안 드러난다). **4종 모두 실 BE 배포 완료**(2026-07-21 — 인가 URL 302 실측, 애플은 `.p8` base64 env가 깨져 기동 실패했다가 복구. 실계정 로그인 왕복 E2E는 FE 배포 후))·모임 진입 3화면(02 홈·03 모임 만들기·02-1 모임 참여 — 참여 코드(joinKey)는 실 BE가 대소문자 혼합 발급·구분 매칭이라 대소문자 변환 없이 그대로 전송(CHMO-285), 초대 링크 복사 URL의 쿼리형 불일치는 CHMO-237에서)·모임 허브(05 모임 상세 + 초대·학부모 공유 시트 + ⚙ 모임 설정 — 이름 수정·모임 삭제(CHMO-277, `DELETE /groups/:id` — 실 BE는 CHMO-273 배포 대기, 목 선행))·이벤트 흐름(06-M 생성 모달·06-E 빈 이벤트·06-U 업로드→AI 분석, 분석중 상태 포함 — 분석중은 `GET /events/:id` 상세의 `progress`(`{processed,total,percent}`, 분석 job 중에만 non-null·목록 응답엔 없음)를 2초 폴링해 쥐→치즈 프로그레스로 표시, 진행률 null이면 쥐 왕복 인디터미넌트 폴백(CHMO-287) — 분석중 판정은 analyzing 상태 외에 `progress` non-null도 포함(CHMO-442)하고, **06-U 진입 차단은 여기에 `photoCount > 0`을 더한다**(CHMO-486 — 업로드는 이벤트당 1회라 사진이 있는 이벤트는 딥링크로도 못 연다. **차단 시 안내 화면 없이 이벤트 상세로 즉시 리다이렉트**(2026-07-29 — 종전엔 분석중/1회 소진 문구를 갈라 보여주고 [이벤트로 돌아가기] 버튼만 두던 화면이라, 08에서 뒤로가기가 막다른 길에 부딪혔다). 나아가 **06-U는 히스토리에 남지 않는다** — 06-E `[사진 업로드]` 진입·업로드 성공 이탈·헤더 `‹` 전부 replace라 `홈 → 05 → 08`만 남고 08 뒤로가기가 모임 상세로 곧장 빠진다). 단 등록 직후엔 AI 첫 진행률 메시지 전이라 progress가 null인 공백이 있어(2026-07-26 실서버 실측 — 등록 +0.7s에 null) 업로드 화면이 navigate state로 '분석 시작' 킥(expected=직전 photoCount+registeredCount)을 넘겨 상세가 폴링을 켠다: photoCount 도달=완료 판정(사진은 분석 커밋 시점에야 반영 — 실측)·progress가 켜지면 인계·30초 안전 타임아웃·전부 중복이면 즉시 소멸. 목은 등록 즉시 progress 노출·사진 즉시 생성이라 킥이 무해 통과 — 킥 실검증은 실 BE로만 가능(CHMO-443))·검수 흐름(08 앨범 그리드+⚙ 이벤트 설정 — 이름 수정·이벤트 삭제(CHMO-278, `DELETE /events/:id` — 실 BE는 CHMO-272 배포 대기, 목 선행. 06-E·분석중에도 헤더 ⚙ 동일 진입)·09 앨범 상세+선택모드·삭제·옮기기(09-1 이동 시트 — 추천 타일은 **이름만** 표기(유사도 % 폐기, 2026-07-29 — 실 BE가 `similarity`를 더는 안 보낸다. 추천 순서가 곧 유사도 순이라 정보는 순서에 남고, 매퍼가 `?? null`로 정규화해 키 누락이 `NaN%`로 새지 않게 계약 테스트로 고정했다) · 추천 맨 끝 '새 앨범' 타일(점선 원형+플러스)로 선택 사진을 새 인물 앨범으로 **생성=이동**(`POST /events/:id/albums` `{name,sourceAlbumId,photoIds}`, CHMO-435 — BE CHMO-416, 이름 필수 1~20자·소스 밖 사진 PHOTO404, 2026-07-25 실서버 프로브. 후보 0개여도 타일만으로 이동 가능) · **빈 앨범 잔류(CHMO-418 — CHMO-231 자동삭제 폐지·CHMO-289 복귀 반전)**: 전량 삭제·이동해도 앨범이 남아 refetch로 빈 상태를 보여준다(0장 카드는 검토 배지 숨김·점선 유지, 목 db.ts도 같은 계약) · **앨범 삭제(CHMO-435 — 전 타입)**: 09 앨범명 줄 🗑(인물은 CHMO-522에서 앨범 설정 시트 하단으로 이동)·빈 앨범 하단 CTA → 확인 다이얼로그(이 앨범에만 속한 사진은 영구 삭제 경고) → `DELETE /albums/:id`(CHMO-271) → 08 복귀(replace) · **빈 앨범 만들기(CHMO-471)**: 08 메인 그리드 **맨 끝** 점선 '앨범 만들기' 타일(09-1 '새 앨범' 타일과 같은 자리 관습 — 품질 제외 섹션 위) → 이름 모달(RenameModal 재사용 — `submitLabel`·`maxLength` prop 추가) → `POST /events/:id/albums`에 `{name}`만 실어 사진 0장 인물 앨범 생성 → 그리드 refetch(09로 보내지 않는다 — 빈 상세엔 할 게 없다). BE는 CHMO-456, **2026-07-27 실서버 프로브로 계약 확정**: `sourceAlbumId`·`photoIds`는 **짝**이라 한쪽만 실리면 `VALID400 "sourceAlbumId 와 photoIds 는 함께 보내거나 함께 생략해야 합니다."`, 검증 순서는 이름(@NotBlank·20자 — 공백만도 거부) → 짝 → 사진 수 → 이벤트 조회이고 문구도 이때 바뀌었다(종전 목의 "원본 앨범은 필수입니다."·"이름은 1~20자로 입력해 주세요."는 폐기). 이름 중복은 BE도 안 본다(personId 별도 발급). ⚠ **만든 빈 앨범은 09-1 이동 추천에 뜨지 않는다**(실측 — 얼굴 벡터 없는 수동 인물은 유사도 계산 제외, CHMO-416 AC-4): `POST /photos/move`는 빈 앨범을 대상으로 이미 정상 동작하므로 막힌 건 후보 노출뿐이고, **BE CHMO-472**(이벤트 전 앨범을 유사도 순 정렬·유사도 없으면 null로 뒤에) 배포 전까지는 사진을 모을 수 없다)·검토 완료는 일반 모드 버튼만(앨범 전체 대상이라 선택모드와 이질적이어서 선택모드 버튼 제거, CHMO-413 — 단 **확인 다이얼로그는 CHMO-488에서 복원**: 검토 해제 폐지로 비가역이 됐다)·성공 시 **다음 미검토 앨범의 09로 직행**(CHMO-521 — 08 복귀(CHMO-414)는 앨범 수만큼 왕복을 만들었다. 규칙은 `lib/reviewFlow.ts`, 남은 앨범이 없으면 14로)·라이트박스 크게 보기(CHMO-242 — 일반 모드 사진 탭, 검수 배지+개별 저장+라이트박스 내 삭제/옮기기. 사진 단위 검토 API는 BE 미도입이라 검토 액션 없음)·'분류가 어려워요' 분류 사유 표시(CHMO-412 — 앨범 상세 사진의 `faceBboxes`(애매 얼굴 bbox **배열**, 원본 px·좌상단)·`causes`(사유 코드 배열)를 소비: BE는 비면 키 생략(@JsonInclude NON_EMPTY)이라 매퍼가 빈 배열로 정규화, uncertain 앨범 라이트박스에서만 bbox 오버레이(naturalWidth 기준 환산)+사유 문구 노출. uncertain 앨범은 검토 UI(라이트박스 배지·[검토 완료]) 비노출 — 검토·발행 대상이 인물·공통뿐(CHMO-357)이라 08 카드 규칙(uncertain 배지 없음·항상 점선)과 정합. BE는 PR #107 develop 머지(2026-07-22)·**실서버 배포 미확인**, bbox·causes는 사진 최초 생성 시에만 저장(백필 없음)이라 실검증은 새 업로드로만 가능)·제작자 다운로드(CHMO-349 → **CHMO-473 앨범 직행 전환**: 일반 모드 [다운로드]=앨범 전체(미검토 포함 — 노출은 person/common 기존 규칙 유지)·선택모드 [저장]=선택 사진, 둘 다 아래 저장 파이프라인)·인물 앨범명 ✎ 이름전파·14 공개 요약+공개하기)·학부모 뷰어 흐름(잠금 해제(비번 4자→viewerToken, 401 시 apiFetch가 뷰어 토큰 삭제→잠금 화면 복귀)·15-L 공개 이벤트 목록·15 공개 앨범·16 사진 그리드+[다운로드](앨범 전체 — CHMO-473 저장 파이프라인)+개별 저장 라이트박스+공유(Web Share→복사 폴백)) 실제 UI. 설정/프로필(닉네임 프리필 — 라벨은 '이름'(CHMO-440, 소셜 표시명 이름 전환 정합)·PIN은 입력 시에만 `PATCH /me`에 포함 + 로그아웃)·404(EmptyState)도 실 UI — 스텁 없음(ScreenStub 제거, CHMO-118). 앨범 커버는 `Album.coverThumbnailUrl`(파생), 공개 요약(14) 미리보기는 `ReviewSummary.previewAlbums`(파생 — 08과 같은 앨범 카드 그리드, CHMO-346. **담는 범위는 전량 검토된 인물·공통 = 발행 대상과 같은 집합**(CHMO-488 — 종전 '검토분 1장 이상'은 공개해도 안 나갈 앨범까지 보여 줬다), 그래서 검토 범례도 없다. 14 통계는 사진/앨범/검토한 앨범 3열 — 분류 애매 카드 비노출(CHMO-347), 검토 진척은 인물·공통(사진 보유) 앨범 단위 FE 파생 `reviewedAlbumCount/reviewableAlbumCount`(CHMO-357 — BE reviewedAlbums는 특수 앨범까지 세서 안 쓰고, 완료 판정은 08 카드 테두리 규칙(미검토 0)과 동일해 통계·카드가 항상 일치. 미리보기 카드 수 = `reviewedAlbumCount`)), 뷰어 커버는 `ViewerEvent`/`ViewerAlbum.coverThumbnailUrl`(파생 — 발행(published) 사진 기준, CHMO-324). 인물 앨범 이름수정은 09 앨범 상세 헤더 + 08 카드 **이름 줄 탭**(CHMO-400 — person만, 이름 뒤 인라인 ✎ 힌트·카드 나머지 탭은 09 진입 그대로, 이름 전폭 2줄(CHMO-354) 유지. 커버 오버레이 칩은 기각) — 변경 시 모임 단위 인물(personId) 이름이 전 이벤트로 전파. **두 진입점은 CHMO-522에서 앨범 설정 시트로 넓어졌다**(아래). 공개(14)는 확인 다이얼로그→published→05 배지·뷰어 목록 노출(기존 코드/목 처리) — **미검토가 남으면 [공개하기]가 잠기고 막고 있는 앨범을 이름·장수로 안내한다**(CHMO-488, force 강행·409 자동 재시도 폐기. 재공개 안내(08 "발행 대기 N장"·14 재활성)는 삭제 — 재공개 경로 소멸). 상세는 확정 모델 검수 항목·목은 `db.ts` publish 헬퍼(PUBLISH409는 2026-07-22 실서버 채집·PUBLISH400은 소스 대조). **서비스 워딩 1차 정리(CHMO-348·353·398, 2026-07-22)** — 'AI 분석'→'사진 분류'(06-U CTA `사진 분류하기`·이벤트 배지 `분류중` — BE `progress` 등 내부/BE 도메인 주석의 '분석'은 유지), 14 화면명 '공개 전 검수'→**'공개 요약'**·08 진입 CTA '요약 보기'(검토/검수 이중 용어 해소 — CTA는 2026-07-29에 **'공개 전 요약보기'**로 다시 손봤다: 무엇의 요약인지·왜 지금 보는지가 없어 다음 단계와 안 이어졌다), 08·14 검토 범례는 미검토 먼저. '검토' 용어 자체는 **유지 확정**(CHMO-356 — 코드 무변경). **BE 실연동 기반(CHMO-191) 완료** — 실 BE 응답 봉투 흡수(`src/api/client.ts`)·에러 코드 정규화(`src/api/errors.ts`)·vite 프록시·숫자 ID(int64) 전환. **엔드포인트 계층(CHMO-192) 완료** — 화면은 URL·응답 형태를 모른다: `src/api/{auth,groups,events,albums,viewer}.ts` 도메인 함수가 경로·요청 바디·응답 변환을 소유하고(`apiFetch` 직접 호출은 `src/api/` 안에만), BE 형태 차이(필드명·대문자 enum·특수 앨범 라벨 파생)는 `src/api/mappers.ts`가 흡수한다. **목 이행(CHMO-195) 완료** — MSW 목이 실 BE 계약(봉투·BE 필드명·bare 배열)으로 응답한다: 스위치 양쪽(`VITE_ENABLE_MSW` true/false)이 같은 계약을 탄다. **업로드 3단계(CHMO-194) 완료** — presign(`{files:[{fileName,size}]}` → `[{s3Key,uploadUrl,contentType}]`) → presigned URL로 S3 직접 PUT(`Content-Type`이 서명과 정확히 일치해야 함 — 아니면 403 SignatureDoesNotMatch) → 등록(`POST /events/:id/photos` `{s3Keys, excludeEyesClosed, excludeBlurry}`). **등록이 곧 분석 시작**(BE가 analyzing 전이 + AI 분류 발행)이라 `POST /analyze`는 부르지 않는다(부르면 같은 사진이 두 job으로 발행). 업로드 제약(확장자 jpg/jpeg/png/heic/webp·20MB·50장)은 `src/lib/upload.ts`가 단일 원천으로 갖고 UI·목이 공유한다. **이제 실 BE 연동 단계 — API가 문서와 안 맞으면 BE가 기준**(하드 제약 참조). **학부모 전환 계약층(CHMO-444) 완료 — 목 선행**: 2026-07-17 법률 자문으로 무로그인 뷰어를 폐기하고 학부모도 로그인+모임 합류로 전환(정책 원천 `docs/parent-model-proposal.md`, API 초안 `docs/parent-model-api-draft.md` — §1~7 BE 협의 확정, **실 BE 미구현이라 목으로만 동작**·경로/필드명은 매퍼가 흡수하고 **구계약 공존도 매퍼가 흡수**(invite 평면→teacher·parent null, join 즉시합류→active — 코드리뷰 반영)). 멤버십이 role(teacher/parent)·승인 상태(pending/active)를 갖고(joinKey 2종이 role을 정함·참여=신청 생성·승인제), 목·api 계층에 신설 6종(join-requests 2·members·person-parents 2·parent-photos 2 — `src/mocks/handlers/parents.ts`)+변경 5종(목록 myMembership(PENDING 포함)·상세 카운트 분리+PARENT 필드 생략·이벤트 목록 PARENT published 필터+노출 사진 기준 카운트·join 신청 생성·invite 2종)+`errors.ts` ROLE403(Q5 확정)이 들어갔다. 제작자 전용 핸들러는 전부 TEACHER 관문(`membershipRoleError` 단일 구현 — PARENT는 ROLE403, 비멤버·PENDING은 404 은닉) 경유. 시드에 학부모 계정 4(민준아빠 1111 = 연결됨+별님반 대기·서연맘 2222·지호네 3333 = 미연결·치즈냥이88 4444 = 승인 대기 신청). **학부모 신청 실 UI(CHMO-445) 완료**: 02-2 참여 신청 3단계(`ParentJoinPage` — 1/3 모임 비밀번호 → 2/3 자녀 이름(복수·빈 칸 필터 + 행 ✕ 삭제·마지막 1행 유지, CHMO-448 추가) → 3/3 동의(자녀 정보 처리 동의 실계약 — CHMO-587, 아래) → 신청(PENDING) → 홈 랜딩. **참여 성공 랜딩은 02-1 모달 포함 status 무관 홈**(CHMO-448 수정 — 구계약(active) 응답이어도 상세 직행하지 않는다: 승인제 과도기 BE에서 상세 직행이 "권한이 없는 스페이스" 오류로 실측)) + 홈 카드 myMembership 소비(PENDING=뮤트+'승인 대기중' 배지+'신청: {자녀}'+탭 토스트 — 모임 API 추가 호출 없음(§7-2) · ACTIVE PARENT='학부모 · 참여 중'). 학부모 링크 분기는 FE 파생 `?role=parent` 마커가 유일한 근거(joinKey는 불투명 — invite parent 채널 joinUrl이 싣는다), 마커 없는 학부모 코드는 02-1 모달이 서버 400(자녀 이름 필요)으로 감지해 02-2로 인계(비밀번호 프리필). 1/3 [다음]은 childNames 없는 프로브 제출 — 비밀번호 오류(JOIN403)·중복(409)은 그 자리에서 잡고 400이면 2/3 진행, 실 BE 검증 순서가 달라도 최종 제출의 WRONG_PASSWORD를 1/3로 복귀시키는 안전망(목 검증 완료 — 실 BE 실검증은 CHMO-449). **05 초대 UX 개편(CHMO-446) 완료**: 통합 초대 바텀시트(05-2, `GroupInviteSheet` — 기존 InviteSheet·ParentShareSheet 2개 대체, 선생님/학부모님 세그먼트 탭·탭별 joinKey+비밀번호(학부모는 sharePassword 재사용, Q2)·링크 복사·카카오톡 공유(기존 shareOrCopy)·학부모 탭 승인/노출 안내. parent 채널 null(구계약 실 BE)이면 탭 숨기고 선생님만) + 05 헤더 [+ 초대하기] 단일 진입(하단 학부모 공유 버튼 제거)·카운트 분리 표기 "선생님 N · 학부모 N · 이벤트 N개"(§7-3 — 분리 필드 없으면 '인원 N명' 폴백)·하단 [초대 관리{ · 신청 N}] 버튼(대기 신청 수 = listJoinRequests, 실패 시 뱃지만 생략). **초대 관리 실 UI(CHMO-447) 완료**: 20 초대 관리 화면(`/groups/:id/invites`, `InviteManagePage` — 선생님/학부모님 세그먼트 탭(기본 학부모님 — 주 업무인 신청 승인·아이 연결이 학부모 쪽)·대기 신청 카드(닉네임·"{자녀} 학부모입니다" 원문·상대 시각(`src/lib/timeAgo.ts` formatTimeAgo 단일 원천)·[승인]/[거절] — 승인은 멤버 확정만(§1 승인·매핑 분리)·거절은 확인 다이얼로그)·멤버 명단(선생님 탭 "(나)" 표기 / 학부모 탭 연결 칩 ✕ 해제(확인 다이얼로그)+[+ 연결]·미연결은 "신청: {이름}" 원문 보존(§2)+[연결]) — 신청 목록 조회 실패는 명단을 가리지 않고 인라인 재시도로만 알린다) + 20-1 아이 연결 바텀시트(`ChildLinkSheet` — 인물 앨범 가로 스크롤 타일 **다중 선택 토글**, 매핑 API가 1건 단위라 선택분 순차 POST·중간 실패 시 성공분은 그대로 반영(닫기+refetch)하고 실패만 토스트(§4 다대다라 부분 성공이 반쪽 상태가 아님)·이미 연결된 인물은 '연결됨' 비활성 타일로 중복 409 예방·열릴 때만 마운트). 인물 후보는 그룹 단위 인물 엔드포인트가 계약 초안에 없어 이벤트→앨범 목록에서 FE 파생(`listGroupPersons` — personId로 합산·정렬은 albumSort 재사용, 전용 엔드포인트는 BE 후속 논의). **학부모 조회 실 UI(CHMO-448) 완료**: 홈 ACTIVE PARENT 카드 탭 → 18 학부모 모임 상세(`/parent/groups/:id`, `ParentGroupPage` — 모임명 + "학부모 · {연결 자녀명}" 헤더, published + **아이(매핑 인물) 등장 이벤트만**(서버 필터 — Q4를 좁힌 **노출 강화, ⚠ BE 미협의 제안**(2026-07-27 사용자 확정): 매핑은 인물 앨범 단위 권한이라 아이가 안 나온 이벤트는 공통이 있어도 통째 은닉·미연결은 0개, parent-photos 딥링크·홈 카드 eventCount도 같은 판정 — 목·초안 §1·§5 반영), 카드는 와이어프레임(307:26) 그대로 텍스트만(커버·상태 배지 없음 — 전부 공개된 이벤트라 배지 무의미). 멤버십·모임명은 **모임 목록(listGroups)에서 읽는다** — 상세 응답엔 myMembership이 없다. 딥링크 안전망: TEACHER·구계약(멤버십 없음)은 05로, PENDING은 홈으로 replace) → 19 학부모 사진 그리드(`/parent/groups/:id/events/:eventId`, `ParentEventPhotosPage` — parent-photos 플랫 그리드 + PhotoLightbox 재사용(개별 저장) + 선택모드(헤더 [선택]·롱프레스 진입 — 09와 같은 CHMO-243 관용, CHMO-473 추가)로 골라 저장 + 하단 고정 [↓ 전체 저장]/선택 [N장 저장](CHMO-473 저장 파이프라인 — zip 폐지), 서브타이틀 "{자녀명} · 공통 포함 N장"(이름 도착 전엔 "사진 N장" 중립 표기), 빈 상태 문구는 18·19 모두 연결 여부로 분기 — 미연결(승인 후 기본 경로)은 "선생님이 아이를 연결하면" 안내). 자녀 이름 원천은 **`myMembership.linkedChildNames`(초안 §1 확장 — CHMO-448 FE 추가 제안, BE 미협의)**: 매핑 조회(`/members`)가 TEACHER 전용이라 학부모 본인이 연결 인물명을 알 방법이 없어 목록 응답에 실었다(목은 §4 매핑에서 파생·매퍼는 생략을 빈 배열 정규화, 미연결 헤더는 "학부모"만). 이벤트 카드 날짜 표기는 `src/lib/eventDate.ts`로 승격(05·15-L 중복 정의 제거 — 18 포함 3화면 공유). 남은 실 화면은 CHMO-449(뷰어 폐기·실연동, BE 배포 후)로 이어진다. 기존 뷰어(/share) 계열은 이관 완료까지 유지. **사진 저장 앨범 직행(CHMO-473) 완료 — ZIP 전면 폐지**: 모든 사진 저장(라이트박스 개별·09 선택/전체·16 [다운로드]·19 [전체 저장])이 `src/lib/photoSave.ts`+`usePhotoSave` 파이프라인 하나를 탄다. iOS(iPadOS 포함 판별+`canShare({files})`)는 원본 선요청(동시 3장·`no-store` — CHMO-326 캐시 함정 동일) 후 공유 시트 `navigator.share({files})`의 시스템 '이미지 저장'으로 사진 앱 직행 — Safari transient activation이 엄격해(탭과 같은 틱 수준) 2단계 UX: [저장] 탭→준비 진행률→준비 직후 자동 share 1회 시도(제스처 생존 시 탭 절약)→만료(NotAllowedError)면 [사진 앱에 저장] 재탭. 배치 20장(`SHARE_BATCH_MAX`) — **준비도 배치 단위**라 메모리 피크는 배치 1~2개(총량 아님)·ready 동안 다음 배치 프리페치로 대기 상쇄·run 세대 가드로 reset 후 늦은 완료의 오염 차단, 배치마다 시트 1회(버튼 'k/n' 표기), 시트 취소(AbortError)는 같은 배치 대기·무토스트, **수동 탭 연속 2회 거부(인앱 등 공유 차단 환경)면 파일 다운로드 폴백**(준비분 재사용·미준비 배치는 그때 받아 흘림, iOS 파일 앱행이라 도착지 토스트 안내 — 인앱에서 다운로드 자체가 되는지는 실기기 검증 항목). Android는 공유 시트에 시스템 '갤러리 저장'이 없어 장별 blob 다운로드(Download 폴더 → 미디어 색인으로 갤러리 자동 노출, 시작 전 '다운로드 허용' 안내 토스트)·데스크탑/미지원도 동일 폴백. 부분 실패는 성공분 진행+실패 수 토스트. BE ZIP 엔드포인트는 존치하되 FE 미호출(zip api 함수 3종은 계약 테스트 고정용 잔존). ⚠ 실기기 검증 필수(iOS 시트 '이미지 저장' 노출 — iOS 16 누락 버그 이력 · Android 갤러리 색인). **약관·개인정보 문서 3종 + 화면 고지(CHMO-478) 완료**: 전문 3종(이용약관·개인정보 처리방침·얼굴 특징정보 처리 안내)은 `docs/legal/*.md`가 정본(변호사 검토용 — `[[결정필요]]`/`[[검토필요]]` 마커가 미확정 지점)이고 `src/legal/*.ts`가 앱 적용판(마커 자리 중립 치환 — **사실과 다른 고지 금지**: 임베딩 암호화 문구·국외 이전·보호책임자는 확정 전이라 미노출) — 둘은 반드시 함께 수정. `/legal/{terms,privacy,biometric}` 라우트(공용 렌더러 LegalDocPage + Header `onBack` 신설(진입점 복수라 절대경로 backTo 불가 — 히스토리 복귀+직진입 랜딩 폴백), **가드 밖** = 스토어 심사용 공개 URL 겸용(CHMO-321 전제))·설정 약관 링크 3종·06-U 얼굴 인식 고지 배너·02-2 이름 사용 목적 안내·03 보호자 동의 확인 게이트(체크 전 CTA 비활성). ⚠ **19 재배포 금지 안내 배너는 제거**(2026-07-29 사용자 피드백) — 아이 사진을 보러 온 학부모의 첫 화면에서 "당신이 위법할 수 있다"는 경고부터 읽혀 인상이 나빴다. 같은 의무는 이용약관 제14조(학부모 회원의 이용 범위)에 그대로 있다. 이용약관은 무효 조항 회피 4원칙(고의·중과실 유보·유출 무과실 면책 금지·단계적 자격상실+30일 소명·키즈노트식 초상권 갈음 조항 미이식) 반영. 가입 동의 체크박스(§1) 실연동은 BE 동의 기록(카탈로그·버전·서버 시각 append-only) 계약 확정 후속. **첫 사용 온보딩 화면 설계(CHMO-480) 완료 — 설계만, 구현은 CHMO-481**: 로그인 직후 1회 노출할 슬라이드 3장(화면 번호 `00`)을 피그마 와이어프레임(`348:4`·`349:4`·`349:19` — 찐와프 페이지)과 `docs/screen-spec.md` §3 카드로 확정했다. **역할로 나누지 않는다**(2026-07-28 사용자 지적 반영) — 계정에는 role이 없고 role은 멤버십(모임별) 속성이라(joinKey 2종이 role을 정함) 같은 사용자가 모임마다 선생님·학부모로 갈리고 가입 시점엔 어느 쪽인지 알 수 없다. 그래서 공통 1종이고 마지막 장이 진입점 둘([모임 만들기]→03 / [초대 코드로 참여]→02-1)로 갈라주는 역할만 한다. 문구는 분류→검토→**초대 1회** 3단(워딩 규칙 준수 — 'AI 분석'이 아니라 '사진 분류'). 3장 초안 "링크 하나로 학부모에게 전달해요"는 **사진 올릴 때마다 링크를 보내는 것처럼 읽혀 폐기**하고 "한 번 초대하면 / 공개할 때마다 바로 봐요"로 잡았다(참여 링크는 합류 초대용 1회고, 이후엔 공개만 하면 합류·연결된 학부모 화면에 뜬다). 비주얼은 신규 일러스트 없이 기존 토큰·형태 재사용(치즈 심볼·`photo` 플레이스홀더·인물 원형 타일). 노출 판정은 BE에 신규 가입 신호가 없어(`AuthResponse` 평면 필드·소셜 exchange도 신규/기존 무구분) FE 로컬 플래그로 가고, 초대 링크 `returnTo`가 있으면 온보딩을 건너뛴다(참여 흐름을 끊지 않게). 계정 단위 서버 저장은 BE 협의 후속(screen-spec §5-6). **선생님 합류 승인제 대응(CHMO-502) 완료**: BE CHMO-475로 선생님 키 합류도 PENDING이 되면서(확정 모델 참조) 화면 4곳을 맞췄다 — ① 02-1 모달 문구를 '참여'→**'참여 신청'**(제목·CTA·승인 안내 한 줄. 랜딩은 종전대로 status 무관 홈) ② 홈 대기 카드가 **카운트 줄로 폴백하지 않는다**(실 BE는 대기 항목에 `memberCount` 생략 + `eventCount` 0을 준다 — 선생님 신청은 자녀 이름도 없어 그대로 두면 "이벤트 0"이 빈 모임처럼 읽힌다. 서브텍스트는 원문 없으면 '선생님으로 참여 신청'/'학부모로 참여 신청') ③ 20 초대 관리 **탭별 대기 수 뱃지**(기본 탭이 학부모님이라 선생님 신청이 묻힌다 — 목록엔 role=TEACHER·childNames 빈 배열로 섞여 온다) ④ `errors.ts`에 `SPACE403`→`PENDING_APPROVAL` 등록 + ErrorState가 **403도 영구 실패로 취급**(재시도 대신 복귀 CTA — 승인 전 딥링크가 막다른 화면이 되지 않게). 목은 이미 role 무관 pending이라 계약 변경은 대기 항목 `eventCount: 0`뿐이고, 시드에 선생님 대기 신청(신입쌤 5555)을 넣어 20 선생님 탭을 볼 수 있게 했다. 화면·기능 결정의 소스 오브 트루스는 여전히 `docs/`. **2026-07-28 정책 변경(업로드 1회·전량 검토 공개 게이트)은 문서 3종에 반영 완료(CHMO-489)** — feature-spec §4 상태머신은 단방향(재분석 회귀·published 증분 폐지)·F5.5 사진 추가 삭제·F5.4에 검토 비가역 명시, api-spec §3.4는 CHMO-194 이전 파이프라인(Lambda 자동 등록·`photoId` 선발급·별도 `POST /analyze`·고아 사진 경고)을 걷어내고 현행 3단계(presign→S3 PUT→등록=분석 시작)로 교체(미등록 S3 잔존물은 BE CHMO-484 참조), screen-spec은 08 `[+ 사진 추가]`·IA 분기 삭제. ⚠ **2회차 등록 거부의 에러 코드는 아직 미확정**(BE CHMO-485 AC-4) — 목은 임시로 `VALID400`을 쓴다. **치즈모아 둘러보기(CHMO-504) 완료(⚠ CHMO-578에서 화면째 삭제 — 첫 안내는 코치 힌트 전담) — 화면 `00-T`, `src/components/AppTour.tsx`**: `모임 > 이벤트 > 앨범` **정보구조**와 **한 바퀴 흐름**을 눌러서 체감시키는 전체 화면 투어 — **선생님 7단**(모임→이벤트→06-U 올리기→08 앨범→09 검토→14 공개→공개된 이벤트) · **학부모 4단**(모임→18 이벤트→19 사진→저장). 초안은 3단에서 끊고 검수·공개는 "온보딩 슬라이드가 말로 설명하니 제외"였는데 **그 슬라이드를 안 띄우게 되면서 아무도 설명하지 않게 돼** 끝까지 넣었다(2026-07-29). **탭 대상은 그 화면의 진짜 동작을 따른다** — 들어갈 게 있으면 카드, 실행할 게 있으면 버튼(`[사진 분류하기]`·`[검토 완료]`·`[공개하기]`·`[전체 저장]`). 어디서나 카드만 누르게 하면 실제 앱과 손버릇이 어긋난다. 축소된 앱 화면을 직접 그리고 탭하면 다음 계층으로 넘어가는 방식이라 **서버 호출·라우팅 이동이 없고 모임이 0개인 신규 가입자에게도 돌아간다**(실제 화면 코치마크는 가리킬 대상이 없어 기각, 샘플 모임 자동 생성은 BE 작업+삭제 책임까지 필요해 기각). 무대는 실물 카드 컴포넌트(`GroupCard`·`EventCard`·`AlbumCard`·`PhotoGrid`)로 조립해 화면 디자인이 바뀌면 투어도 따라 바뀐다. **역할로 갈라 보여준다**(2026-07-28 사용자 지적) — 계정엔 role이 없지만 **구조는 역할마다 실제로 다르다**: 선생님은 모임→이벤트→앨범 3단, 학부모는 **앨범 단계가 없다**(18·19 — 아이 사진이 한 화면에 펼쳐진다). 선생님 흐름만 보여주면 학부모로 합류한 사람은 자기 화면을 못 보므로 첫 장에서 갈래를 고르고 마지막 장에서 반대편으로 건너뛴다(고른 갈래는 계정에 남기지 않는다 — role은 멤버십(모임별) 속성). **초안(310px `Modal` 안 미니 화면)은 "촌스럽다"는 피드백으로 폐기**(2026-07-28) — 낡아 보인 원인 셋을 걷어냈다: ① 액자(가짜 `‹ 홈` 상단 바) 제거, 전체 화면 여백 위에 실물 카드만 띄우고 어느 화면인지는 실제 화면의 제목·서브텍스트로 알린다 ② 카드 전체를 감싼 점멸 링(핫스팟 — 가장 낡은 튜토리얼 패턴)을 카드 모서리 작은 탭 물결 하나로 축소 ③ 일곱 겹(제목·설명·`1/3` 카운터·링크·버튼 2개…)을 **무대/문구/액션 1개** 세 겹으로 줄이고 진행은 상단 세그먼트 바 3칸이 맡는다(한 화면 한 가지). 전환 애니메이션 `step-in`·탭 물결 `tap-ripple`은 tailwind.config keyframes. **노출은 첫 로그인 1회 자동**(⚠ 자동 노출은 CHMO-565에서 폐지 — 설정 pull 전용. 홈 위에서 연다 — `postLoginDestination`은 언제나 홈이고 별도 화면으로 튕기지 않는다). **단 초대 링크로 합류한 방문은 한 박자 미룬다**(2026-07-30 — 참여 흐름 02-1·02-2가 홈으로 보낼 때 `state.fromJoin`을 싣고 홈이 그 방문만 건너뛴다: 그 사람이 홈에 온 이유는 '앱을 처음 열었다'가 아니라 '참여 신청을 막 끝냈다'라서 투어가 신청 완료 토스트·승인 대기 카드를 덮고 자녀 이름까지 적은 사람에게 첫 장이 다시 역할을 묻는다. 완료 플래그는 소진하지 않아 **다음 홈 방문에 뜬다**. 종전 `!joinOpen` 가드가 이걸 노렸지만 `openJoin` 신호를 보내는 곳이 노출 중단된 00 슬라이드뿐이라 실제 초대 흐름엔 안 걸렸다). 그때 뜨는 투어는 **초대 링크가 밝힌 갈래로 열려 첫 장(역할 고르기)을 건너뛴다**(학부모 링크=parent·선생님 키=teacher를 `lib/onboarding.ts`가 계정별로 저장 → 홈이 `initialTrack`으로 전달. 자동 노출이 다음 방문으로 밀려 navigate state로는 못 넘긴다. 설정에서 다시 볼 때는 넘기지 않는다 — 반대편 흐름을 보러 오는 경우가 있다. role을 계정에 박는 게 아니라 '들어온 문'의 기록) + 설정 '치즈모아 둘러보기'(다시 보는 유일한 경로). **자동 노출은 무조건 최초 1회**(2026-07-29) — '봤다' 기록을 **닫을 때가 아니라 뜰 때** 남긴다(종전엔 투어 도중 새로고침·뒤로가기로 이탈하면 다음 홈 방문에 또 떴다)·홈 빈 상태 `[치즈모아 둘러보기]` 버튼은 제거(모임 0개인 동안 상시 노출돼 '1회'와 어긋났다). ⚠ **첫 사용 온보딩 슬라이드(00)는 일단 노출하지 않는다**(같은 결정) — 로그인 후 목적지·설정 링크 등 진입점을 모두 내렸고 화면·라우트(`/onboarding`)는 되살릴 수 있게 남겼다. 노출 판정 플래그(`lib/onboarding.ts`)는 **둘이 공유한다**(따로 세면 슬라이드를 되살릴 때 이미 본 사람에게 다시 뜬다). **그라데이션 전면 폐지(CHMO-512) 완료 — UI 면은 전부 단색**: `tailwind.config.js`의 `backgroundImage` 3종(`gradient-cheddar`·`gradient-primary`·`gradient-emblem`)을 걷어냈다. ① primary는 **`#F5B82E` → `#FFC93C`**(전자는 파랑이 섞여(B=46) 탁해서 "치즈"보다 "머스터드"로 읽혔다 — 후보 5종을 실제 05 화면에 그려 대조 후 선택) ② **연한 옐로우(구 `gradient-primary`)는 되살리지 않는다** — 종전 sm 버튼은 면 안의 음영이 모양을 만들고 있었는데 단색이 되자 cream 배경 대비 1.4:1로 묻혀 글자만 떴다(05 `＋ 초대하기` 실측). primary는 크기 무관 같은 색이고 **경계는 색이 아니라 그림자가 만든다** — sm 전용 `shadow-card-sm`(`0 3px 10px/.16`, md의 `0 8px 30px`은 h36에서 아래로 번진다) 신설, primary 버튼은 양쪽 크기 다 그림자를 진다(크기 위계는 높이·radius 담당) ③ **로고 심볼 뒤 갈색 엠블럼 타일 폐지**(CHMO-351 반전 — 배경과 떼어놓으려던 색면이 무거웠다): `BrandHero`(심볼 64→88·그림자 제거)·`GroupCard`(28→40)·00-T 역할 선택(30→44) 전부 심볼 단독, **슬롯 크기는 유지**해 정렬이 안 흔들린다 ④ **로고 심볼(`Cheddar.tsx`) 자체는 무변경** — 브랜드 자산이라 내부 SVG 그라데이션 유지(2026-07-29 사용자 결정. 원본 렌즈의 크림색 링이 흰 배경에서 형태를 잡아 준다) ⑤ `.cheese-dots`의 `radial-gradient`는 **점 패턴이라 예외**(빼면 단색이 아니라 텍스처가 사라진다 — 점 색만 새 primary로, 투명도 .18→.22) ⑥ 곁가지로 `ParentJoinPage` 동의 체크 ✓가 `text-white`였던 걸 `text-heading`으로 교정(옐로우 위 흰 글자 금지 규칙 위반이 노랑이 밝아지며 드러났다). **홈 정리 + 서체 Jua 단일화(CHMO-513) 완료 — 2026-07-30 시안 검토로 확정한 두 변경**: ① **모임 참여 진입점 삭제** — 홈 하단 보조 버튼 `[모임 참여하기]`를 없애 하단은 `[＋ 모임 만들기]` 하나뿐이고, **참여는 링크로만** 이뤄진다(받은 링크가 02-1·02-2로 곧장 데려간다 — 코드를 받아 온 사람만 쓰던 문을 모두에게 상시 노출할 이유가 없다). `HomePage`에서 `JoinGroupModal`·`joinOpen`·`openJoin`(HomeLocationState)을 걷어냈고 **링크 진입 화면과 모달 자체는 그대로 산다**(`/join/:joinKey` JoinPage가 계속 쓴다). 빈 상태 설명은 **"첫 모임을 만들어 보세요."**(종전 "초대받은 모임에 참여해 보세요"는 앱 안에 그 방법이 없어져 사실과 달라졌다 — 시안 안 ㄴ 채택, 링크 안내를 덧붙인 안 ㄱ은 기각). 노출 중단된 00 슬라이드의 `초대 코드로 참여하기` 링크도 신호를 받을 곳이 없어져 안내 한 줄로 교체. ⚠ 딸린 미결정: **05 초대 시트의 '참여 코드' 텍스트 노출**(받는 쪽 입력란이 사라져 죽은 정보 — screen-spec §5-7) ② **서체 Jua 한 벌**(시안 A안 — 로고와 본문이 같은 얼굴. Gothic A1 폐지·`font-display` 토큰과 사용처 4곳 제거·구글 폰트 요청 4건→1건): **Jua는 굵기가 400 하나뿐이라 굵기로 위계를 만들 수 없다** — `index.css`에 `font-synthesis: none`을 걸어 합성 볼드를 차단하고(안 막으면 브라우저가 가짜 굵기를 만들어 획이 뭉갠다) `font-bold`는 의미 표시로만 남긴다(시각 효과 없음). 굵기가 유일한 구분이던 두 자리는 다시 벌렸다: **섹션 라벨 8곳**(홈 '모임'·05 '이벤트'·08 '품질 제외'·14 '미리보기'·20 명단 머리글 2·시트 소제목 2) = `12px + 자간 0.06em + muted`로 바로 위 13px 본문과 크기로 갈라지고, **약관 전문 조문 제목**은 장(17px heading) > 조(15px heading) > 본문(14px text)으로 **색이 목차를 만든다**(조문 수십 개가 본문과 한 덩어리로 읽히던 문제). 실측 확인은 실 BE 계정으로 홈·05·08 + 가드 밖 화면(01·약관·`/dev/components`)까지 했고, `docs/design/screen-system.dc.html`도 Jua·굵기 400으로 맞췄다(단 **주석 글(.note)은 중립 서체 유지** — 캔버스가 절대 좌표라 앱 서체로 두면 설명이 길어져 아래 블록과 겹친다). **05 모임·이벤트 구별 강화(CHMO-515) 완료 — "모임이랑 이벤트가 잘 구별이 안 된다"는 화면 피드백**: 원인은 셋이었다(홈 모임 카드와 05 이벤트 카드의 실루엣이 같다 · 헤더가 `모임 상세`라고만 말하며 본문 큰 제목과 역할이 겹친다 · 이벤트는 사진 묶음인데 카드가 사진을 안 보여준다). 시안 3안(좌측 커버 1장 / 사진 스트립 / **전면 커버**)을 실제 05 화면에 그려 대조한 뒤 **전면 커버 채택**(사용자 결정) — ① **이벤트 카드 상단 전면 커버**(높이 128, `EventCard`의 `cover` prop): 원천은 이벤트 **목록** 응답의 `thumbnailUrl`로 **BE 변경이 없다**(`toEvent` 매퍼가 버리던 값을 `EventItem.coverThumbnailUrl`로 살렸다 — 뷰어 `toViewerEvent`는 같은 필드를 이미 쓰고 있었다. **상세 응답엔 thumbnailUrl이 없으니** 카드 원천을 상세로 바꾸면 커버가 통째 사라진다 — 계약 테스트가 이 비대칭을 고정). **사진이 있는 이벤트만 커버를 진다**: 사진 0장은 커버 없는 컴팩트 카드로 남아 "아직 올릴 차례"가 목록에서 보이고, 등록 직후 분류중(사진 반영이 분석 커밋 시점)도 그 구간을 컴팩트로 지나간다. 커버 URL이 없으면 치즈 도트 플레이스홀더(투어 무대가 이 상태) ② **모임 = 색면, 이벤트 = 흰 카드** — 모임명·인원·`＋ 초대하기`를 `surface` 블록(r18)으로 묶었다(⚠ 이 블록은 **CHMO-530에서 폐지**됐다 — 모임명이 헤더로 올라가며 색면 쪽이 사라졌다) ③ **헤더 타이틀 비움** — `모임 상세`는 어느 모임인지도 아래 목록이 뭔지도 말하지 않았다. 06-E·분류중 헤더가 이미 타이틀 없이 도는 꼴을 따른 것이고(08 앨범 그리드 분기의 `이벤트 상세` 타이틀은 같은 중복이지만 범위 밖으로 뒀다), 18 학부모 모임 상세도 같이 비웠다 ④ **이벤트 수는 목록 섹션 라벨로 이동**(`이벤트 N개`) — 모임 블록 메타는 `선생님 N · 학부모 N`만(인원과 한 줄에 섞이면 이벤트가 모임의 속성처럼 읽혔다) ⑤ **`모임 상세` 워딩 잔재 정리** — 20 뒤로가기(→`모임`)·08 폴백(→`뒤로`)·20 본문 안내 2곳(→`모임 화면의`)·에러 복귀 CTA 5곳(→`모임으로`) ⑥ 둘러보기(00-T)·`/dev/components`도 같은 카드 꼴로 맞췄다(투어는 사진이 없어 플레이스홀더 커버). ⚠ **받은 시안 중 셋은 반영하지 않았다** — `분류중 72%`(진행률은 상세 응답 전용이라 목록에서 못 쓴다, CHMO-287)·`인원 22명`(이벤트 API에 인원 필드 없음 — 확정)·`권한 관리` 워딩(권한 등급이 없는 모델이라 없는 개념을 만든다 — `초대 관리` 유지). `이벤트/멤버` 세그먼트 탭은 20 초대 관리 흡수 결정이 선행돼야 해 보류, **사진 스트립은 목록 응답에 커버 1장뿐이라 BE 미리보기 URL 배열 필드가 필요한 후속**(전면 커버 자리를 바꾸는 변경이라 지금 넣은 걸 버리지 않는다). ⚠ 실기기 확인 항목: 목록 썸네일 원본 크기가 문서에 없어 **358px 폭 커버의 선명도**는 실 BE 사진으로 봐야 안다(뭉개지면 커버 높이를 낮추거나 좌측 56px 커버로 전환 — 카드 한 곳 수정). **업로드 보호자 동의 확인 게이트 + 약관 동의 API 계층(CHMO-516) 완료 — BE CHMO-514(PR #154) 계약 선행 대응**: BE가 presign에 아동 보호자 동의 확보 확인을 걸어(`AGREEMENT428` 428 Precondition Required — 그 모임의 `CHILD_CONSENT_ATTESTED` 현재 버전 기록이 없으면 발급 거부), 기록 없는 기존 선생님은 **BE만 먼저 배포되면 모든 업로드가 즉시 막힌다**. FE는 셋을 붙였다 — ① `src/api/agreements.ts` 3종(`GET /agreements` 카탈로그+내 동의 상태 · `POST /agreements` 회원 동의 제출 · `POST /groups/:id/agreements` 보호자 동의 확보 확인 `{version}`) + `errors.ts` `AGREEMENT428`→`GUARDIAN_CONSENT_REQUIRED` + MSW 목(`handlers/agreements.ts` — 검증 순서·문구를 BE `SubmitAgreementsUseCase`와 동일하게, 시드는 기록 없음이라 목에서도 428 경로가 보인다) ② **06-U 게이트 대응**: presign 428은 에러가 아니라 조건이 빈 것이라 문구 대신 확인 모달(`GuardianConsentDialog`)을 띄우고 → 확인 기록 → **presign부터 재시도**한다(①에서 걸려 S3에 올라간 파일이 없어 중복이 없고 사진 선택도 그대로 남는다). 확인은 **모임 단위·선생님별 1회**라 다음 업로드엔 안 뜬다(다른 선생님의 확인으로 갈음되지 않는다 — BE `GuardianConsentGuard`) ③ **03 이중 확인 제거**: 모임 생성 성공 직후 확인 기록을 자동 전송(멱등)해 만든 사람은 첫 업로드에서 같은 문장을 두 번 읽지 않는다(실패해도 조용히 넘긴다 — 업로드 모달이 안전망). 문구 원천은 `src/legal/consents.ts` 하나(03 체크박스와 06-U 모달이 공유 — 서버에 남는 기록이 하나인데 문장이 둘이면 증빙과 화면이 어긋난다). ⚠ **버전의 원천은 아직 서버다** — BE가 `currentVersion`을 주고 FE가 되돌려주는 에코라, 화면에 보여준 문구가 정말 그 버전인지는 아무도 안 지킨다(FE 문서에 버전을 박는 건 CHMO-517, 가입 동의 화면은 CHMO-479, 학부모 3/3은 CHMO-587에서 실계약 교체). ⚠ 함정: `GET /agreements`의 `CHILD_CONSENT_ATTESTED`는 모임별이라 `agreed`가 **항상 false** — 가입 게이트를 `required && !agreed`로 계산하면 영원히 못 넘어가므로 `scope==="user"`만 걸러야 한다. ⚠ **배포는 FE 선행 또는 동시**(FE가 먼저 나가도 428이 안 오면 기존 동작 그대로라 무해). **검토 동선 이어가기(CHMO-521) 완료 — 09 [검토 완료]가 08로 나가지 않는다**: 검토는 앨범 단위로 반복하는 작업인데 완료할 때마다 그리드로 나갔다 다음 카드를 눈으로 찾아 다시 들어가야 해서 왕복이 앨범 수만큼 생겼다(CHMO-414 반전). 이제 **다음 미검토 앨범의 09를 바로 열고**(토스트가 다음 앨범 이름을 알린다) **남은 앨범이 없으면 14 공개 요약으로 보낸다**(그때가 곧 공개할 차례). 규칙은 `src/lib/reviewFlow.ts` 단일 원천 — 08 그리드 표시 순서(`albumSort`)에서 **현재 앨범 다음부터** 찾고 끝나면 앞쪽으로 순환, 후보는 **인물·공통 + 사진 보유 + 미검토 잔여**(특수 앨범은 검토 UI가 없어(CHMO-357) 넣으면 영영 못 끄는 앨범을 무한히 다시 연다 · `unreviewedPhotoCount`가 없으면(계약상 optional) 미검토로 단정하지 않는다 · 사진 0장은 CHMO-418 잔류 앨범이라 건너뛴다). 이동은 09→09·09→14 모두 **replace**(검토 완료가 비가역이라 지나온 앨범을 되짚을 이유가 없고, 09가 쌓이면 08로 나가는 데 뒤로가기를 앨범 수만큼 눌러야 한다 — 어느 단계에서 뒤로가도 08로 나간다). 앨범 목록은 09 진입 시 미리 읽고(완료 시점에 조회하면 그 대기가 이동 앞에 붙는다) **조회 실패면 종전대로 08 복귀**(검토 자체는 이미 성공했으니 에러로 알리지 않는다), 09-1 이동·새 앨범 생성 성공 시엔 목록도 함께 refetch해 다음 대상 판정이 옛 수치를 보지 않게 한다. ⚠ **09→09는 같은 라우트라 컴포넌트가 재사용된다** — `key={albumId}` 래퍼로 화면 상태를 통째로 새로 만든다(안 하면 방금 검토를 끝낸 `busy=true`나 열려 있던 라이트박스·선택모드가 딸려 가 잠긴 화면이 열린다). 곁들여 **14에 '공개되지 않는 앨범' 고지**(`ReviewSummary.excludedAlbums` 파생 = BE `albums[]` 중 `!visibleToViewer && photoCount > 0`): 미리보기 아래에 "학부모에게는 인물·공통 앨범만 보여요 / 분류가 어려워요 6장 · 눈감은 사진 3장은 공개 대상이 아니에요". 그 앨범들은 검토 동선에 한 번도 등장하지 않아(CHMO-357) 검토를 다 끝내고 온 사람에게 사진의 행방이 사라진다 — 카드가 아니라 문장인 건 미리보기 그리드가 공개 대상만 담기 때문이고(CHMO-347), 문구가 시제 중립인 건 공개 후 재진입에도 사실이어야 하기 때문(빈 특수 앨범은 알릴 것이 0장이라 제외). **08 앨범 구획 기준 재편(CHMO-523) 완료 — 기준이 '품질'에서 "공개하면 학부모에게 나가는가"로 바뀌었다**: 메인 3열 그리드는 **인물·공통**(검수·발행 대상)이고, `분류가 어려워요`가 눈감음·흔들림과 함께 하단 섹션 **"공개해도 학부모에게 안 보여요"**로 내려갔다(CHMO-114 반전 — 종전엔 '재분류 대상'이라 메인 그리드였다). 이유는 메인 그리드가 **검토 테두리·배지·범례를 공유하는 자리**라는 것: 검토 UI도 발행 대상도 아닌 앨범(CHMO-357)이 거기 섞이면 "이것도 검토해야 하나"로 읽히고, 14 검토 진척·공개 게이트(CHMO-488)가 세는 범위와도 어긋났다. 구획 판정은 타입 열거가 아니라 파생 필드 `visibleToViewer`(`person`·`common`만 true — `mappers.toAlbum` 단일 원천, 14 집계와 같은 필드라 두 화면이 함께 움직인다). 라벨을 명사(`공개 제외`)로 두지 않은 건 선생님이 그 앨범들을 **지금 그 화면에서 보고 있어서** 누구에게 안 보이는지가 빠지면 자기모순으로 읽히기 때문이고, `N장 · 재분류 대상` 메타·🤔 커버는 남겨 할 일은 계속 드러낸다. 범례는 메인 그리드 몫이라 인물·공통이 0개면 걷는다. 바로 위 CHMO-521의 **14 비공개 앨범 고지와 한 짝**이다 — 같은 사실(학부모에게는 인물·공통만 나간다)을 08은 **구획**으로, 14는 **문장**으로 말한다. **08 헤더 이벤트명 + 앨범 설정 시트(CHMO-522) 완료 — 앨범 하나에 대한 조작을 한 시트로 모았다**: ① **08 헤더 중앙 = 이벤트명**, 본문 큰 제목 삭제(`이벤트 상세`라는 화면 종류명과 본문 이벤트명이 같은 자리를 두 번 쓰고 있었다 — CHMO-515가 "별도 판단"으로 남긴 중복). 05는 같은 중복을 헤더를 **비워** 풀었는데 08은 **채운** 이유는 하나다: 05는 본문 제목이 남았고 08은 그 제목을 없애 **스크롤해도 이름이 보이는 쪽**을 골랐다(앨범 그리드는 스크롤이 길어 본문 제목이 곧 사라진다). 긴 이름 잘림을 줄이려 뒤로가기를 `‹ 이벤트 목록`→**`‹ 모임`**으로 줄이고 09 뒤로가기도 이벤트명으로 맞췄다(09는 이동 확인용으로 이미 `getEvent`를 하고 있어 요청이 늘지 않는다 — 도착 전엔 `이벤트` 중립 폴백). 06-E·분류중 분기는 상태 배지가 본문 제목 옆이라 종전 배치 유지 ② **`AlbumSettingsSheet` 신설** — 인물 앨범의 **이름 수정 + 학부모 연결/해제**를 한 바텀시트에 담고, 진입점 둘(08 카드 이름 줄 탭 · 09 헤더 `[✎ 앨범 설정]`)이 같은 시트를 연다. 09에서는 헤더 🗑까지 시트 하단 `[앨범 삭제]`로 합쳤고(확인 다이얼로그·08 복귀는 종전대로 화면 소유 — 앨범이 사라지면 나가야 하므로), **08 진입에는 삭제를 두지 않는다**(카드 이름 줄 탭에서 앨범이 사라지는 동작까지 열지 않는다). 특수 앨범은 이름·연결 대상이 아니라 종전처럼 🗑만 ③ **학부모 연결은 20 초대 관리의 반대 방향**이다(20-1은 학부모를 골라 아이를 붙이고, 여기선 아이를 보면서 학부모를 붙인다 — 매핑이 다대다라 같은 API `person-parents`를 공유). 연결 단위가 **personId(모임 단위 인물)**라 한 번 연결하면 그 모임 전 이벤트에 걸쳐 적용되고 시트 문구도 그렇게 말한다. 연결됨=칩+✕(확인 다이얼로그)·미연결=명단 행+[연결]이며 **신청 원문(`신청: 서연`)을 함께 보여** 누가 이 아이의 학부모인지 알아볼 단서를 남긴다(§2). 이름 저장은 성공 시 시트를 닫고(호출부가 든 `album` prop이 옛 이름이라 열어 두면 부제·placeholder가 어긋난다) 연결/해제는 시트를 유지하고 명단만 다시 읽는다 ④ **명단 조회 실패가 이름 수정을 막지 않는다** — 섹션 안 인라인 재시도 한 줄로만 알린다(멤버 조회는 TEACHER 전용이라 구계약 실 BE에선 이 섹션만 빈다) ⑤ `BottomSheet bodyScrollable` 신설 — 기본 `touch-action: pan-x`가 **자손의 세로 스크롤까지 막아** 시트 안 명단을 스크롤할 수 없었다(조상의 pan-x가 교집합으로 내려온다). 이 모드는 세로 팬을 브라우저에 남기고 끌어내려 닫기를 그랩 핸들·제목 영역에 맡긴다(기존 시트 3곳은 prop 미지정 = 동작 불변). ⚠ **`person-parents`·`members`는 실 BE 미구현이라 목 모드에서만 동작**(검증도 목 — 햇살반 → 6.15 운동회 오전 → 김민준) · iOS 실기기에서 시트 본문 스크롤 확인 필요. **초대 진입점 통합(CHMO-520) 완료 — 초대는 화면 하나(`20`), 05-2 시트 폐지**: 05 하단에 `[＋ 이벤트 생성]`(primary)과 `[초대 관리]`(secondary)가 같은 무게로 쌓여 있던 걸 정리했다 — 하단 고정 영역은 그 화면에서 **할 일**의 자리인데 초대 관리는 다른 화면으로 **가는 일**이었고, `[＋ 초대하기]`(링크 보내기)와 `[초대 관리]`(승인·아이 연결)는 실제로 같은 국면에서 왕복하는 한 쌍(링크 보냄 → 신청 들어옴 → 승인 → 연결)인데 화면 양 끝에 떨어져 있었다. 시안 4종(A 시트에 관리 링크 한 줄 흡수 / B 인원 카운트 줄을 탭 대상으로 / C 멤버 카드 신설 / D 화면 통합)을 실제 토큰으로 그려 대조한 뒤 **D 채택**(2026-07-30 사용자 결정) — **결정 근거는 취향이 아니라 중복이다**: `GroupInviteSheet`와 `InviteManagePage`가 `선생님/학부모님` 세그먼트 탭을 각자 한 벌씩 갖고 있었고, 같은 축으로 두 번 가르는 화면이 둘이라는 건 원래 하나였다는 뜻이다. 그래서 ① 05 하단은 `[＋ 이벤트 생성]` 하나 ② 모임 블록의 `＋ 초대하기`가 시트를 열지 않고 20으로 직행(`ButtonLink` — 순수 이동이라 앵커 시맨틱) — ⚠ **①·②는 CHMO-530에서 반전됐다**(초대 진입점이 하단 `[초대 관리]`로 돌아오고 모임 블록은 폐지 — 아래 참조) ③ 시트 본문은 `components/GroupInviteLinks.tsx`로 이관해 20의 첫 섹션이 됐고 `GroupInviteSheet.tsx`는 삭제 — 이제 **탭 하나가 그 역할의 한 바퀴를 통째로 바꾼다**(선생님 탭 = 선생님 링크 + 선생님 신청 + 선생님 명단, 섹션 순서는 부르기 → 기다림 → 들어온 사람) ④ 20 헤더 타이틀 `초대 관리`→**`초대`**·EmptyState의 "모임 화면의 [＋ 초대하기]에서" 자기 참조 정리(이제 이 화면 위쪽이 그 자리다). **대기 신청 수는 05 표면에 남긴다**(승인은 학부모가 기다리는 일이라 들어가 봐야 아는 정보로 내리면 며칠씩 방치된다) — 다만 버튼 코너 오버레이가 아니라 **라벨 안쪽 인라인 뱃지**이고, 20 탭 뱃지와 같은 컴포넌트로 뽑아 한 모양으로 맞췄다(`ui/Badge.tsx`의 `CountBadge` — `accent` 갈색: 안내용 빨간 글씨 금지 톤 규칙 + 옐로우 버튼 위에서 대비가 서는 색이 accent뿐). **세 섹션이 서로를 가리지 않는다** — 초대 링크 섹션은 **멤버 조회 밖**에 둬서 명단이 일시 실패해도 링크는 꺼낼 수 있고(링크 복사가 이 화면의 첫 번째 일이다), 링크·신청 조회 실패는 각각 `ui/InlineRetry`(CHMO-520 신설 — 종전 20의 인라인 재시도 마크업을 공용화, 두 곳이 쓴다) 한 줄로만 알린다. **구계약 실 BE(parent 채널 null)에선 링크 섹션만 감춘다** — 시트는 탭을 통째로 숨겼지만 20은 같은 탭에 신청·명단이 살아 있어 탭을 없애면 승인·연결을 할 수 없다(섹션 제목까지 `GroupInviteLinks`가 소유해 제목만 남는 빈 섹션이 안 생긴다). 조회 키에 role을 넣지 않아 **탭 전환에 재조회가 없다**(두 역할이 한 응답에 함께 온다). ⚠ **탭 라벨에 인원 수는 얹지 않았다**(티켓 범위에서 덜어냄) — 바로 아래 섹션 제목이 이미 `선생님 3`·`학부모 12`를 말해서 삼중이 된다. 감수한 대가: 가장 잦은 동작인 **링크 복사에 화면 이동 1회**가 붙는다(시안 A는 그 대가가 없지만 역할 탭 중복이 남아 밀렸다 — 되돌릴 땐 A가 후보). **에러 리포트에 요청 추적 ID(CHMO-500) 완료 — 실서버 실측**: BE가 모든 응답에 싣는 `X-Request-Id`(BE CHMO-493)를 실패 응답에서 읽어 `ApiRequestError.requestId`에 담고, **에러 화면(`ErrorState`) 맨 아래 회색 11px 한 줄 `코드 · 추적ID`**(`toErrorReference`)로 노출한다 — 제보받은 캡처 한 장으로 CloudWatch에서 그 요청의 로그를 곧바로 짚는다(종전엔 시각·엔드포인트·닉네임을 되물어 좁혔다). 2026-07-30 실서버 실측으로 계약을 확인했다: 12자 hex · `access-control-expose-headers: X-Request-Id`(배포 환경 `app.`→`api.` 교차 출처에서도 브라우저가 읽는다) · preflight `allow-headers`에도 있어 FE가 되돌려 보낼 수도 있지만 **보내지 않는다**(이을 FE 세션 추적 ID가 없어 BE 생성분으로 충분). **노출 자리는 에러 화면 하나**(사용자 확정 — 티켓 예시는 토스트였다): 토스트는 3초 뒤 사라져 복사할 틈이 없는 채로 평상시 화면만 상태 코드로 지저분해지고, 화면이 통째로 실패해 사용자가 멈춰 서 있는 자리가 캡처가 나오는 자리다. 사용자에게 보여도 **보안상 무해**하다(요청마다 새로 발급되는 난수 — 토큰도 서버 내부 구조도 아니고, Stripe·GitHub 콘솔도 오류 화면에 같은 값을 노출한다). ⚠ 단 **앱 안에 문의 경로가 없어** 실제 도착 경로는 "선생님이 캡처를 카톡으로 보낸다" 하나다(문의 화면·에러 추적 서비스 도입은 별건). 값이 화면에 그대로 그려지므로 **BE와 같은 형식 규칙**(ASCII 영숫자·`-`·`_`·64자)으로 걸러 중간 프록시가 실은 엉뚱한 헤더가 새지 않게 했고, 추적 ID가 없는 실패(네트워크 끊김)는 줄 자체를 그리지 않는다(코드만 남으면 사용자에겐 의미 없고 우리에게도 로그를 좁혀 주지 않는다). 목도 봉투 헬퍼 4종 전부에 같은 헤더를 실어(`shared.ts` — 실 BE가 성공 응답에도 싣는다) 스위치 양쪽이 같은 계약을 타고, 계약 테스트가 헤더 읽기·형식 거르기·재발급 재시도 시 **재시도 응답의** ID를 든다는 점까지 고정한다. **05 모임명 헤더 이동 + 초대 하단 복귀(CHMO-530) 완료 — 상단 모임 색면 블록 폐지**: 08이 CHMO-522에서 택한 규칙(**헤더 중앙이 이 화면의 대상을 말한다**)을 05도 따르려니 헤더의 모임명과 색면 블록의 모임명이 한 화면에 두 번이 됐다. 시안 5종(현행 유지 / 헤더로 올리고 블록은 인원·초대만 / 블록 해체 / 스크롤 교대 / 블록 sticky)과 하단 3종(2단 / 한 줄 분할 / 텍스트 링크)을 실제 토큰으로 그려 대조한 뒤 **블록을 통째로 없애고 초대를 하단으로 내리는 쪽**을 택했다(2026-07-30 사용자 결정) — ① **헤더 중앙 = 모임명**(CHMO-515 ① 반전 · `‹ 홈`·⚙ 유지)이라 목록을 끝까지 내려도 어느 모임인지가 남는다 ② **상단 색면 블록 폐지**(같은 항목 ② 반전) — "모임=색면 / 이벤트=흰 카드"의 색면 쪽을 잃지만 헤더가 모임을 말하고 본문은 카드만 남아 역할이 갈린다 ③ **인원은 목록 라벨 줄 우측**(`이벤트 3개` … `선생님 3 · 학부모 12` — §7-3 분리 표기·`인원 N명` 폴백 그대로, 값 없으면 생략) ④ **하단 = 같은 크기 두 버튼**(`[초대 관리]` secondary + `[＋ 이벤트 생성]` primary, `flex-1` 균등 · 워딩은 `초대`가 아니라 **`초대 관리`** — 그 화면 일이 링크 보내기만이 아니라 승인·아이 연결까지다): **위계는 폭이 아니라 색이 만든다**(primary=할 일 / secondary=가는 일). CHMO-520이 하단 `[초대 관리]`를 걷어낸 근거("하단은 할 일의 자리인데 초대 관리는 가는 일이라 같은 무게로 쌓일 자리가 아니다")는 **상단 진입점이 사라져 쌓일 무게 경쟁 자체가 없어졌다** — 되돌린 결정임을 남긴다 ⑤ **스크롤은 이벤트 목록만 갖는다** — `main`은 `overflow-hidden`이고 라벨 줄·하단 액션이 `shrink-0`으로 위아래에 붙는다. **sticky는 쓰지 않는다**(CHMO-424 — 화면 내 sticky 바가 실기기 WebKit에서 위로 떠 콘텐츠가 비쳤다). ⚠ 밀린 안: **하단 2단**은 상단에서 번 76px을 하단이 58px 되받아 첫 화면 카드 수가 그대로여서(1단이라야 2장 → 3.5장이 된다) · **초대를 텍스트 링크 한 줄로** 두는 안은 탭 영역이 가장 약해 승인 대기가 며칠 방치될 위험이 있어서. ⚠ 실기기 확인 항목: 하단 두 버튼 라벨 잘림(390px에서 버튼당 170px, `＋ 이벤트 생성`이 ≈160px이라 여유가 10px뿐 — 더 좁은 기기는 라벨 축약 필요). **14 공개 요약 정보 재설계(CHMO-531) 완료 — "수치가 별로고 리즈닝을 해야 한다"·"이벤트 이름이 너무 안 보인다"**: 통계 3칸이 **같은 사실을 세 번** 말하고 있었다 — `공개할 앨범`과 `검토 n/m`의 분자가 **같은 수**인데(둘 다 검토 완료된 인물·공통) 화면이 그걸 말하지 않아 관계를 찾게 되고, `n/m`에서 남은 수를 뺄셈하면 아래 안내가 그 결과를 또 말했다. 시안 3안씩 두 번 대조해 **통계=안 B·이름 자리=안 나** 채택(2026-07-30 사용자 결정) — ① **같은 축(검토)의 앨범 2칸**(`검토된 앨범 N` · `검토 남은 앨범 N` — `N + N = 전체`가 눈으로 더해지고 비율 표기가 필요 없다. 왼쪽을 `공개될 앨범`으로 두지 않은 건 2026-07-30 피드백 — 세는 값은 같아도(검토 끝난 인물·공통 = 발행 대상) 오른쪽이 '검토'를 말하는데 왼쪽만 '공개'를 말하면 **다른 축처럼 보여** 또 관계를 따지게 된다. 무엇이 나가는지는 아래 미리보기 구획이 카드로 답한다. 축이 같아지니 published 시제 분기도 0일 때 예외 표기도 사라졌다. 인물·공통이 0개면 통계 줄 자체를 걷는다) + **전체 사진 수는 부제로 강등**(`공개 요약 · 사진 N장` — 공개될 장수가 아닌 값이 가장 큰 자리를 차지하고 있었다. 발행 장수 전환은 BE `publishablePhotoCount`(CHMO-505) 후) ② **막고 있는 앨범을 08과 같은 카드로**(`인물 2 1장 · 인물 4 3장 · …` 나열 폐지 — 숫자 둘이 붙어 `인물 21장`으로 읽히고, `1장`이 잔여인지 전체인지 모르고, **아직 이름을 안 붙인 인물은 텍스트로 누군지 알 수 없다**(단서는 커버 얼굴뿐), 갈 문도 없었다). 메타 `N장 남음`은 `AlbumCard.metaText` 신설(값이 없으면 기본 메타로 폴백 — `unreviewedPhotoCount`는 계약상 optional), **탭하면 그 앨범 09로** 가 `14 → 09 → 검토 완료 → 다음 09 → … → 14` 고리가 닫힌다(CHMO-521과 한 짝). 미리보기보다 **위**(할 일 먼저)·전부 펼침 ③ **게이트 문장은 하단 [공개하기] 바로 위**(`앨범 N개를 마저 검토하면 공개할 수 있어요`) — 스크롤을 내려도 잠긴 이유가 버튼 옆에 남는다(규칙은 버튼 옆, 수는 목록 위 — 각자 한 번씩) ④ `미리보기` → **`공개하면 이렇게 보여요`**(published면 `학부모에게 이렇게 보여요`) — 명사 `공개될 앨범`으로 두면 바로 위 통계 칸과 같은 말이 두 번이라, 08 하단 구획(`공개해도 학부모에게 안 보여요`)과 같은 문장 꼴로 맞췄다 ⑤ **이벤트명이 큰 자리를 갖는다** — 종전엔 `공개 요약`이라는 **화면 종류명**이 큰 제목이고 이름은 회색 13px 부제였다(05 모임명·08 이벤트명·09 앨범명이 전부 고유명사를 큰 자리에 두는데 **14만 예외**). 08처럼 헤더로 올리지 않고 **본문 큰 제목**으로 둔 이유 둘: 헤더는 한 줄이라 긴 이름이 잘리는데 여기는 곁에 붙는 컨트롤이 없어 **2줄 클램프**로 온전히 보이고(09의 `truncate`와 갈리는 지점 — 09는 이름 줄에 컨트롤이 붙는다), 08과 달리 하단 [공개하기]가 늘 붙어 있어 스크롤로 제목이 사라져도 화면의 정체가 안 흔들린다. ⚠ **본문 큰 제목은 이제 09와 14뿐이다** — 05도 같은 꼴이었으나 CHMO-530에서 헤더로 올라갔다(목록만 스크롤시키려 상단 블록을 걷어낸 결과). 뒤로가기도 `‹ 이벤트 상세`→**`‹ 앨범`**(08은 스스로를 '이벤트 상세'라 부르지 않는다 — CHMO-522) ⑥ **확인 다이얼로그에 이벤트명**(`'{이름}'을 공개할까요?` — 09 검토 완료/앨범 삭제와 같은 꼴)+나갈 앨범 수: 본문 큰 제목이 스크롤로 사라진 뒤 결정하는 마지막 자리라 ⑤의 유일한 약점을 여기서 메운다 ⑦ 공개 제외 고지 가운뎃점→쉼표(앨범명 안 공백과 섞여 한 덩어리로 읽혔다) ⑧ **앨범 카드 검토 대비를 선 → 면으로**(2026-07-30 "검토와 미검토 카드의 대비가 없다"): 110px 카드에서 2px 선의 색·점선 여부만으론 두 상태가 같아 보였다 — **검토완료 = 채운 면(`surface`)+갈색 실선 / 미검토 = 빈 면(흰색)+회색 점선**. 채움 유무가 작은 크기에서 가장 멀리서 읽히고 '다 한 건 도장 찍히듯 차고 남은 건 빈 윤곽'이라는 뜻도 맞다(05 모임 색면 ↔ 이벤트 흰 카드와 같은 문법, CHMO-515). 새 색 없이 기존 토큰만 쓴다. `AlbumCard` 공용 변경이라 **08·14·00-T가 함께** 바뀌고 08 범례도 `점선=미검토 · 채운 면=검토완료`로, `screen-system.dc.html` §06도 동기화. **CHMO-530 머지 후 재확인 완료**(2026-07-30) — 05가 헤더로 가면서 이름 자리가 갈렸지만 14는 본문 제목을 유지한다: 05는 목록만 스크롤시키느라 상단을 비워야 했고 14엔 그 제약이 없으며, 14의 두 근거(긴 이름 2줄 · 하단 고정 버튼이 화면 정체를 잡아 준다)는 05의 변경과 무관하게 성립한다. **05 이벤트 카드끼리 구별(CHMO-532) 완료 — 경계는 테두리, 그림자는 띄우는 몫만**: "카드들이 서로 잘 구별되지 않는다"는 피드백의 원인은 수치였다 — 카드 면 `#FFFFFF`과 배경 `cream #FFFDF5`은 **명도차가 1%도 안 돼**(사실상 같은 색) 경계가 전부 테두리·그림자 몫인데, 테두리는 `border` 1px(대비 ~10%)로 옅고 **`shadow-card`의 blur 30px이 카드 간격 12px보다 넓어** 아래 카드 그림자가 위 카드 밑면을 덮어 간격을 뿌옇게 채웠다(경계를 만들라고 넣은 장치가 경계를 지운다). 커버가 상단 전면(CHMO-515)이라 끊기는 자리를 사진 내용이 결정하던 것도 한몫했다. 시안 4종(그림자 정리 / 진한 테두리+그림자 제거 / 카드 본문 색면 / 배경 회색 반전)을 실제 토큰으로 그려 대조한 뒤 **테두리 강화 + 그림자 축소 조합**을 택했다(2026-07-30 사용자 결정) — `EventCard` 테두리 **`1.5px #D8CFBB`**(대비 ~18% · 팔레트에 중간 톤 라인 색이 없어 임의값이고, 다른 카드도 같은 처방을 하게 되면 토큰으로 올린다) · 그림자 **`shadow-card-stack = 0 2px 8px rgba(58,49,40,.07)`** 신설(`tailwind.config.js` — 이름이 말하는 대상은 "목록에서 세로로 쌓이는 카드") · 간격 `gap-3`→**`gap-3.5`**(그림자 blur 8보다 넓어야 사이가 보인다 · 05 목록과 둘러보기 선생님 트랙 무대 둘 다, 학부모 트랙은 커버 없는 텍스트 카드라 제외) · radius 16→**18**. **그림자를 아예 버리지 않은 이유**는 홈·앨범·시트에 "카드는 떠 있다"는 결이 두루 깔려 있어 05만 평평해지면 어긋나기 때문(시안 C 기각). `EventCard` 사용처 셋(05 목록·둘러보기 무대·`/dev/components`)이 함께 바뀐다. ⚠ 남는 것: **원인 하나(카드 면 = 배경 색)는 그대로**라 사진 0장 컴팩트 카드가 개선폭이 가장 작다 — 부족하면 그 카드만 본문을 `surface` 색면으로(시안 D) · 배경색 규칙 전면 변경(시안 E — 회색 배경 + 순백 카드)은 크림 배경이 이 앱의 따뜻한 톤을 만드는 축이라 홈·15-L·20까지 함께 가는 별도 스토리 · **홈 모임 카드·앨범 카드는 `shadow-card` 유지**(한 앱에서 카드 꼴이 두 가지가 되는 대가를 감수하고, 같이 갈지는 05를 보고 판단). **출시 필수 설정·멤버 관리(CHMO-526) 완료** — App Store 심사 요건(5.1.1(v) 계정 삭제 · 1.2 UGC 차단/연락 수단)을 화면으로 채웠다: ① 설정 **계정 삭제**(폼 밖 하단 버튼 → 확인 다이얼로그(마지막 선생님인 모임은 사진·앨범과 함께 삭제 명시) → `DELETE /me` → **성공 시에만** 토큰 폐기·랜딩 복귀(실패에 토큰을 지우면 "삭제됐다"로 오독) — **실계약 정합 완료(CHMO-575, 2026-08-03)**: BE CHMO-524가 티켓 초안 `/users/me` 대신 `GET/PATCH /me` 관례대로 **`/me`**로 배포돼 경로를 교체하고(목·계약 테스트 동기), 실서버 일회용 계정으로 삭제→재로그인 AUTH401·리프레시 TOKEN401까지 실측했다. 분석 중 이벤트가 있어도 안 막힌다(ADR 019 — 모임 나가기 MOMENT409와 다른 점, 목도 가드 없음 확인). 목은 마지막 ACTIVE 선생님인 모임은 모임째 삭제(CHMO-525 가드와 같은 판정)·그 외 멤버십·매핑만 정리·유저 행 삭제로 자기서술형 목 토큰이 자동 무효, persist 보존분도 제거) ② 설정 **신고·문의 mailto**(`biniwonihyuni@gmail.com` — 스토어 등록정보의 지원 이메일과 반드시 같은 주소) ③ 20 초대 관리 **멤버 내보내기**(멤버 행 [내보내기] → 확인 다이얼로그(role별 문구 — 사진·앨범은 모임에 남고 재신청 가능) → `DELETE /groups/:id/members/:userId`, BE CHMO-525 PR #155 머지 — 실서버 배포·채집은 미완. **자기 행 제외**: 같은 API의 본인 대상은 '나가기'로 동작해 05가 맡고, me 도착 전엔 선생님 행 버튼을 숨긴다(자기 행을 못 가리는 동안 내보내기인 줄 알고 눌러 나가기가 되면 안 된다)) ④ 05 ⚙ 모임 설정 **모임 나가기**(확인 다이얼로그 → 확인 시점에 GET /me로 내 userId를 읽어 같은 API 호출(`/members/me` 없음 — BE 결정) → 홈 replace. **MEMBER409**(마지막 ACTIVE 선생님 가드 — `errors.ts`에 `LAST_TEACHER` 등록, BE 소스 대조)는 BE 메시지 자체가 "모임을 삭제해 주세요" 안내라 토스트로 그대로 노출). 목 멤버 제거는 BE 스펙 판정 순서 그대로(본인 부재 SPACE403 — 모임 존재 은닉, `shared.ts` spaceForbidden 신설 → 모임 없음 SPACE404 → 비멤버·PENDING SPACE403 → 학부모 ROLE403 → 마지막 선생님 MEMBER409 → 대상 부재 멱등 성공). 약관 링크 3종은 기존(CHMO-478)으로 충족 — 버전 표기 일치는 CHMO-517 별도. **상단바 확대 + 08 검토 범례 제거(CHMO-556) 완료 — 화면 피드백 2건(2026-08-02)**: ① 공용 `Header` 확대 — 높이 52→**60px**·서브형 타이틀 16→**17px**·홈형 체다 30→**34**·워드마크 20→**22px**(뒤로가기 라벨 15·패딩 유지, dc.html §01 동기화). Jua 단일 굵기(CHMO-513)라 크기가 곧 위계인데, 헤더가 화면의 대상(모임명·이벤트명)을 말하는 규칙(CHMO-522·530)이 자리 잡은 뒤에도 무게는 그대로였다. 공용 컴포넌트 하나라 전 화면 동시 적용 ② **08 검토 범례 줄 삭제**(`점선=미검토 · 채운 면=검토완료` — CHMO-523의 범례 노출 규칙·CHMO-531 ⑧의 범례 워딩은 소멸): 검토완료=채운 면/미검토=점선 대비를 면으로 키운 뒤(CHMO-531 ⑧ — 카드 스타일은 유지)라 카드가 스스로 말하는데 설명 줄이 남아 있었다. 앨범 0건 "앨범이 아직 없어요." 안내는 유지. 크기 수치는 1차 판단값 — 실기기에서 보고 조정 여지. **닉네임+PIN 로그인 화면 제거(CHMO-557) 완료 — 소셜 로그인 단일화**: 01-1 로그인·01-2 계정 생성·공용 폼(AuthCredentialsForm)을 삭제하고 01 랜딩이 소셜 버튼 4종 전용이 됐다(returnTo 소비·초대 링크 배너도 랜딩이 이어받음). **`/login` 라우트는 남기고 01을 그린다** — 앱 전역 401 복귀 목적지(가드·`unauthorizedTo` 40여 곳)·JoinPage returnTo 착지가 이 경로를 봐서, 라우트를 지우면 로그아웃·토큰 만료가 전부 404로 빠진다. `/signup`은 삭제. **PIN 로그인은 DEV 전용 `/dev/login`으로만 잔존**(2026-08-02 사용자 결정 — 실 BE `test`/1111·목 시드 이현정/1234가 유일한 확인 진입로인데 목 소셜 로그인은 프로바이더별 별도 계정을 find-or-create 해 시드 데이터에 못 들어간다. 가입 폼은 안 남김 — 계정은 시드·소셜로 만든다). **설정의 'PIN 번호 변경' 필드도 삭제**(같은 결정 — 로그인 입구가 사라진 PIN은 쓸 곳이 없다). `login`·`signup` api 함수·목 핸들러·계약 테스트는 BE 엔드포인트가 살아 있어 잔존(CHMO-473 zip 전례), `PinField`·`lib/pin.ts`는 뷰어 잠금·DEV 폼·목이 계속 쓴다. BE가 엔드포인트를 걷으면 DEV 폼·목·픽스처를 함께 정리한다. **Android 앱 웹뷰 Apple 로그인 숨김(CHMO-551) 완료**: Apple 로그인은 iOS 심사 요건(4.8 Login Services) 대응용이라 **Android 앱 웹뷰에서만** 숨긴다(2026-08-01 결정) — 판정은 기존 UA 마커 파서 재사용(`nativeAppInfo().platform`, 중복 파서 없음), 노출 규칙은 `src/lib/socialProviders.ts` `isSocialProviderVisible()` 단일 원천. 마커가 있어도 형식이 달라 플랫폼 미상이면 노출 유지(잘못 숨긴 쪽=iOS 심사 반려가 더 비싸다). iOS 앱 웹뷰·일반 브라우저(Android 크롬 포함)는 4종 그대로, UA별 분기는 단위 테스트로 고정. **약관 문서 버전 표기(CHMO-517) + 가입 동의 화면·게이트(CHMO-479) 완료 — 제출 버전의 원천은 FE 문구다**: `LegalDoc.version`(문서 3종 1.0 — 정본 md에도 같은 표기)과 `GUARDIAN_CONSENT_COPY.version`이 동의 제출에 실리는 값의 단일 원천이라(`attestGuardianConsent`의 서버 currentVersion 에코 폐지 — GET 1회 제거) 화면에 보여준 문구와 기록 버전이 어긋날 수 없고, 목 카탈로그와의 드리프트는 serializers 이음매 테스트가 고정한다(**문구 개정 시 FE version·BE `AgreementType`·목을 함께 올린다** — 안 맞으면 제출이 전부 VALID400). `/legal/*` 상태 박스에 '버전 1.0' 표기(상태줄은 게시일 — '초안·법률 검토 후 확정' 고지는 CHMO-577에서 제거). **01-A 가입 동의(`/consent`, `SignupConsentPage`)**: 소셜엔 가입 화면이 없어 **로그인 성공 시점**(01-C 콜백·`/dev/login`)이 `GET /agreements` → `lib/consentGate.evaluateConsentGate`(pass/consent/stale)로 판정해 필수 미동의(신규·문구 개정 재동의)면 01-A를 먼저 거치고 returnTo를 state로 인계한다(조회 실패는 통과 — 게이트는 FE 몫·서버 전역 강제는 CHMO-519. 이미 로그인된 세션은 다음 로그인에서 걸린다 — 미배포·사용자 0명이라 레거시 세션 없음). 판정은 FE가 수집하는 4종 기준 — GROUP 스코프(보호자 확인)는 `agreed`가 항상 false라 제외(정본 §1 함정)·화면에 없는 MARKETING도 무관. **버전 불일치는 stale**로 폼 대신 새로고침 안내(구버전 번들이 체크를 받아도 VALID400이라 체크부터 시키지 않는다). 화면 문구 정본은 **`docs/legal/app-copy.md` §1~§4 신설**(노션 「앱 노출 문구」 8-03 갱신분의 리포 정본 — 처리방침 앱판 §8 위탁 표(Rekognition 얼굴 이미지 전송 명시·Cloudflare)도 동기화): 전체 동의는 일반 3종만 — **얼굴 특징정보는 구획선 아래 별도 영역 개별 체크**(법 제23조 민감정보 — 전체 동의 포함 금지)·필수 기본 체크 금지·**[전문 보기]는 바텀시트**(체크 상태 보존 — 렌더러는 `/legal/*`와 공용 `LegalDocBody`로 추출, 이용약관=전문 그대로·개인정보=§2 동의 시점 고지 전문·얼굴=§3 요약 전문)·**마케팅(선택)은 뺐다**(2026-08-03 결정 — 발송 채널이 없어 전문을 사실대로 쓸 수 없다. 채널이 생기면 §1.2 '선택 항목' 안내와 함께 추가). 제출은 4종 일괄 `agreed:true` → returnTo 복귀(홈 도착 시 둘러보기 종전대로). 목 시드엔 동의 기록이 없어 **목 로그인마다 01-A가 재현된다**(실 BE 기존 계정도 같은 이유로 첫 로그인에서 한 번 지난다). **모임 나가기 확인 모달 분기(CHMO-571) 완료 — BE CHMO-564 대응, 함께 배포 필수**: BE가 마지막 ACTIVE 선생님의 나가기를 거부(MEMBER409) 대신 **모임 전체 삭제로 승격**했다(응답 계약 불변 200 — 파괴적 결과의 확인 책임이 FE 모달로 넘어와, 이 모달 없이 BE만 운영에 나가면 무경고 삭제가 된다). ① 05 ⚙ 나가기 확인이 두 갈래 — 판별은 FE 파생 `lib/lastTeacher.ts isLastActiveTeacher`(멤버 목록의 teacher가 1명 = 나. ACTIVE 전용 목록이라 내가 없거나 0명이면 false가 정답) 단일 원천이고, **확인 다이얼로그를 열기 전에** GET /me+members로 판별한다(문구가 곧 결과라서 — 마지막 선생님에게 기존 "사진·앨범은 모임에 남고"는 거짓 안내다. 설정 모달 버튼이 '확인 중…' busy, 판별 실패는 진행 불가(토스트·모달 잔류), 예외는 ROLE403 하나 — 멤버 조회가 TEACHER 전용이라 학부모는 항상 걸리는데 마지막 선생님일 수 없으니 기존 확인으로 통과 = 학부모 흐름 불변) ② 마지막 선생님 문구는 삭제 경고([삭제하고 나가기]·이벤트·앨범·사진 소멸·비가역 명시), 성공 랜딩은 두 갈래 다 홈 replace(토스트만 분기) ③ 에러 2건 — `MOMENT409`→`MOMENT_ANALYZING` 신규 등록(모임 삭제·마지막 선생님 나가기의 분석 중 가드, BE ErrorStatus 소스 대조·미채집. BE 메시지가 삭제 문맥이라 나가기 화면은 "분석 중인 이벤트가 있어 지금은 모임을 나갈 수 없습니다."로 교체) · `MEMBER409`는 **내보내기 경쟁 전용**으로 남고 메시지가 "모임에는 최소 1명의 선생님이 남아야 합니다."로 갱신(종전 "모임 삭제를 쓰라" 안내는 BE 메시지째 폐기 — 픽스처·계약 테스트 동기화) ④ 목도 같은 계약 — 나가기 승격(deleteGroupCascade 재사용)+분석 중 가드, `DELETE /groups/:id`에도 같은 가드 추가(BE DeleteSpaceUseCase 대응 — `db.hasAnalyzingEvent`는 정산(settle) 후 판정해 기한 지난 analyzing이 가드에 걸리는 목 전용 거짓 거부를 막는다). **스토어 심사용 숨김 로그인(CHMO-572) 완료**: 01 랜딩 로고(치즈 심볼) **5회 연속 탭**(탭 간격 1.5초 이내 — 판정은 `lib/reviewLogin.ts` 단일 원천)이면 소셜 버튼 아래 ID/PIN 폼이 열린다 — 애플 2.1(심사관 데모 계정) 대응 **상시 진입점**(소셜 계정은 해외 IP 차단·2FA로 제공 불가, "심사 때만 켜는 토글"은 꺼진 시점 심사가 반려라 배제 — 심사 노트에 절차를 공개하는 한 숨김 기능(2.3.1) 문제없음, 2026-08-03 확정). 노출은 컴포넌트 상태뿐이라 새로고침이면 다시 숨고(비영속 — 오발견 최소화), 5회 탭 전엔 DOM 무변화라 일반 화면은 그대로다(로고 래퍼 `touch-manipulation`은 연속 탭이 더블탭 줌으로 새는 것 방지 — 시각 무변화). 폼은 `components/PinLoginForm.tsx`로 추출해 `/dev/login`과 공용 — 제출 흐름(토큰 저장→동의 게이트(CHMO-479)→returnTo 복귀)이 입구마다 갈라지지 않게. 심사용 계정 `applereview`/`1111`(실 BE, 2026-08-03 `POST /auth/signup`으로 생성 — **DB 초기화 시 소멸·재생성 필요**, 절차·영문 노트 문구는 CHMO-543 코멘트). **투어 자동 노출 폐지 + 코치 힌트(CHMO-565) 완료 — "튜토리얼이 너무 별로다" 피드백의 답을 리서치로 좁혔다**: 원인은 촌스러움이 아니라 패턴 — NN/g 정량 실험(선행 튜토리얼은 과업 성공률 91↔94%로 무효·체감 난이도만 악화)·5단 초과 완주율 절벽(현행 선생님 트랙 7단)·자동 노출 완주율 39% vs 스스로 연 투어 67%·직접 경쟁 키즈노트·하이클래스는 앱 내 투어 없음(선생님 배포 안내문이 온보딩 채널). 시안 5종(A 압축 투어 / B 빈 상태 릴레이 / C 시작하기 카드 / D 초대 문구 / E 코치 힌트)을 실제 토큰으로 그려 대조한 뒤 **E+D 채택**(2026-08-03 사용자 결정 — "직관적인 모임 만들기·이벤트 생성은 따로 가르치지 말고, 사진을 올렸을 때 그 자리에서 한 번만"): ① **00-T 첫 로그인 자동 노출 폐지**(CHMO-504 반전) — 설정 '치즈모아 둘러보기' pull 전용 잔존(⚠ CHMO-578에서 이 잔존마저 삭제. 스스로 연 투어가 완주율 2~3배라 같은 자산의 성과가 오히려 오르는 배치), 자동 노출 전제이던 갈래 힌트(setPreferredTourTrack·getPreferredTourTrack)·fromJoin 미루기 신호·AppTour initialTrack prop을 참여 흐름 2곳과 함께 삭제. onboarded 플래그는 **기록만 유지**(읽는 곳은 없지만 슬라이드 00 부활 시 "이미 본 사람" 판정 원천이라 지우면 그때 전원 재노출) ② **CoachHint 신설**(`ui/CoachHint.tsx` + `lib/onboarding.ts` 힌트 기록 — id 단위·계정별·'뜰 때' 기록) — 실제 화면의 낯선 지점에 계정당 1회 붙는 말풍선: 딤·오버레이·pointer-events 없음(아래 카드 탭을 안 막는다)·아무 곳이나 탭하면 소멸(감지는 pointerdown이 아니라 **click** — 터치 스크롤 시작에 닫히면 안 된다)·닫기 버튼 없음. 배치는 **08 앨범 그리드 첫 카드 한 곳**("사진이 아이별로 나뉘었어요 / 앨범을 눌러 확인해 보세요") — 사진을 직접 올린 사람(분류 완료 도착)과 나중에 합류한 사람(앨범이 이미 찬 화면 첫 대면)의 막막함이 같은 자리라서(NN/g가 코치마크를 유효하다고 본 유일한 조건 = 기능 첫 조우 + 실과업 짝지음). 09 [검토 완료] 곁 힌트는 한 차례 기각 후 **같은 날 복귀**(사용자 — "모든 앨범이 검토돼야 공개" 게이트도 힌트로 원함): id `album-review`, [검토 완료] 버튼 위 꼬리 우측 말풍선(검토 + 전량 검토=공개 게이트 두 개념을 그 개념이 실행되는 버튼 곁에서), **이미 전량 검토된 앨범엔 안 띄운다**(죽은 버튼을 누르라는 거짓 안내 방지) ③ **잔존 투어 문구 정합** — 공개 요약 무대를 CHMO-531 꼴로(구 3칸 통계 → 같은 축 2칸·이벤트명이 큰 제목·'공개하면 이렇게 보여요' 라벨), 모임 무대 메타를 CHMO-530 라벨 줄 순서(이벤트 N개 · 선생님 N · 학부모 N)로. 7단 압축·'건너뛰기' 라벨은 pull 전용이 되며 급하지 않아 **보류** ④ **학부모 초대 카톡 문구 3줄 안내**(D — GroupInviteLinks parent share): 신청→승인→공개 순서를 초대 메시지가 말한다(도메인 관행 — 학부모 온보딩 채널은 앱이 아니라 선생님). 홈 빈 상태 둘러보기 링크 복구안은 기각(재진입은 설정 한 곳 유지). 마무리로 **사용자 노출 문구의 긴 대쉬(—)를 · 로 통일하고 긴 문장을 줄였다**(사용자 지시 — 검토 완료·참여 신청 토스트, 투어 3곳, 초대 문구·힌트 문구). **개발환경(스테이징 BE)용 FE 배포(CHMO-573) 완료 — `VITE_API_DIRECT` 직통 모드**: BE·AI가 개발/운영을 분리했는데(BE CHMO-568 **온디맨드 스테이징** EC2 — 평시 정지·도메인 없음·S3/SQS 미생성 · AI CHMO-570) 개발환경을 브라우저로 조작할 FE가 없었다. `VITE_API_DIRECT=true`면 `client.ts` `API_BASE`가 `/api/v1` 상대경로 대신 `VITE_API_ORIGIN`을 직접 부른다(실 BE 경로엔 프리픽스가 없어 그대로·뒤 슬래시 제거. **미설정 빌드는 dead branch가 제거돼 번들이 바이트 동일** — 운영 회귀 0 실측). vercel.json rewrite는 같은 리포 한 파일이라 프로젝트별로 가를 수 없어, 개발용은 **전용 Vercel 프로젝트 `cheese-moa-dev`**(Production Branch=`develop`, 도메인 `dev.cheese-moa.com` 연결 완료, env `VITE_API_ORIGIN=https://staging-api.cheese-moa.com`+`VITE_API_DIRECT=true` — 생성은 대시보드 수동, 절차는 CHMO-573 코멘트. env 변경은 재배포해야 반영 — 빌드타임 값)가 이 모드로 스테이징을 바라본다. 소셜 로그인 시작 URL은 이미 `VITE_API_ORIGIN`을 타서 같이 따라간다. ⚠ 직통 호출은 CORS를 타므로 **BE 스테이징 `.env` CORS_ALLOWED_ORIGINS에 `https://dev.cheese-moa.com` 추가가 선행**(스테이징 API 도메인은 BE가 실제로 건 `staging-api.cheese-moa.com`이 정본 — BE README 초안의 `staging.` 표기와 다르다. 2026-08-03 실측: DNS·HTTPS·서버 응답 확인, CORS만 미개통(preflight 403). Origin CA 와일드카드가 1단계만 커버라 `staging.api.` 같은 2단계는 불가) · MSW와 병용 금지 · 소셜 로그인은 프로바이더 Redirect URI가 운영 BE뿐이라 스테이징에선 불가(진입은 로고 5탭 숨김 PIN 폼, 계정은 스테이징 `POST /auth/signup`으로 생성). **출시 워딩 정리(CHMO-577) 완료 — "이거 다 안 만들어졌나?" 인상을 주는 노출 고지 전면 제거(2026-08-03 출시 결정)**: ① 약관 3종+가입 동의 전문 2종 상태줄 "초안 · … — 법률 검토 후 확정됩니다" → **게시일만**("2026. 7. 28. 게시" 꼴, `LegalDoc.status`) ② 이용약관 부칙·처리방침 §13 "서비스 정식 오픈일부터 시행" → "게시한 날부터 시행합니다" ③ 처리방침 §11 "보호책임자와 문의 창구는 정식 오픈 전 안내 예정" → **'개인정보에 관한 문의처'**로 개편해 신고·문의 이메일(설정 mailto·스토어 지원 이메일과 동일 주소) 안내 — 보호책임자 성명은 지정 전이라 미기재(사실과 다른 고지 금지, 지정되면 §11에 추가·정본 마커 동기화) ④ 02-2 동의 라벨 "(문구 확정 전)" 꼬리표 제거 ⑤ 소셜 콜백 "아직 준비되지 않은"→"지원하지 않는 로그인 방법". ⚠ **문서 version은 1.0 유지** — 버전은 BE `AgreementType`과 함께만 올릴 수 있고(어긋나면 동의 제출 전부 VALID400) 이번 변경은 동의 실질(항목·목적·기간)이 아니라 상태 표기·문의처 안내다. 변호사 확정 문구 반영(중립 치환 해소)은 CHMO-566 별건. **둘러보기 삭제 + 코치 힌트 확충 + 문구 정리(CHMO-578) 완료 — 2026-08-03 사용자 피드백 4건**: ① **치즈모아 둘러보기(00-T) 화면째 삭제**(CHMO-565의 설정 pull 잔존까지 폐지 — `AppTour.tsx`·설정 진입점·전용 keyframe `tap-ripple` 제거, `step-in`은 CoachHint가 써서 유지. 00 슬라이드 라우트·`onboarded` 플래그는 부활 대비 종전대로) — 첫 안내는 코치 힌트가 전담하고 투어를 다시 볼 경로는 없다 ② **코치 힌트 2개 추가(총 4)**: 08 [공개 전 요약보기] 곁 `publish-gate`("모든 앨범을 검토 완료하면 / 학부모에게 공개할 수 있어요" — "전량 검토=공개 게이트"(CHMO-488)를 14에 가서 잠긴 버튼으로 처음 아는 문제의 답. **`album-grid` 힌트를 본 다음 방문부터**(마운트 시점 1회 판정이라 같은 방문에 두 힌트가 겹치지 않는다 — 한 화면 한 힌트 유지)·published·앨범 0개 비노출) · 05 [초대 관리] 곁 `invite-manage`("학부모가 참여를 신청하면 / 여기서 승인하고 아이를 연결해요" — 승인제(CHMO-475)에서 승인이 늦으면 학부모가 며칠씩 기다린다) ③ **09 검토 완료 다이얼로그 간결화** — "사진 N장을 검토 완료해요. 완료하면 되돌릴 수 없어요."(이동·삭제 안내 문장은 삭제, 비가역 경고만 남김) ④ **사용자 노출 문구 긴 대쉬(—) 잔여 4곳 제거**(CHMO-565 1차 통일의 잔여 — 검토 다이얼로그·01 초대 로그인 배너·06-U 100장 캡 안내·`uncertainCauses` low_resolution. `src/legal/*` 전문(정본 md와 짝인 문서 — UI 문구 규칙 대상 아님)·DEV 갤러리·콘솔 로그는 제외). screen-spec 00-T 카드는 삭제 기록 + 코치 힌트 배치 4곳 명세로 교체. **토스트 하단 이동 + 09 검토 완료 토스트 문구 정리(CHMO-585) 완료 — 2026-08-03 사용자 피드백 2건**: ① 전역 토스트를 상단 중앙 → **하단 중앙**으로(CHMO-429 반전 — 상단 노출이 별로라는 피드백). CHMO-429가 피하려던 하단 고정 CTA 겹침은 **CTA 스택(pt-4+버튼 h-12+pb-safe-9 ≈ 100px+safe-area) 바로 위 오프셋**(`bottom-[calc(7rem+…)]`)으로 회피 — 단일 CTA 줄 화면(08·09·14·19 등)은 안 가리고, 그보다 높은 스택(01 소셜 4단 등)은 일부 겹치지만 pointer-events-none이라 조작은 안 막는다 ② 09 [검토 완료] 토스트에서 **"다음은 '{앨범명}'" 제거**(CHMO-521 문구 축소) — 다음 앨범 이름은 도착한 09 제목이 이미 말하고, 이름 없는 인물이면 "다음은 '인물 3'"으로 읽혀 어색했다. 검토 완료 토스트는 "🧀 검토를 완료했어요" 하나로 통일(albums 미도착 폴백·DEV 갤러리·dc.html §10 동기), "모두 검토했어요 · 공개할 차례예요"(14행)는 유지. **학부모 합류 자녀 정보 처리 동의(CHMO-587) 완료 — ⚠ BE CHMO-586 배포 게이트: FE 선행 또는 동시 배포 필수(BE만 먼저 나가면 학부모 합류 전면 차단)**: 02-2 3/3의 자리표시자 동의 2건을 실계약 1건으로 교체 — 동의권자(보호자) 본인이 체크하는 '자녀의 사진·얼굴 특징정보 처리 동의'(필수·기본 미체크, 4요소(촬영·업로드·분류·공유) 문장 원천은 `src/legal/consents.ts` `GUARDIAN_CHILD_CONSENT_COPY`) + [전문 보기] 바텀시트(`/legal/biometric`과 같은 전문 — 입력·체크가 컴포넌트 state라 화면 이탈 없이, 01-A 관용). 최종 제출에 `childConsentVersion`을 동봉하는데 **버전 원천은 서버 카탈로그다**(`GET /agreements` `GUARDIAN_CHILD_CONSENT`(GROUP 스코프 신설) currentVersion 에코 — 티켓 확정 하드코딩 금지, CHMO-517(버전 FE 소유)과 다른 선택: 문서 개정 시 FE 배포 없이 새 버전이 전송된다. 대신 이 문구를 고칠 땐 BE 버전 인상과 함께). 02-2 진입 시 미리 읽고 조회 실패면 3/3 CTA 잠금+InlineRetry(버전 없이 제출해 봐야 VALID400), **카탈로그에 항목이 없으면(구계약 BE) 필드를 생략**해 FE 선행 배포가 무해하다. 누락·구버전은 신청째 VALID400(BE CHMO-586 — 동의 행은 신청 시 기록·append-only·멱등, 목도 같은 계약이고 자녀 이름 검증을 동의보다 앞에 둬 1/3 프로브의 400 감지가 유지된다). 선생님 키 경로·02-1 모달 무변경. 실 BE 미배포라 목으로만 검증 — 실검증은 BE 배포 후. **계정 삭제·데이터 삭제 안내 공개 페이지(CHMO-588) 완료 — Google Play 삭제 URL 요건 2종**: 가드 밖 라우트 `/account-deletion`·`/data-deletion`(`DeletionGuidePage` — `LegalDocBody` 재사용 + 하단 바로가기 행: 설정 이동·mailto·처리방침, 복귀 규칙은 LegalDocPage와 동일), 문서 데이터는 `src/legal/deletionGuides.ts` — 약관 3종과 달리 **동의 제출 대상이 아니라** version(1.0)이 BE AgreementType과 짝을 이루지 않고(표기용), 삭제/보관 문구는 처리방침 앱판 §5·§9의 사실 그대로(탈퇴 회원 사진 모임 잔존(CHMO-583)·접속 기록 중립 표기·마지막 선생님 모임째 삭제)라 **처리방침을 고치면 함께 고친다**. 콘솔 기입 URL `https://app.cheese-moa.com/{account,data}-deletion` — CHMO-550(앱 Play 심사)이 Data safety 폼에 기입, **심사 전 배포 필수**(운영 도메인은 main 배포). screen-spec 카드는 기존 `/legal/*` 관습대로 두지 않았다. **관리자 페이지 1차(CHMO-379) 완료 — `/admin` 3화면(A1 대시보드·A2 모임 목록·A3 모임 상세), BE CHMO-377·378 계약(정본 BE 리포 spec — FE 요약은 `docs/admin-spec.md` §4)**: 코드는 `src/admin/` 한 폴더에만(격리 규칙 — 서비스 코드가 admin을 import하지 않고, 공유는 바닥 인프라(api/client·lib/auth·훅)와 브랜드 심볼(Cheddar)뿐), router는 lazy로만 이어 **어드민 청크는 /admin 진입자만 받는다**(빌드 실측 — 별도 청크 6개). 진입 가드는 `GET /admin/me`(BE가 명시한 게이트, `AdminLayout` 소유) — 무토큰·401은 /login, **ADMIN403은 차단 화면(재시도 없음)**: `errors.ts` `ADMIN403`→`NOT_ADMIN`(2026-08-04 실서버 채집 — 가드는 운영 배포돼 있다). 계약 요점: **경로는 `/admin/groups`**(티켓 초안 spaces 폐기 — BE 정책: 직렬화 경계 밖은 group/event) · 목록 페이지 정보는 **봉투의 `pageInfo`**(BE `Response.onSuccess(Page)`)라 `client.ts`에 `apiFetchPaged` 신설(result만 언랩하는 apiFetch로는 못 읽는다 — sendRequest가 `{result, pageInfo}`를 들고 apiFetch는 result만 준다) · 정렬은 BE 화이트리스트(createdAt·name × asc·desc, size 1~100 — 밖은 COMMON400. 집계 컬럼 정렬이 1차 제외라 시안의 '사진 많은 순'은 없다) · 상세 멤버는 **PENDING 포함**(운영자는 신청도 봐야 한다 — 시안(닉네임·합류일뿐)에 없던 역할·대기 칩을 붙여 memberCount(ACTIVE 기준)와 행 수 차이를 화면이 설명) · 이벤트 상태는 BE MomentStatus 대문자 그대로(배지 라벨·색 admin-spec §3-0 — `admin/lib/format.ts` 단일 원천, 미지 상태는 원문 중립 표기) · 시크릿 4종은 어떤 어드민 응답에도 없다(AC-6 — 이음매 테스트가 고정). 디자인은 tailwind `admin.*` 이름공간(중립 그레이+치즈 옐로우 — 모바일 cream/brown 미사용)과 **중립 시스템 서체 `font-admin`**(Jua는 단일 굵기 둥근 서체라 표·숫자 화면에 안 맞고, 시스템 서체는 실제 볼드가 있어 굵기 위계를 쓴다 — 폰트 요청 0건 추가). 목: `DbUser.role` 신설(기본 USER — persist 복원분도 USER 정규화·signup/소셜 생성도 USER), **시드 이현정 = ADMIN 겸용 계정**(admin-spec §2-3 — 서비스 화면은 role을 안 본다), 핸들러 4종(`handlers/admin.ts`)은 가드 순서(무토큰 COMMON401 → 비관리자 ADMIN403 — 실서버 관찰 순서)·검증(COMMON400)·집계 규칙(사진 수=앨범 매핑 distinct — 분석 전 사진 제외, `db.albumMappedPhotosOfEvent`)을 BE와 동일하게 두고 응답 전 settleAnalysis로 서비스 화면과 수치를 맞춘다(`shared.ts` okPaged·adminForbidden·commonBadRequest 신설). ⚠ **실 BE 실측 게이트 2개**: ① CHMO-378은 BE develop 머지만 — 운영 배포 후 실채집으로 픽스처 교체(현재는 BE 스펙 문서 대조 — ADMIN403만 실채집) ② **관리자 role 부여는 DB 직접 변경뿐**(부여 UI 없음 — CHMO-377)이라 실계정 3화면 조회(AC)는 role 부여 후. 곁가지 발견: 실 BE `test`/1111 계정 소멸(2026-08-04 — DB 초기화 추정, `applereview`/1111 생존). **어드민 진입 로그인(CHMO-594) 완료 — 비로그인 `/admin`이 모바일 랜딩으로 튕기지 않는다**: 어드민 셸 안 데스크탑 로그인 카드(`AdminLoginCard` — 닉네임+PIN, 같은 계정·같은 `POST /auth/login`이라 인증 체계 추가 아님(CHMO-375 유지). 격리 규칙대로 `src/api/auth.ts`를 import하지 않고 admin api 모듈의 `adminLogin`이 직접 호출·토큰 저장)를 그 자리에 그리고, 성공하면 **라우팅 이동 없이 게이트(GET /admin/me) 재판정**(AdminLayout `authTick` — useApi 키에 섞는다). 401 세션 만료도 같은 카드(+안내 한 줄), 비관리자 차단 화면엔 [다른 계정으로 로그인](토큰 폐기→카드), 소셜 계정 관리자는 카드 하단 링크로 01 랜딩 왕복(`state.returnTo='/admin'` — 기존 returnTo 파이프라인 재사용). **가입 동의 게이트(CHMO-479)는 어드민 로그인에 안 태운다**(서비스 이용이 아니다 — 그 계정이 서비스로 로그인하면 그때 걸린다). ⚠ 운영 어드민 계정 생성은 **반드시 signup API로**(BE가 PIN을 BCrypt 해시 비교 — DB 평문 직접 삽입은 로그인 영구 실패, 2026-08-04 실측) 후 role 칼럼만 DB 변경. **어드민 로그아웃(CHMO-595)**: 사이드바 하단 프로필 곁 [로그아웃] — `adminLogout`이 서비스와 같은 계약(`POST /auth/logout` auth 미첨부·body refreshToken)으로 서버 무효화 후 로컬 토큰 삭제(**서버 실패에도 로컬 로그아웃 진행**), 복귀는 이동 없이 게이트 재판정(authTick)으로 로그인 카드.

## 소스 오브 트루스 (작업 전 관련 문서를 읽는다)

화면·기능·데이터 결정은 모두 여기서 나온다.
- `docs/feature-spec.md` — 기능 명세(도메인 모델, 상태머신, 기능 F1~F7, 추적표)
- `docs/screen-spec.md` — 화면 명세(IA, 화면별 카드, 디자인 토큰, 26개 와이어프레임 매핑)
- `docs/api-spec.md` — API 계약(리소스 스키마, 엔드포인트 req/res, 화면 매핑). **단, 실연동 단계에선 실 BE 스웨거가 우선**(`https://api.cheese-moa.com/v3/api-docs`) — 문서와 다르면 FE를 BE에 맞추고, 문서 갱신은 CHMO-196에서 한다
- `docs/mvp-explanation.md` — 서비스 배경/취지
- `docs/parent-model-proposal.md` — **학부모 접근 모델 전환 정책 원천**(법률 자문 후속 — 무로그인 뷰어 폐기, 멤버십 role·승인제·인물 매핑. Q1~Q6·§7 전부 BE 협의 확정) · `docs/parent-model-api-draft.md` — 그 API 계약 초안(**목 선행 구현의 기준** — 실 BE 배포 후 실채집으로 재확인, CHMO-444~449)
- `docs/legal/` — **약관·개인정보 처리방침·얼굴 특징정보 안내 정본**(CHMO-478, 변호사 검토 전 초안): `[[결정필요]]`(팀 결정)·`[[검토필요]]`(변호사 확정) 마커가 미확정 지점. 근거 리서치는 노션(약관·처리방침 필수 내용 정리 / 이용약관 작성 리서치 / 사진 보안 리서치 / AWS Rekognition Opt-Out). 앱 적용판 `src/legal/*.ts`와 반드시 함께 수정. **`app-copy.md`(2026-08-03 신설) = 화면 노출 문구(가입 동의·게이트·배너·촬영 고지문) 정본** — 노션 「앱 노출 문구」 8-03 갱신분을 검토·교정해 반영한 것(학부모 재배포 배너는 7-29 제거 결정대로 미적용·§5 게이트는 모임 단위 1회·수집 항목은 소셜 기준). 8-03 갱신으로 들어온 사실: 저장소 수준 암호화 적용(CHMO-552 — 단 문구 노출은 변호사 확인 후)·**파기 구현 미완(CHMO-559 — 인물 벡터 삭제 경로 없음, 실사용자 유치 전 구현 필수)**·접속기록 서버 731일(법 근거는 검토필요). **전문 개정 병합(CHMO-581, 2026-08-03)**: CHMO-490 신규 초안을 기존 조문 구조에 병합 — 이용약관 22→25조(AI 자동 분류 특성·통신 고지·서비스 지식재산권·금지행위·세션 관리·문의 채널 신설, **미채택**: 업로드=동의 간주·AS-IS 전면 보증 부인·포괄 배상·전속 합의관할 — 각 조 마커에 사유), 처리방침에 비회원 얼굴 정보(§4④)·비회원 피촬영자 권리(§10⑤)·Rekognition 위탁 행 분리(무저장은 증적 확인 전 앱판 미노출)·보관 서버 서울 리전 확정·AI 학습 미이용 명시. **문의 주소는 cheesemoa03@gmail.com으로 통일**(처리방침 §11·약관 제21조③·설정 SUPPORT_EMAIL — 스토어 지원 이메일도 이 주소). 접속 로그 보유는 신규 초안 1년 vs 서버 731일 충돌로 `[[결정필요]]`(확정 값에 서버 설정 일치 필수). **문서 버전은 1.0 유지** — FE 단독 인상 시 가입 동의 제출이 VALID400(BE AgreementType과 함께 올리는 개정 절차는 출시 후부터). **결정 2건 확정 반영(CHMO-583, 2026-08-03)**: ① 접속 로그 보유 = 수집일로부터 최대 1년 — 단 서버 로그 보존 731→366일 이하 변경(BE CHMO-584) 완료 전까지 **앱판은 중립 표기 유지**(실제보다 짧게 고지하면 사실과 다른 고지) ② 탈퇴 회원 사진은 모임에 잔존(키즈노트식 — 파기 트리거는 사진 삭제·모임 삭제 둘뿐, 약관 제19조②·처리방침 §5·특징정보 안내 §5·가입 동의 전문 동시 반영. 잔존의 법적 구성은 변호사 검토필요 유지)
- `docs/admin-spec.md` — **관리자(어드민) 페이지 명세**(CHMO-375 확정): 내부 운영자용 백오피스. 1차는 조회 전용(대시보드·모임 목록·모임 상세) · 같은 리포 `/admin` 라우트(+lazy) · 유저 role 인증(`/admin/*`에서만 검사, 기존 서비스 API는 role을 안 본다) · 인프라 지표는 Grafana 담당이라 제외. 나중에 별도 리포로 격리하기 위한 규칙 3개(관리자 코드는 `src/admin/`에만·공유는 바닥 인프라뿐·BE 경로 `/admin/*` 분리)를 구현 시 지킨다. **화면 설계(CHMO-376) 완료 — §3-0**: 피그마 페이지 `어드민 (Admin)`(`287:3`, 모바일 와이어프레임 페이지와 분리)에 A1 대시보드(`289:3`)·A2 모임 목록(`289:45`)·A3 모임 상세(`289:87`) + 공용 컴포넌트(사이드바·지표 카드·상태 배지·로고 심볼). 데스크탑 1440×1024(사이드바 240 + 상단바 56 + 콘텐츠), 팔레트는 **중립 그레이 + 치즈 옐로우 포인트**(토큰은 `치즈모아 토큰` 컬렉션의 `admin/` 그룹 — 모바일 cream/brown을 쓰지 않는다)
- `docs/design/screen-system.dc.html` — **디자인 시안(공용 컴포넌트 스펙)**: Header/Button/Badge/Toggle/카드류/PhotoTile/오버레이/Toast의 색·크기·규칙. 토큰 원천은 `tailwind.config.js`(충돌 시 코드가 이김) · `docs/design/logo-v3.dc.html` — 로고 시안(체다 심볼)

## 확정 MVP 모델 (코드로 추론 불가 — 반드시 준수)

- 정보구조: 모임(Group) > 이벤트(Event) > 앨범(Album). 앨범 `type`: `person`/`common`/`uncertain`/`eyes_closed`/`blurry`.
- 제작자 인증: **소셜 로그인 4종(카카오·구글·네이버·애플)으로 완전 대체 확정**(CHMO-359, 2026-07-20 — 미배포·사용자 0명이라 마이그레이션·계정 연결 없음). 닉네임+4자리 PIN은 **화면에서 내렸다**(CHMO-557 — 01-1·01-2 삭제, DEV 전용 `/dev/login`만 잔존. api 함수·목·계약 테스트는 BE 엔드포인트 제거 전까지 유지). **소셜 표시명은 사람 이름 기반**(2026-07-25 확정 — 네이버 검수 반려(이름 활용처 증빙)가 계기): 구글(`name`)·애플(성+이름)은 기구현, 네이버는 별명→이름 전환(BE CHMO-439 — **실서버 배포 후에** 네이버 재검수 신청, 배포 전 신청 금지), 카카오는 실명 스코프가 비즈니스 앱 검수 필요라 카톡 닉네임 유지. 설정 화면 라벨도 '이름'(CHMO-440 — 데이터 필드는 BE 계약대로 `nickname`, 레거시 PIN 로그인·가입 화면은 '닉네임' 유지). **권한 등급 없음** — 로그인한 모임 멤버는 전원 동일(업로드·검수·공개 가능).
- 합류는 **role 무관 승인제**(CHMO-475, 2026-07-28 BE 배포): joinKey는 "누가 신청할 수 있나(=어떤 role로)"만 정하고, "누가 들어오나"는 그 모임의 ACTIVE 선생님이 승인으로 정한다 — 선생님 키 합류도 즉시 ACTIVE가 아니라 PENDING 신청이다. **모임 생성자만 예외**로 즉시 TEACHER/ACTIVE. 승인 전 모임 API(상세·이벤트·앨범)는 전부 `SPACE403`(FE `PENDING_APPROVAL`). 신청 목록엔 선생님·학부모가 섞여 오고(role로 구분·선생님은 childNames 빈 배열), 승인/거절은 기존 API 그대로(`PATCH /join-requests/:id` — 거절은 행 삭제라 재신청 가능).
- 모임 진입점 2개(분리): ① **선생님 초대** = 제작자 합류(모임 단위, 모임 비밀번호+참여 링크) ② **학부모 공개** = 무로그인 뷰어(**모임 단위** 공유 링크 + **학부모 전용 비밀번호**(모임 비밀번호와 별개, 모임 생성 시 자동), 조회·다운로드만). 비번 입력 후 **공개된 이벤트 목록**에서 선택.
- 공개(publish) 단위: **이벤트별**(검수 → 공개 요약(14) → 공개하기). 학부모 접근(공유) 단위: **모임별**(공개된 이벤트만 목록 노출). 상세 [[viewer-share-group-level]].
- 검수: **사진 단위 `reviewed`**(앨범 [검토 완료] = 일괄 처리, 앨범 자체엔 검토 상태 없음 — 실 BE는 CHMO-395로 매핑(AlbumPhoto) 단위, 목은 사진 단위 근사). 뷰어 노출 = 사진 `reviewed && published`(서버 필터링, CHMO-324). · **공개 게이트 = 전량 검토 완료(하드)**(CHMO-487·488, 2026-07-28 정책 변경 — **재공개 게이트 CHMO-324·265 폐지·`?force=true` 폐기**): 인물·공통 앨범에 미검토가 1장이라도 남으면 공개할 수 없다(서버는 항상 `PUBLISH409`, 14는 [공개하기]를 잠그고 막고 있는 앨범을 이름·남은 장수로 안내). 판정 범위가 인물·공통뿐인 이유는 특수 앨범엔 검토 UI가 없어(CHMO-357) 포함하면 영영 공개 불가한 이벤트가 생기기 때문 — 목 게이트도 여기에 맞췄다(`unreviewedGatePhotoCount`, 종전 목은 이벤트 전 사진을 세서 force가 그 차이를 삼키고 있었다). **검토 완료는 비가역**(해제 폐지 — 되돌리려면 삭제뿐)이라 09 [검토 완료]에 확인 다이얼로그를 되살렸다(CHMO-413 반전). `pendingPublishCount`는 화면이 더는 읽지 않는다(BE가 걷어낼 때까지 매퍼만 통과 — 값은 구조적으로 항상 0) · **공개 후 사진 추가·재분석은 폐지**(CHMO-486 — 업로드·분류는 **이벤트당 1회**). 삭제·이동은 그대로 가능하고 옮긴 사진은 `reviewed`·`published`를 유지해 학부모 화면에 **즉시** 반영되므로, published 이벤트의 09-1 이동은 확인 다이얼로그를 거친다(CHMO-488 — 미공개는 종전대로 즉시).
- 타깃: 기관(B2B/유치원) 우선. 일반(B2C) 전용 화면은 후순위.

## 하드 제약

- **BE/AI 코드는 건드리지 않는다.** 이번 범위는 프론트엔드뿐.
- **API 계약 기준은 실 BE다**(오리진 `https://api.cheese-moa.com` — Cloudflare 경유 HTTPS(CHMO-260), EC2 직통 IP는 보안그룹으로 차단, 스웨거 `/v3/api-docs`) — `docs/api-spec.md`와 안 맞는 부분은 **FE를 BE에 맞춘다**. BE 규약: 성공/실패 모두 `{isSuccess, code, message, result}` 봉투 · 숫자 ID(int64) · 경로에 `/api/v1` 프리픽스 없음(vite 프록시가 rewrite) · 시각이 오프셋 없는 UTC(CHMO-205 전까지 FE 보정). 봉투·에러 코드·시각 차이는 `src/api/client.ts`·`src/api/errors.ts`가, 리소스 필드명(`groupId`≠`id`·`eventDate`·`thumbnailUrl`·`personName`)·대문자 enum·서버가 주지 않는 파생값(특수 앨범 표시명·`visibleToViewer`·원본 `url`)은 `src/api/mappers.ts`+도메인 모듈이 흡수한다(CHMO-192) — 화면 코드는 봉투도 BE 필드명도 모른다. 매퍼의 절반은 단순 개명이라 **CHMO-201(BE DTO 필드명 정렬) 확정 후 걷어낼 수 있다** — 그 전엔 움직이는 과녁이라 유지한다. **리프레시 토큰(CHMO-193) 완료** — accessToken(만료 1시간) 401 시 `src/api/client.ts`가 refreshToken으로 1회 자동 재발급(회전 저장·in-flight promise 공유) 후 원 요청 재시도, 실패(TOKEN401) 시 두 토큰 삭제·로그인 복귀. 로그아웃은 `POST /auth/logout`(auth 미첨부, body `{refreshToken}`)으로 서버 무효화 후 로컬 삭제(`src/lib/auth.ts`가 두 토큰 저장 소유). **계약 테스트(CHMO-219) 완료** — 실 BE 응답 픽스처로 봉투·시각 보정·401 재발급·매퍼를 고정했다(`npm run test`). **에러 코드는 실서버에서 채집된 것만 목이 흉내 낸다**(COMMON401·AUTH400/401·TOKEN401·JOIN403·SPACE404·MOMENT404·ALBUM404·PHOTO400/404·VALID400·PUBLISH409 — SPACE404 = 모임 404, 2026-07-16 채집, CHMO-285 · PUBLISH409 = 미검토 공개, 2026-07-22 채집, CHMO-265. PUBLISH400은 BE 소스 대조만 · **SPACE403 = 승인 전 모임 접근, 2026-07-28 BE 완료 공지(CHMO-475 AC2 — 실서버 채집은 학부모 전환 배포 후 CHMO-449)**). 미채집 케이스(닉네임 중복·이미 멤버)는 FE 의미 코드를 그대로 두고 `// BE 코드 미확인` 주석을 단다 — `errors.ts`가 미지 코드를 통과시키므로 화면은 동작하고, 추측한 코드를 BE 진실 테이블에 굳히지 않는다. 남은 몫: CHMO-196(문서 갱신 — 목이 구현한 계약이 곧 갱신본).
- **BE 결함 4건은 2026-07-10 전부 완료 — 이 제약은 해제됐다**(CHMO-194 착수 중 발견해 티켓을 냈던 것들): 사진 추가 시 기존 사진·앨범·검수 상태 삭제(CHMO-215 — 증분 재분석으로 전환) · 공개 이벤트에 사진 추가 시 `published` 해제(CHMO-216) · 요청당 50장 상한이라 200~300장 경로 없음(CHMO-217) · 분석 실패가 `EMPTY`로 회귀해 미시작과 구분 불가(CHMO-218). 다만 **2026-07-28 정책 변경으로 사진 추가·재분석 경로 자체가 폐지됐다**(CHMO-486 — 업로드·분류는 이벤트당 1회. 08 [+ 사진 추가] 제거·06-U는 `photoCount > 0`이면 진입 차단·목은 presign·등록 양쪽에 게이트. 서버 게이트는 CHMO-485). 단 주의 두 가지: ① Jira '완료'일 뿐 **실 BE 배포·동작은 FE가 검증하지 않았다**, 이 경로를 건드리는 스토리는 실 BE로 먼저 확인한다. ② **업로드 상한은 BE 계약과 화면이 서로 다른 값이다**(CHMO-497) — BE는 요청당 **500장**(2026-07-28 실측: presign·등록 501개 → `VALID400 "한 번에 500장까지 업로드할 수 있습니다."` — CHMO-482 배포 확인. 그 전 상한 50은 2026-07-24 실측분), 웹 화면은 **100장**에서 캡한다. 상수도 갈라 뒀다(`src/lib/upload.ts`): `MAX_UPLOAD_BATCH = 500`은 **BE가 받아 주는 수**(목·계약 테스트가 쓴다), `MAX_UPLOAD_PICK = 100`은 **웹이 스스로 거는 수**(06-U만 쓴다 — 브라우저는 고른 파일을 전부 디코드해 미리보기를 만들어야 해서 장수가 곧 CPU·메모리 부담이고, 500장 규모는 앱의 네이티브 업로드가 맡는다). BE 상한이 더 올라가도 웹은 100을 지키고, 반대로 `MAX_UPLOAD_PICK > MAX_UPLOAD_BATCH`가 되면 BE가 배치 전체를 400으로 거절한다(`upload.test.ts`가 이 부등식을 고정). 06-U는 선택 시점 100장 캡으로 대응한다(CHMO-397). 등록이 곧 분석 시작(analyzing 중 등록 400)이라 분할 업로드는 불가하고, **1회 정책이라 이어 올리기도 없다**(CHMO-430은 철회된 선행 티켓). ⚠ 목은 크기 검증을 1회 게이트·analyzing 체크 **뒤에** 두는데 실 BE는 `@Size`가 **먼저**다(501장이 SPACE403 대신 VALID400) — 화면이 100에서 캡해 도달 불가라 방치 중.
- FE 개발 기본은 여전히 **MSW 목**(`VITE_ENABLE_MSW=true`) — 실 BE 확인 시 `false`로 끄면 프록시 경유. 실 BE 테스트 계정: **`test`/PIN `1111`**(2026-07-30 — 로그인 확인은 항상 이 계정. 종전 `FE연동테스트`/`0709` 대체. 목 시드에는 없는 계정이라 이걸 쓸 때는 MSW를 끈다. **진입은 01 랜딩 DEV 링크 → `/dev/login`** — PIN 로그인 화면은 프로덕션에서 내렸다, CHMO-557).
- MVP 제외: 결제/요금제, 다운로드 한도, 휴지통, 인물 병합, 학부모 로그인.

## 기술 스택

React 18 + Vite 5 + TypeScript(모바일웹, 기준 프레임 390×844) · 라우팅 react-router v6 · 스타일링 **Tailwind CSS v3**(토큰은 `tailwind.config.js` + `src/index.css`) · 데이터패칭 커스텀 `useApi` 훅 · API 목 레이어 **MSW**(`VITE_ENABLE_MSW=true`일 때만 활성 — 전 엔드포인트를 **실 BE 계약대로** 구현, CHMO-195). MSW를 끄면 `/api/v1` 요청이 vite dev 프록시로 실 BE에 전달된다(`VITE_API_ORIGIN`, 기본 `https://api.cheese-moa.com` — CHMO-191·260). 테스트는 **Vitest**(`vitest.config.ts`, node 환경 — jsdom·RTL 없음): 범위는 `src/api/`·`src/lib/` **계약 테스트**와 `src/mocks/serializers.test.ts` **이음매 테스트**뿐이고 화면 테스트는 의도적으로 제외한다(CHMO-219·195).

### 코드 구조 (`src/`)
- `main.tsx` 진입(+MSW 부트스트랩) · `router.tsx` 전 라우트 정의 · `index.css` Tailwind+디자인 토큰.
- `components/` PhoneShell(390×844 프레임 — **뷰포트 고정 `h-dvh`**: 문서 스크롤이 없고 스크롤은 화면별 콘텐츠 영역(`overflow-y-auto`)이 소유, 하단 고정 요소는 safe-area 합성 간격 `pb-safe-6/7/9`(tailwind.config spacing), 중앙 정렬형은 `justify-center` 대신 `my-auto`(넘칠 때 위 잘림 방지), CHMO-396 · sm+ 코너 클리핑은 `overflow-clip`: hidden이면 sticky가 프레임에 갇힌다, CHMO-369)·BrandHero(`onSymbolTap` — 심사용 숨김 진입 탭, 01만 전달)·PinLoginForm(닉네임(ID)+PIN 로그인 폼 공용 — `/dev/login`·01 심사용 숨김 폼이 같은 제출 흐름, CHMO-572)·SocialLoginButtons(소셜 로그인 버튼 4종 — 01·01-1·01-2 공용, 브랜드 고정색, CHMO-359 · 앱 웹뷰에선 브리지 `socialLogin`으로 시스템 인증 세션 왕복 후 callbackUrl 쿼리를 SPA 이동으로 `/auth/callback`에 합류 — 인가 URL은 `socialAuthorizeUrlForApp`(실 BE 절대주소+`client=app`, MSW 상대경로 함정 회피), 취소(CANCELLED)는 무토스트 제자리, MSW 목 모드·capability 없음은 종전 브라우저 이동 그대로, CHMO-539)·JoinGroupModal(02-1 참여 모달 — **초대 링크 전용**: 홈 진입점은 CHMO-513에서 삭제, `/join/:joinKey`만 쓴다)·GroupInviteLinks(20 초대 화면의 '부르기' 섹션 — 역할별 비밀번호·링크복사·카카오톡 공유. CHMO-446의 05-2 통합 시트 본문을 이관한 것이고, 시트(`GroupInviteSheet`)·그 전신(InviteSheet·ParentShareSheet)은 모두 폐지됐다, CHMO-520)·PhotoLightbox(사진 크게 보기 공용 라이트박스 — 09 검수·16 뷰어, iOS 사진 앱풍 라이트 크롬(흰 배경·상단 ✕/카운터·하단 아이콘 툴바)·좌우 스와이프·개별 저장 + info/actions 슬롯, CHMO-242)·AlbumSettingsSheet(인물 앨범 설정 바텀시트 — 이름 수정 + 학부모 연결/해제 + (09만) 앨범 삭제 진입, 08 카드 이름 줄 탭·09 헤더 `[✎ 앨범 설정]` 공용, CHMO-522) · `components/ui/` **공용 컴포넌트 세트**(CHMO-107: Header·Button(+ButtonLink)·Badge·Toggle·EmptyState·카드류·PhotoTile/Grid·Modal·BottomSheet(끌어내려 닫기 제스처 — 포인터 이벤트라 터치·마우스 공용, 수직/수평 축 판별로 내부 가로 스크롤과 공존·`touch-action: pan-x`로 터치 네이티브 팬 보존, CHMO-345 + `bodyScrollable`로 본문 세로 스크롤 모드(CHMO-522 — pan-x가 자손의 세로 스크롤까지 막아서, 이 모드에선 세로 팬을 브라우저에 남기고 끌어내려 닫기는 핸들·제목이 맡는다))·ConfirmDialog·Toast + TextField(CHMO-110) + ErrorState·PinField(CHMO-118: ErrorState = useApi 실패 공용, 401 `unauthorizedTo` 복귀·404 `notFoundTo` 돌아가기 CTA / PinField = 4자리 PIN 입력 공용) + LoadState(CHMO-181 — useApi 로딩/에러 폴백 공용: loading이면 로딩 문구, error면 ErrorState, 둘 다 아니면 null) + CountBadge·InlineRetry(CHMO-520: CountBadge = '처리할 것이 N건' 갈색 필 — 05 [＋ 초대하기]·20 탭 공용, 수만 알리는 장식이라 aria-hidden이고 호출부가 전체 문장을 aria-label로 단다 / InlineRetry = 곁가지 조회 실패를 **화면을 가리지 않고** 알리는 한 줄 — LoadState/ErrorState는 본문을 통째로 대체하므로 그 조회가 주 내용이 아닐 때 쓴다) + icons(SF Symbols풍 라인 아이콘 IconDownload·IconTrash·IconFolderMove·IconClose — currentColor, 라이트박스·09 선택모드·16 다운로드 버튼 공용, CHMO-242) — 데모 `/dev/components`, DEV 전용) · `guards/` 제작자/뷰어 라우트 가드 · `pages/`(+`pages/viewer/`) 화면 — 인증 01(소셜 단일 랜딩 — `/`·`/login` 겸용·returnTo 배너 소비, CHMO-557 + 로고 5회 탭 심사용 숨김 ID/PIN 폼, CHMO-572. 01-1·01-2·AuthCredentialsForm은 삭제, DEV 전용 `pages/dev/DevLoginPage` `/dev/login`만 잔존)·01-C 소셜 콜백(SocialCallbackPage — `/auth/callback` 코드 교환, 가드 밖, CHMO-359)·모임 진입 02·03·02-1(CHMO-111)·모임 허브 05(CHMO-112)·20 초대(InviteManagePage + 20-1 ChildLinkSheet — 링크·승인·명단이 한 화면, CHMO-447·520)·18/19 학부모 조회(ParentGroupPage·ParentEventPhotosPage — `/parent/groups/…`, CHMO-448)·이벤트 06-M(05 위 모달)·06-E/분석중(EventDetailPage 상태 분기)·06-U(PhotoUploadPage, CHMO-113 — 카운트·토글은 일반 흐름, CTA는 스크롤 밖 하단 고정: CHMO-369의 화면 내 sticky는 실기기 WebKit에서 상·하단 모두 바가 떠 사진이 비쳐 철회(CHMO-424)·미리보기 축소 썸네일(원본 업로드는 그대로, CHMO-369)·업로드 100장 상한 UX(CHMO-397·497 — "한 번에 최대 100장" 사전 고지 상시 노출, 파일 선택 시 남은 슬롯까지만 선택 상태로 담고 초과분은 해제 상태+토스트, 캡 도달 후 신규 선택 탭은 토스트로 차단, 미선택 잔여분은 **올라가지 않는다는 사실을 그대로 고지**(CHMO-486 — 1회 정책이라 "다음에 이어 올리세요"는 거짓 안내) — 기존 overBatchLimit 경고·CTA 비활성은 회귀 방어선으로만 잔존. 상한 원천은 `MAX_UPLOAD_PICK`(화면 상수)이고 BE 계약 상한 `MAX_UPLOAD_BATCH`(500)와 별개다)·미리보기는 **원본 장당 1회 디코드**(CHMO-497 — 타일에 원본 objectURL을 꽂지 않고 썸네일이 올 때까지 PhotoTile 플레이스홀더, 준비 소요는 DEV에서만 화면에 표기)·등록 응답 유실 안전망(등록 단계 실패 시 상세를 되물어 실제로 시작됐으면 에러 대신 인계, CHMO-486))·08 앨범 그리드(EventDetailPage 안 EventAlbumGrid + ⚙ 이벤트 설정 = RenameModal(이름 수정)+이벤트 삭제, CHMO-114·278)·09 앨범 상세(AlbumDetailPage 선택모드·삭제, CHMO-114)·설정(SettingsPage — GET/PATCH /me + 로그아웃, CHMO-118 + 약관·정책 링크 3종, CHMO-478)·약관 전문(LegalDocPage — `/legal/{terms,privacy,biometric}` 가드 밖 공용 렌더러, 문서 데이터는 `src/legal/`, CHMO-478)·404(NotFoundPage) 포함 전 화면 실 UI.
- `api/` **API 계층**(CHMO-191·192·193) — `client.ts`(fetch 래퍼: 실 BE 봉투 `result` 언랩·시각 'Z' 보정 + 제작자 401 시 refreshToken 1회 자동 재발급 후 재시도(`/auth/refresh` 경로 소유 — transport 인프라) + 뮤테이션 401 공용 `redirectIfUnauthorized`)·`errors.ts`(BE 에러 코드→FE 의미 코드 매핑, 미지 코드는 통과)·**도메인 모듈** `auth.ts`·`groups.ts`·`events.ts`·`albums.ts`·`viewer.ts`·`agreements.ts`(엔드포인트 함수 — 경로·바디·응답 변환 소유, 화면은 이것만 호출. `agreements.ts`는 약관 동의 3종 — 조회·제출·보호자 동의 확보 확인, CHMO-516)·`mappers.ts`(BE DTO→FE 타입 변환 — 내부 전용, 화면에서 import 금지) · `lib/` `auth.ts`(제작자 토큰 — accessToken+refreshToken 저장·삭제 단일 소유, CHMO-193)·`viewer.ts`(뷰어 토큰 + 모임명 캐시 — BE는 모임명을 unlock 응답에만 준다)·`clipboard.ts`(복사 헬퍼)·`download.ts`(blob 저장 헬퍼 — 교차 출처 download 무시·서비스워커 우회 함정 회피(CHMO-242) + `cache:'no-store'`로 `<img>`가 오염시킨 CORS 헤더 없는 캐시 재사용 차단(CHMO-326 — S3는 Origin 없는 요청에 ACAO·Vary를 안 줌))·`pin.ts`(4자리 PIN 규칙 단일 원천 — UI·목 공유)·`albumLabels.ts`(특수 앨범 표시명 단일 원천 — UI·목 공유, CHMO-192)·`uncertainCauses.ts`(분류 사유 코드→문구 단일 원천 — 문구는 FE 소유, 미지 코드 무시·빈 결과는 범용 문구 수렴, CHMO-412)·`albumSort.ts`(앨범 표시 정렬 단일 원천 — 인물 가나다(이름 없음 뒤)→공통→분류어려움, 검토 상태 무관 고정 · 08·14·15 공용, 09-1 추천은 유사도 순이라 제외. 실 BE 응답은 미검토 우선+albumId(CHMO-336)라 화면이 정렬을 소유하고 목도 실 BE 순서로 응답, CHMO-411)·`upload.ts`(업로드 제약 단일 원천 — 확장자 화이트리스트·20MB·50장, UI·목 공유, CHMO-194)·`concurrency.ts`(S3 PUT 동시 실행 수 제한)·`previewThumb.ts`(06-U 업로드 미리보기 축소 썸네일 — 원본 수십 장 타일 디코드 프리즈 방지, 실패 시 원본 폴백, CHMO-369. **디코드는 장당 1회**(CHMO-497): 1순위 `createImageBitmap(file, {resizeWidth, resizeQuality})`로 디코드 단계에서 축소해 원본 해상도 비트맵을 메모리에 안 올리고 — resize 옵션을 무시하는 브라우저도 뒤이은 캔버스 축소가 같은 결과를 만들어 **능력 감지가 필요 없다** — 이 API가 못 읽는 포맷(HEIC 등)만 `<img>`+캔버스 폴백) · `hooks/` `useApi.ts`(**`useApi(key, fetcher)`** — key가 요청 정체성, fetcher는 도메인 함수에 signal만 전달)·`useAlive.ts`(언마운트 후 늦은 응답 차단)·`useMutation.ts`(뮤테이션 공용 스켈레톤 — unmount 가드+401 리다이렉트+에러 메시지 정규화 소유, handle\*엔 성공/실패 후속만 남긴다; busy/성공/에러표시는 호출부 소유라 회귀 없음, CHMO-181)·`useDragScrollX.ts`(가로 스크롤 목록 마우스 끌기 스크롤 — 터치는 네이티브 팬 유지·드래그 후 click 삼킴, 09-1 추천 목록, CHMO-345) · `types/api.ts`(API 계약 타입 — `ID = number`, 라우트 파라미터는 도메인 함수가 `ID | string`으로 받아 경로에 그대로 쓴다).
- `native/` **앱(Flutter 웹뷰) 브리지 계층**(CHMO-537) — `types.ts`(계약 v1 타입)·`bridge.ts`(전부): 앱 감지 단일 원천 `isNativeApp()` = **UA 마커 `CheeseMoaApp/`만 본다**(주입 객체 `window.flutter_inappwebview`는 platformReady 이벤트 후에야 생겨 감지에 안 쓴다 — 호출 통로일 뿐)·`callBridge()` 래퍼(platformReady 대기·기본 5s 타임아웃(ready 대기는 장기 실행이어도 5s 캡)·봉투 `{ok,…}` 해석·미지 에러 코드 통과)·capability 세션 캐시 `hasCapability()`(실패는 캐시 안 함 — 일시 실패가 기능을 영영 숨기지 않게, 구버전 앱 하위 호환은 웹이 소유)·진행 이벤트 구독 `subscribeBridgeProgress`(`cheesemoa:event`, opId는 웹이 `newOpId()`로 생성)·타입드 메서드 `socialLogin`·`savePhotos`(둘은 무제한 — 소요가 사용자·망에 비례)·`openAppSettings`·`getCapabilities`. **웹 단독 실행은 전부 no-op/UNSUPPORTED**(isNativeApp false·hasCapability false)라 기존 화면 무영향. **계약 원천은 앱 리포 `CheeseMoa-App/docs/app-shell-spec.md` §2**(CHMO-535 — 타입을 바꾸려면 그 문서·Flutter 셸과 함께) · 화면·lib는 이 모듈만 import(소비: 소셜 로그인 CHMO-539 완료 — SocialLoginButtons · 사진 저장 CHMO-540 예정).
- `test/` **테스트 하네스**(CHMO-219) — `setup.ts`(node용 localStorage + 테스트 간 토큰 격리)·`http.ts`(fetch 스텁·응답 빌더)·`fixtures/be.ts`(**실 BE 응답 원문** — 2026-07-10 채집분 + 스웨거 스키마 기준분 + 2026-07-16 SPACE404, 시크릿은 치환). 테스트는 대상 옆에 둔다(`src/api/*.test.ts`·`src/lib/*.test.ts`·`src/mocks/serializers.test.ts`). **MSW를 HTTP 서버로 쓰지 않는다** — 목이 목을 검증하는 꼴이다. 두 테스트가 지키는 건 서로 다르다: `contracts.test.ts` = "매퍼가 **실 BE**와 말이 통하는가"(BE 픽스처), `mocks/serializers.test.ts` = "목과 매퍼가 서로 말이 통하는가"(tsc는 이 둘을 잇지 못한다 — 목이 `id`를 주고 매퍼가 `albumId`를 읽어도 빌드는 통과한다).
- `mocks/` **MSW 목 레이어**(CHMO-108·109, BE 계약 이행 CHMO-195) — `db.ts`(인메모리 스토어·상태머신·다대다 — `personNameOf`는 인물만 이름을 주고 특수 앨범은 null, BE와 동일. 분류 로직 자체는 증분("앨범 없는 사진만")이지만 **2회차 등록이 게이트로 막혀 도달하지 않는다**, CHMO-486) · `fixtures.ts`(시드 — 호출마다 새 객체) · `persist.ts`(가입 계정 localStorage 보존) · `handlers/`(auth·groups·events(+검수요약/공개)·albums(검수)·share(학부모 뷰어)·agreements(약관 동의·보호자 동의 확보 확인, CHMO-516) + `shared.ts` 봉투 헬퍼(`ok`/`created`/`errorResponse`)·검증 헬퍼(`toId` 경로/본문 id 정규화 포함)·`serializers.ts`(**BE DTO 직렬화** — 함수명이 BE DTO를 따른다(`toAlbumSummary`≠`toAlbumDetail`), 목록/상세가 다른 필드를 주는 것까지 BE 그대로. 뷰어 필터링 포함)). 시드 ID는 숫자(모임 1~3·이벤트 1~4·앨범 1~12·사진 101/201/301대, 신규 발급 1001~ — CHMO-191). 업로드 3단계도 BE 계약대로 구현(CHMO-194): presign은 사진을 만들지 않고 `originals/events/{id}/{uuid}.{ext}` 키만 발급, 가짜 S3 PUT(`/mock-s3/*`)이 `db.uploadedKeys`에 기록, 등록(`POST /events/:id/photos`)이 사진 생성 + 분석 시작을 맡는다(PUT 안 한 키는 404). 시드 로그인: 이현정/1234 · 학부모 공유 시연: `/share/shr_grp1`, 비번 7421.

## 작업 방식

- **애매하면 추론하지 말고 사용자에게 묻는다.** (사용자 명시 선호)
- 사용자·문서는 한국어 — **한국어로 응답**한다.
- 화면 작업 시 와이어프레임을 Figma MCP로 확인: file `pcgDOk6iZYtuUEhKhaPWPM`, page `211:1311`. 화면별 노드 id는 `docs/screen-spec.md` 매핑표 참조.
- **브랜치·커밋 규칙**: `docs/convention.md` 준수 — Git Flow(`main`/`develop`/`feature`/`release`/`hotfix`), 커밋 메시지는 `[CHMO-###] type: 메세지` 형식.
- **스토리 작업 흐름**: 착수는 `/start-story CHMO-###`(Jira SP·상태 세팅 + `develop` 기준 `feature/CHMO-###-*` 브랜치), 마무리는 `/finish-story`(커밋·푸시·`develop` 대상 PR 생성 + **develop 머지·Jira '완료' 전환까지** — 2026-08-03 사용자 결정으로 자동화. **`main` 머지는 릴리즈 성격이라 사용자가 명시 지시할 때만**). `main`·`develop` 직접 커밋/푸시는 가드 훅이 차단한다. 담당자·상태는 Jira에서 직접 지정한다. 백로그는 Jira 프로젝트 `CHMO`(에픽 CHMO-96~105 / 스토리 CHMO-106~. 실연동 후속: 192 엔드포인트 계층 추출·193 리프레시 토큰(완료)·194 업로드 3단계·195 목 이행·196 API 문서 갱신).

## 미확정 결정 (임의로 정하지 말 것 · 상세 `docs/screen-spec.md` §5)

홈 관리자 배지 표시 여부 · 품질 제외 사진 라우팅(눈감음/흔들림 앨범) · 공통(common) 앨범 정의. (학부모 공유 = 모임 단위로 **확정**됨.)

## 명령어

- `npm run dev` — 개발 서버(Vite, 기본 5173) · `npm run build` — 타입체크(`tsc --noEmit`)+프로덕션 빌드 · `npm run preview` — 빌드 결과 미리보기.
- `npm run typecheck` · `npm run lint` — 검사 · `npm run test`(=`vitest run`)/`test:watch` — **api 계층 계약 테스트**(CHMO-219).
- Prettier: `npm run format`(쓰기)/`npm run format:check`(검사). **주의**: 리포에 기존 CRLF/포맷 잔재가 있어 `format:check`는 클린 체크아웃에서도 실패한다 — 통과/실패 신호로 쓰지 말 것. `format` 전체 실행도 무관 파일까지 재포맷하므로 커밋 전엔 스토리 파일만 스테이징한다.

**워크플로우 스킬**(`.claude/skills/`):
- `/start-story CHMO-###` — 스토리 시작: SP 추천·설정, 상태 '진행 중', `feature/CHMO-###-<slug>` 브랜치 생성.
- `/test-guide` — 현재 브랜치 변경분 기준 테스트 가이드 생성: 자동 검증(typecheck·lint·build) 실행 + "진입 경로 → 조작 → 기대 결과" 수동 체크리스트.
- `/finish-story` — 커밋·푸시·PR 생성(테스트 가이드 포함) + Jira PR 코멘트(best-effort) + **develop 머지(머지 커밋)·원격/로컬 브랜치 정리·Jira '완료' 전환까지**. `main` 머지는 사용자가 명시 지시할 때만(릴리즈).
- `/bug-briefing` — 버그·코드리뷰 결함을 "무슨 코드 → 언제 터지나 → 왜 그렇게 됐나 → 어떻게 고치나" 4단 형식 + 코드 위치 링크 + 요약 표로 브리핑.

**가드 훅**(`.claude/settings.json` → `.claude/hooks/`): `main` 직접 commit/push 차단, commit/push 전 typecheck·lint·test(스크립트 정의 시) 실행.
