# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트

치즈모아 — 유치원 행사 사진을 AI가 아이별 앨범으로 자동 분류 → 선생님이 검수 → **이벤트별 공개** → 학부모가 링크+비밀번호로 **무로그인** 조회·다운로드하는 **모바일웹** 서비스(MVP).

현재 단계: **스캐폴딩(CHMO-106) + 공용 컴포넌트 세트(CHMO-107) + MSW 목 레이어(CHMO-108·109) + 인증 플로우 실 UI(CHMO-110) + 모임 진입 실 UI(CHMO-111) + 모임 허브 실 UI(CHMO-112) + 이벤트·업로드·분석 실 UI(CHMO-113) + 앨범 그리드·앨범 상세/삭제 실 UI(CHMO-114) + 옮기기·이름전파 실 UI(CHMO-115) + 공개 전 검수·공개 실 UI(CHMO-116) + 학부모 뷰어 실 UI(CHMO-117) + 설정/프로필·통합 마무리(CHMO-118) 완료 — 전 화면 실 UI.** Vite+React18+TS 토대·Tailwind v3 디자인 시스템·전 라우트 뼈대 + 재사용 UI 세트(`src/components/ui/`, 데모 `/dev/components`) + 목 DB·픽스처·api-spec 전 엔드포인트 핸들러(`src/mocks/` — 인증·모임·이벤트·앨범·검수·공개·뷰어) + 인증 3화면(01·01-1·01-2 + **소셜 로그인 4종 병행**(카카오·구글·네이버·애플, CHMO-359) — 버튼은 fetch가 아닌 브라우저 이동으로 BE `/auth/social/{provider}` 시작(오리진은 `VITE_API_ORIGIN`, MSW 목은 내비게이션을 못 가로채 콜백 직행), BE가 FE `/auth/callback?code=일회용코드`(TTL 60초)로 복귀하면 `POST /auth/social/exchange`로 토큰 교환 — 응답이 기존 로그인과 동일 형태라 토큰 저장·리프레시 그대로. returnTo는 sessionStorage 위탁(외부 리다이렉트는 router state 유실). 실패는 BE가 `?error=`로 싣고 화면이 셋으로 갈라 안내한다(`OAUTH400` 미지원 프로바이더=다른 방법 유도·`OAUTH401` 인가 실패=재시도·`OAUTH502` 프로바이더 통신 실패=잠시 후 재시도, 미지 코드는 재시도로 수렴). 목의 일회용 코드도 실 BE처럼 **1회 소진**이라 시도마다 새 코드를 발급한다(고정 문자열이면 재사용 401 계약을 목이 못 지켜 콜백의 StrictMode 이중 교환 가드가 깨져도 로컬에선 안 드러난다). **4종 모두 실 BE 배포 완료**(2026-07-21 — 인가 URL 302 실측, 애플은 `.p8` base64 env가 깨져 기동 실패했다가 복구. 실계정 로그인 왕복 E2E는 FE 배포 후))·모임 진입 3화면(02 홈·03 모임 만들기·02-1 모임 참여 — 참여 코드(joinKey)는 실 BE가 대소문자 혼합 발급·구분 매칭이라 대소문자 변환 없이 그대로 전송(CHMO-285), 초대 링크 복사 URL의 쿼리형 불일치는 CHMO-237에서)·모임 허브(05 모임 상세 + 초대·학부모 공유 시트 + ⚙ 모임 설정 — 이름 수정·모임 삭제(CHMO-277, `DELETE /groups/:id` — 실 BE는 CHMO-273 배포 대기, 목 선행))·이벤트 흐름(06-M 생성 모달·06-E 빈 이벤트·06-U 업로드→AI 분석, 분석중 상태 포함 — 분석중은 `GET /events/:id` 상세의 `progress`(`{processed,total,percent}`, 분석 job 중에만 non-null·목록 응답엔 없음)를 2초 폴링해 쥐→치즈 프로그레스로 표시, 진행률 null이면 쥐 왕복 인디터미넌트 폴백(CHMO-287) — 분석중 판정은 analyzing 상태 외에 `progress` non-null도 포함(CHMO-442)하고, **06-U 진입 차단은 여기에 `photoCount > 0`을 더한다**(CHMO-486 — 업로드는 이벤트당 1회라 사진이 있는 이벤트는 딥링크로도 못 연다. 분석중/1회 소진은 다음 할 일이 달라 안내 문구를 가른다). 단 등록 직후엔 AI 첫 진행률 메시지 전이라 progress가 null인 공백이 있어(2026-07-26 실서버 실측 — 등록 +0.7s에 null) 업로드 화면이 navigate state로 '분석 시작' 킥(expected=직전 photoCount+registeredCount)을 넘겨 상세가 폴링을 켠다: photoCount 도달=완료 판정(사진은 분석 커밋 시점에야 반영 — 실측)·progress가 켜지면 인계·30초 안전 타임아웃·전부 중복이면 즉시 소멸. 목은 등록 즉시 progress 노출·사진 즉시 생성이라 킥이 무해 통과 — 킥 실검증은 실 BE로만 가능(CHMO-443))·검수 흐름(08 앨범 그리드+⚙ 이벤트 설정 — 이름 수정·이벤트 삭제(CHMO-278, `DELETE /events/:id` — 실 BE는 CHMO-272 배포 대기, 목 선행. 06-E·분석중에도 헤더 ⚙ 동일 진입)·09 앨범 상세+선택모드·삭제·옮기기(09-1 이동 시트 — 추천 맨 끝 '새 앨범' 타일(점선 원형+플러스)로 선택 사진을 새 인물 앨범으로 **생성=이동**(`POST /events/:id/albums` `{name,sourceAlbumId,photoIds}`, CHMO-435 — BE CHMO-416, 이름 필수 1~20자·소스 밖 사진 PHOTO404, 2026-07-25 실서버 프로브. 후보 0개여도 타일만으로 이동 가능) · **빈 앨범 잔류(CHMO-418 — CHMO-231 자동삭제 폐지·CHMO-289 복귀 반전)**: 전량 삭제·이동해도 앨범이 남아 refetch로 빈 상태를 보여준다(0장 카드는 검토 배지 숨김·점선 유지, 목 db.ts도 같은 계약) · **앨범 삭제(CHMO-435 — 전 타입)**: 09 앨범명 줄 🗑·빈 앨범 하단 CTA → 확인 다이얼로그(이 앨범에만 속한 사진은 영구 삭제 경고) → `DELETE /albums/:id`(CHMO-271) → 08 복귀(replace) · **빈 앨범 만들기(CHMO-471)**: 08 메인 그리드 **맨 끝** 점선 '앨범 만들기' 타일(09-1 '새 앨범' 타일과 같은 자리 관습 — 품질 제외 섹션 위) → 이름 모달(RenameModal 재사용 — `submitLabel`·`maxLength` prop 추가) → `POST /events/:id/albums`에 `{name}`만 실어 사진 0장 인물 앨범 생성 → 그리드 refetch(09로 보내지 않는다 — 빈 상세엔 할 게 없다). BE는 CHMO-456, **2026-07-27 실서버 프로브로 계약 확정**: `sourceAlbumId`·`photoIds`는 **짝**이라 한쪽만 실리면 `VALID400 "sourceAlbumId 와 photoIds 는 함께 보내거나 함께 생략해야 합니다."`, 검증 순서는 이름(@NotBlank·20자 — 공백만도 거부) → 짝 → 사진 수 → 이벤트 조회이고 문구도 이때 바뀌었다(종전 목의 "원본 앨범은 필수입니다."·"이름은 1~20자로 입력해 주세요."는 폐기). 이름 중복은 BE도 안 본다(personId 별도 발급). ⚠ **만든 빈 앨범은 09-1 이동 추천에 뜨지 않는다**(실측 — 얼굴 벡터 없는 수동 인물은 유사도 계산 제외, CHMO-416 AC-4): `POST /photos/move`는 빈 앨범을 대상으로 이미 정상 동작하므로 막힌 건 후보 노출뿐이고, **BE CHMO-472**(이벤트 전 앨범을 유사도 순 정렬·유사도 없으면 null로 뒤에) 배포 전까지는 사진을 모을 수 없다)·검토 완료는 일반 모드 버튼만(앨범 전체 대상이라 선택모드와 이질적이어서 선택모드 버튼 제거, CHMO-413 — 단 **확인 다이얼로그는 CHMO-488에서 복원**: 검토 해제 폐지로 비가역이 됐다)·성공 시 08 복귀(push — 앨범이 남아 뒤로가기 무해, CHMO-414)·라이트박스 크게 보기(CHMO-242 — 일반 모드 사진 탭, 검수 배지+개별 저장+라이트박스 내 삭제/옮기기. 사진 단위 검토 API는 BE 미도입이라 검토 액션 없음)·'분류가 어려워요' 분류 사유 표시(CHMO-412 — 앨범 상세 사진의 `faceBboxes`(애매 얼굴 bbox **배열**, 원본 px·좌상단)·`causes`(사유 코드 배열)를 소비: BE는 비면 키 생략(@JsonInclude NON_EMPTY)이라 매퍼가 빈 배열로 정규화, uncertain 앨범 라이트박스에서만 bbox 오버레이(naturalWidth 기준 환산)+사유 문구 노출. uncertain 앨범은 검토 UI(라이트박스 배지·[검토 완료]) 비노출 — 검토·발행 대상이 인물·공통뿐(CHMO-357)이라 08 카드 규칙(uncertain 배지 없음·항상 점선)과 정합. BE는 PR #107 develop 머지(2026-07-22)·**실서버 배포 미확인**, bbox·causes는 사진 최초 생성 시에만 저장(백필 없음)이라 실검증은 새 업로드로만 가능)·제작자 다운로드(CHMO-349 → **CHMO-473 앨범 직행 전환**: 일반 모드 [다운로드]=앨범 전체(미검토 포함 — 노출은 person/common 기존 규칙 유지)·선택모드 [저장]=선택 사진, 둘 다 아래 저장 파이프라인)·인물 앨범명 ✎ 이름전파·14 공개 요약+공개하기)·학부모 뷰어 흐름(잠금 해제(비번 4자→viewerToken, 401 시 apiFetch가 뷰어 토큰 삭제→잠금 화면 복귀)·15-L 공개 이벤트 목록·15 공개 앨범·16 사진 그리드+[다운로드](앨범 전체 — CHMO-473 저장 파이프라인)+개별 저장 라이트박스+공유(Web Share→복사 폴백)) 실제 UI. 설정/프로필(닉네임 프리필 — 라벨은 '이름'(CHMO-440, 소셜 표시명 이름 전환 정합)·PIN은 입력 시에만 `PATCH /me`에 포함 + 로그아웃)·404(EmptyState)도 실 UI — 스텁 없음(ScreenStub 제거, CHMO-118). 앨범 커버는 `Album.coverThumbnailUrl`(파생), 공개 요약(14) 미리보기는 `ReviewSummary.previewAlbums`(파생 — 08과 같은 앨범 카드 그리드, CHMO-346. **담는 범위는 전량 검토된 인물·공통 = 발행 대상과 같은 집합**(CHMO-488 — 종전 '검토분 1장 이상'은 공개해도 안 나갈 앨범까지 보여 줬다), 그래서 검토 범례도 없다. 14 통계는 사진/앨범/검토한 앨범 3열 — 분류 애매 카드 비노출(CHMO-347), 검토 진척은 인물·공통(사진 보유) 앨범 단위 FE 파생 `reviewedAlbumCount/reviewableAlbumCount`(CHMO-357 — BE reviewedAlbums는 특수 앨범까지 세서 안 쓰고, 완료 판정은 08 카드 테두리 규칙(미검토 0)과 동일해 통계·카드가 항상 일치. 미리보기 카드 수 = `reviewedAlbumCount`)), 뷰어 커버는 `ViewerEvent`/`ViewerAlbum.coverThumbnailUrl`(파생 — 발행(published) 사진 기준, CHMO-324). 인물 앨범 이름수정은 09 앨범 상세 헤더 ✎ + 08 카드 **이름 줄 탭**(CHMO-400 — person만, 이름 뒤 인라인 ✎ 힌트·카드 나머지 탭은 09 진입 그대로, 이름 전폭 2줄(CHMO-354) 유지. 커버 오버레이 칩은 기각) — 변경 시 모임 단위 인물(personId) 이름이 전 이벤트로 전파. 공개(14)는 확인 다이얼로그→published→05 배지·뷰어 목록 노출(기존 코드/목 처리) — **미검토가 남으면 [공개하기]가 잠기고 막고 있는 앨범을 이름·장수로 안내한다**(CHMO-488, force 강행·409 자동 재시도 폐기. 재공개 안내(08 "발행 대기 N장"·14 재활성)는 삭제 — 재공개 경로 소멸). 상세는 확정 모델 검수 항목·목은 `db.ts` publish 헬퍼(PUBLISH409는 2026-07-22 실서버 채집·PUBLISH400은 소스 대조). **서비스 워딩 1차 정리(CHMO-348·353·398, 2026-07-22)** — 'AI 분석'→'사진 분류'(06-U CTA `사진 분류하기`·이벤트 배지 `분류중` — BE `progress` 등 내부/BE 도메인 주석의 '분석'은 유지), 14 화면명 '공개 전 검수'→**'공개 요약'**·08 진입 CTA **'요약 보기'**(검토/검수 이중 용어 해소), 08·14 검토 범례는 미검토 먼저. '검토' 용어 자체는 **유지 확정**(CHMO-356 — 코드 무변경). **BE 실연동 기반(CHMO-191) 완료** — 실 BE 응답 봉투 흡수(`src/api/client.ts`)·에러 코드 정규화(`src/api/errors.ts`)·vite 프록시·숫자 ID(int64) 전환. **엔드포인트 계층(CHMO-192) 완료** — 화면은 URL·응답 형태를 모른다: `src/api/{auth,groups,events,albums,viewer}.ts` 도메인 함수가 경로·요청 바디·응답 변환을 소유하고(`apiFetch` 직접 호출은 `src/api/` 안에만), BE 형태 차이(필드명·대문자 enum·특수 앨범 라벨 파생)는 `src/api/mappers.ts`가 흡수한다. **목 이행(CHMO-195) 완료** — MSW 목이 실 BE 계약(봉투·BE 필드명·bare 배열)으로 응답한다: 스위치 양쪽(`VITE_ENABLE_MSW` true/false)이 같은 계약을 탄다. **업로드 3단계(CHMO-194) 완료** — presign(`{files:[{fileName,size}]}` → `[{s3Key,uploadUrl,contentType}]`) → presigned URL로 S3 직접 PUT(`Content-Type`이 서명과 정확히 일치해야 함 — 아니면 403 SignatureDoesNotMatch) → 등록(`POST /events/:id/photos` `{s3Keys, excludeEyesClosed, excludeBlurry}`). **등록이 곧 분석 시작**(BE가 analyzing 전이 + AI 분류 발행)이라 `POST /analyze`는 부르지 않는다(부르면 같은 사진이 두 job으로 발행). 업로드 제약(확장자 jpg/jpeg/png/heic/webp·20MB·50장)은 `src/lib/upload.ts`가 단일 원천으로 갖고 UI·목이 공유한다. **이제 실 BE 연동 단계 — API가 문서와 안 맞으면 BE가 기준**(하드 제약 참조). **학부모 전환 계약층(CHMO-444) 완료 — 목 선행**: 2026-07-17 법률 자문으로 무로그인 뷰어를 폐기하고 학부모도 로그인+모임 합류로 전환(정책 원천 `docs/parent-model-proposal.md`, API 초안 `docs/parent-model-api-draft.md` — §1~7 BE 협의 확정, **실 BE 미구현이라 목으로만 동작**·경로/필드명은 매퍼가 흡수하고 **구계약 공존도 매퍼가 흡수**(invite 평면→teacher·parent null, join 즉시합류→active — 코드리뷰 반영)). 멤버십이 role(teacher/parent)·승인 상태(pending/active)를 갖고(joinKey 2종이 role을 정함·참여=신청 생성·승인제), 목·api 계층에 신설 6종(join-requests 2·members·person-parents 2·parent-photos 2 — `src/mocks/handlers/parents.ts`)+변경 5종(목록 myMembership(PENDING 포함)·상세 카운트 분리+PARENT 필드 생략·이벤트 목록 PARENT published 필터+노출 사진 기준 카운트·join 신청 생성·invite 2종)+`errors.ts` ROLE403(Q5 확정)이 들어갔다. 제작자 전용 핸들러는 전부 TEACHER 관문(`membershipRoleError` 단일 구현 — PARENT는 ROLE403, 비멤버·PENDING은 404 은닉) 경유. 시드에 학부모 계정 4(민준아빠 1111 = 연결됨+별님반 대기·서연맘 2222·지호네 3333 = 미연결·치즈냥이88 4444 = 승인 대기 신청). **학부모 신청 실 UI(CHMO-445) 완료**: 02-2 참여 신청 3단계(`ParentJoinPage` — 1/3 모임 비밀번호 → 2/3 자녀 이름(복수·빈 칸 필터 + 행 ✕ 삭제·마지막 1행 유지, CHMO-448 추가) → 3/3 동의(문구 확정 전 자리라 consents 미전송, §8) → 신청(PENDING) → 홈 랜딩. **참여 성공 랜딩은 02-1 모달 포함 status 무관 홈**(CHMO-448 수정 — 구계약(active) 응답이어도 상세 직행하지 않는다: 승인제 과도기 BE에서 상세 직행이 "권한이 없는 스페이스" 오류로 실측)) + 홈 카드 myMembership 소비(PENDING=뮤트+'승인 대기중' 배지+'신청: {자녀}'+탭 토스트 — 모임 API 추가 호출 없음(§7-2) · ACTIVE PARENT='학부모 · 참여 중'). 학부모 링크 분기는 FE 파생 `?role=parent` 마커가 유일한 근거(joinKey는 불투명 — invite parent 채널 joinUrl이 싣는다), 마커 없는 학부모 코드는 02-1 모달이 서버 400(자녀 이름 필요)으로 감지해 02-2로 인계(비밀번호 프리필). 1/3 [다음]은 childNames 없는 프로브 제출 — 비밀번호 오류(JOIN403)·중복(409)은 그 자리에서 잡고 400이면 2/3 진행, 실 BE 검증 순서가 달라도 최종 제출의 WRONG_PASSWORD를 1/3로 복귀시키는 안전망(목 검증 완료 — 실 BE 실검증은 CHMO-449). **05 초대 UX 개편(CHMO-446) 완료**: 통합 초대 바텀시트(05-2, `GroupInviteSheet` — 기존 InviteSheet·ParentShareSheet 2개 대체, 선생님/학부모님 세그먼트 탭·탭별 joinKey+비밀번호(학부모는 sharePassword 재사용, Q2)·링크 복사·카카오톡 공유(기존 shareOrCopy)·학부모 탭 승인/노출 안내. parent 채널 null(구계약 실 BE)이면 탭 숨기고 선생님만) + 05 헤더 [+ 초대하기] 단일 진입(하단 학부모 공유 버튼 제거)·카운트 분리 표기 "선생님 N · 학부모 N · 이벤트 N개"(§7-3 — 분리 필드 없으면 '인원 N명' 폴백)·하단 [초대 관리{ · 신청 N}] 버튼(대기 신청 수 = listJoinRequests, 실패 시 뱃지만 생략). **초대 관리 실 UI(CHMO-447) 완료**: 20 초대 관리 화면(`/groups/:id/invites`, `InviteManagePage` — 선생님/학부모님 세그먼트 탭(기본 학부모님 — 주 업무인 신청 승인·아이 연결이 학부모 쪽)·대기 신청 카드(닉네임·"{자녀} 학부모입니다" 원문·상대 시각(`src/lib/timeAgo.ts` formatTimeAgo 단일 원천)·[승인]/[거절] — 승인은 멤버 확정만(§1 승인·매핑 분리)·거절은 확인 다이얼로그)·멤버 명단(선생님 탭 "(나)" 표기 / 학부모 탭 연결 칩 ✕ 해제(확인 다이얼로그)+[+ 연결]·미연결은 "신청: {이름}" 원문 보존(§2)+[연결]) — 신청 목록 조회 실패는 명단을 가리지 않고 인라인 재시도로만 알린다) + 20-1 아이 연결 바텀시트(`ChildLinkSheet` — 인물 앨범 가로 스크롤 타일 **다중 선택 토글**, 매핑 API가 1건 단위라 선택분 순차 POST·중간 실패 시 성공분은 그대로 반영(닫기+refetch)하고 실패만 토스트(§4 다대다라 부분 성공이 반쪽 상태가 아님)·이미 연결된 인물은 '연결됨' 비활성 타일로 중복 409 예방·열릴 때만 마운트). 인물 후보는 그룹 단위 인물 엔드포인트가 계약 초안에 없어 이벤트→앨범 목록에서 FE 파생(`listGroupPersons` — personId로 합산·정렬은 albumSort 재사용, 전용 엔드포인트는 BE 후속 논의). **학부모 조회 실 UI(CHMO-448) 완료**: 홈 ACTIVE PARENT 카드 탭 → 18 학부모 모임 상세(`/parent/groups/:id`, `ParentGroupPage` — 모임명 + "학부모 · {연결 자녀명}" 헤더, published + **아이(매핑 인물) 등장 이벤트만**(서버 필터 — Q4를 좁힌 **노출 강화, ⚠ BE 미협의 제안**(2026-07-27 사용자 확정): 매핑은 인물 앨범 단위 권한이라 아이가 안 나온 이벤트는 공통이 있어도 통째 은닉·미연결은 0개, parent-photos 딥링크·홈 카드 eventCount도 같은 판정 — 목·초안 §1·§5 반영), 카드는 와이어프레임(307:26) 그대로 텍스트만(커버·상태 배지 없음 — 전부 공개된 이벤트라 배지 무의미). 멤버십·모임명은 **모임 목록(listGroups)에서 읽는다** — 상세 응답엔 myMembership이 없다. 딥링크 안전망: TEACHER·구계약(멤버십 없음)은 05로, PENDING은 홈으로 replace) → 19 학부모 사진 그리드(`/parent/groups/:id/events/:eventId`, `ParentEventPhotosPage` — parent-photos 플랫 그리드 + PhotoLightbox 재사용(개별 저장) + 선택모드(헤더 [선택]·롱프레스 진입 — 09와 같은 CHMO-243 관용, CHMO-473 추가)로 골라 저장 + 하단 고정 [↓ 전체 저장]/선택 [N장 저장](CHMO-473 저장 파이프라인 — zip 폐지), 서브타이틀 "{자녀명} · 공통 포함 N장"(이름 도착 전엔 "사진 N장" 중립 표기), 빈 상태 문구는 18·19 모두 연결 여부로 분기 — 미연결(승인 후 기본 경로)은 "선생님이 아이를 연결하면" 안내). 자녀 이름 원천은 **`myMembership.linkedChildNames`(초안 §1 확장 — CHMO-448 FE 추가 제안, BE 미협의)**: 매핑 조회(`/members`)가 TEACHER 전용이라 학부모 본인이 연결 인물명을 알 방법이 없어 목록 응답에 실었다(목은 §4 매핑에서 파생·매퍼는 생략을 빈 배열 정규화, 미연결 헤더는 "학부모"만). 이벤트 카드 날짜 표기는 `src/lib/eventDate.ts`로 승격(05·15-L 중복 정의 제거 — 18 포함 3화면 공유). 남은 실 화면은 CHMO-449(뷰어 폐기·실연동, BE 배포 후)로 이어진다. 기존 뷰어(/share) 계열은 이관 완료까지 유지. **사진 저장 앨범 직행(CHMO-473) 완료 — ZIP 전면 폐지**: 모든 사진 저장(라이트박스 개별·09 선택/전체·16 [다운로드]·19 [전체 저장])이 `src/lib/photoSave.ts`+`usePhotoSave` 파이프라인 하나를 탄다. iOS(iPadOS 포함 판별+`canShare({files})`)는 원본 선요청(동시 3장·`no-store` — CHMO-326 캐시 함정 동일) 후 공유 시트 `navigator.share({files})`의 시스템 '이미지 저장'으로 사진 앱 직행 — Safari transient activation이 엄격해(탭과 같은 틱 수준) 2단계 UX: [저장] 탭→준비 진행률→준비 직후 자동 share 1회 시도(제스처 생존 시 탭 절약)→만료(NotAllowedError)면 [사진 앱에 저장] 재탭. 배치 20장(`SHARE_BATCH_MAX`) — **준비도 배치 단위**라 메모리 피크는 배치 1~2개(총량 아님)·ready 동안 다음 배치 프리페치로 대기 상쇄·run 세대 가드로 reset 후 늦은 완료의 오염 차단, 배치마다 시트 1회(버튼 'k/n' 표기), 시트 취소(AbortError)는 같은 배치 대기·무토스트, **수동 탭 연속 2회 거부(인앱 등 공유 차단 환경)면 파일 다운로드 폴백**(준비분 재사용·미준비 배치는 그때 받아 흘림, iOS 파일 앱행이라 도착지 토스트 안내 — 인앱에서 다운로드 자체가 되는지는 실기기 검증 항목). Android는 공유 시트에 시스템 '갤러리 저장'이 없어 장별 blob 다운로드(Download 폴더 → 미디어 색인으로 갤러리 자동 노출, 시작 전 '다운로드 허용' 안내 토스트)·데스크탑/미지원도 동일 폴백. 부분 실패는 성공분 진행+실패 수 토스트. BE ZIP 엔드포인트는 존치하되 FE 미호출(zip api 함수 3종은 계약 테스트 고정용 잔존). ⚠ 실기기 검증 필수(iOS 시트 '이미지 저장' 노출 — iOS 16 누락 버그 이력 · Android 갤러리 색인). **약관·개인정보 문서 3종 + 화면 고지(CHMO-478) 완료**: 전문 3종(이용약관·개인정보 처리방침·얼굴 특징정보 처리 안내)은 `docs/legal/*.md`가 정본(변호사 검토용 — `[[결정필요]]`/`[[검토필요]]` 마커가 미확정 지점)이고 `src/legal/*.ts`가 앱 적용판(마커 자리 중립 치환 — **사실과 다른 고지 금지**: 임베딩 암호화 문구·국외 이전·보호책임자는 확정 전이라 미노출) — 둘은 반드시 함께 수정. `/legal/{terms,privacy,biometric}` 라우트(공용 렌더러 LegalDocPage + Header `onBack` 신설(진입점 복수라 절대경로 backTo 불가 — 히스토리 복귀+직진입 랜딩 폴백), **가드 밖** = 스토어 심사용 공개 URL 겸용(CHMO-321 전제))·설정 약관 링크 3종·06-U 얼굴 인식 고지 배너·02-2 이름 사용 목적 안내·19 재배포 금지 안내·03 보호자 동의 확인 게이트(체크 전 CTA 비활성). 이용약관은 무효 조항 회피 4원칙(고의·중과실 유보·유출 무과실 면책 금지·단계적 자격상실+30일 소명·키즈노트식 초상권 갈음 조항 미이식) 반영. 가입 동의 체크박스(§1) 실연동은 BE 동의 기록(카탈로그·버전·서버 시각 append-only) 계약 확정 후속. **첫 사용 온보딩 화면 설계(CHMO-480) 완료 — 설계만, 구현은 CHMO-481**: 로그인 직후 1회 노출할 슬라이드 3장(화면 번호 `00`)을 피그마 와이어프레임(`348:4`·`349:4`·`349:19` — 찐와프 페이지)과 `docs/screen-spec.md` §3 카드로 확정했다. **역할로 나누지 않는다**(2026-07-28 사용자 지적 반영) — 계정에는 role이 없고 role은 멤버십(모임별) 속성이라(joinKey 2종이 role을 정함) 같은 사용자가 모임마다 선생님·학부모로 갈리고 가입 시점엔 어느 쪽인지 알 수 없다. 그래서 공통 1종이고 마지막 장이 진입점 둘([모임 만들기]→03 / [초대 코드로 참여]→02-1)로 갈라주는 역할만 한다. 문구는 분류→검토→**초대 1회** 3단(워딩 규칙 준수 — 'AI 분석'이 아니라 '사진 분류'). 3장 초안 "링크 하나로 학부모에게 전달해요"는 **사진 올릴 때마다 링크를 보내는 것처럼 읽혀 폐기**하고 "한 번 초대하면 / 공개할 때마다 바로 봐요"로 잡았다(참여 링크는 합류 초대용 1회고, 이후엔 공개만 하면 합류·연결된 학부모 화면에 뜬다). 비주얼은 신규 일러스트 없이 기존 토큰·형태 재사용(치즈 심볼·`photo` 플레이스홀더·인물 원형 타일). 노출 판정은 BE에 신규 가입 신호가 없어(`AuthResponse` 평면 필드·소셜 exchange도 신규/기존 무구분) FE 로컬 플래그로 가고, 초대 링크 `returnTo`가 있으면 온보딩을 건너뛴다(참여 흐름을 끊지 않게). 계정 단위 서버 저장은 BE 협의 후속(screen-spec §5-6). **선생님 합류 승인제 대응(CHMO-502) 완료**: BE CHMO-475로 선생님 키 합류도 PENDING이 되면서(확정 모델 참조) 화면 4곳을 맞췄다 — ① 02-1 모달 문구를 '참여'→**'참여 신청'**(제목·CTA·승인 안내 한 줄. 랜딩은 종전대로 status 무관 홈) ② 홈 대기 카드가 **카운트 줄로 폴백하지 않는다**(실 BE는 대기 항목에 `memberCount` 생략 + `eventCount` 0을 준다 — 선생님 신청은 자녀 이름도 없어 그대로 두면 "이벤트 0"이 빈 모임처럼 읽힌다. 서브텍스트는 원문 없으면 '선생님으로 참여 신청'/'학부모로 참여 신청') ③ 20 초대 관리 **탭별 대기 수 뱃지**(기본 탭이 학부모님이라 선생님 신청이 묻힌다 — 목록엔 role=TEACHER·childNames 빈 배열로 섞여 온다) ④ `errors.ts`에 `SPACE403`→`PENDING_APPROVAL` 등록 + ErrorState가 **403도 영구 실패로 취급**(재시도 대신 복귀 CTA — 승인 전 딥링크가 막다른 화면이 되지 않게). 목은 이미 role 무관 pending이라 계약 변경은 대기 항목 `eventCount: 0`뿐이고, 시드에 선생님 대기 신청(신입쌤 5555)을 넣어 20 선생님 탭을 볼 수 있게 했다. 화면·기능 결정의 소스 오브 트루스는 여전히 `docs/`. **2026-07-28 정책 변경(업로드 1회·전량 검토 공개 게이트)은 문서 3종에 반영 완료(CHMO-489)** — feature-spec §4 상태머신은 단방향(재분석 회귀·published 증분 폐지)·F5.5 사진 추가 삭제·F5.4에 검토 비가역 명시, api-spec §3.4는 CHMO-194 이전 파이프라인(Lambda 자동 등록·`photoId` 선발급·별도 `POST /analyze`·고아 사진 경고)을 걷어내고 현행 3단계(presign→S3 PUT→등록=분석 시작)로 교체(미등록 S3 잔존물은 BE CHMO-484 참조), screen-spec은 08 `[+ 사진 추가]`·IA 분기 삭제. ⚠ **2회차 등록 거부의 에러 코드는 아직 미확정**(BE CHMO-485 AC-4) — 목은 임시로 `VALID400`을 쓴다. **치즈모아 둘러보기(CHMO-504) 완료 — 화면 `00-T`, `src/components/AppTour.tsx`**: `모임 > 이벤트 > 앨범` 3단 **정보구조**를 눌러서 체감시키는 전체 화면 투어. 축소된 앱 화면을 직접 그리고 탭하면 다음 계층으로 넘어가는 방식이라 **서버 호출·라우팅 이동이 없고 모임이 0개인 신규 가입자에게도 돌아간다**(실제 화면 코치마크는 가리킬 대상이 없어 기각, 샘플 모임 자동 생성은 BE 작업+삭제 책임까지 필요해 기각). 무대는 실물 카드 컴포넌트(`GroupCard`·`EventCard`·`AlbumCard`·`PhotoGrid`)로 조립해 화면 디자인이 바뀌면 투어도 따라 바뀐다. **역할로 갈라 보여준다**(2026-07-28 사용자 지적) — 계정엔 role이 없지만 **구조는 역할마다 실제로 다르다**: 선생님은 모임→이벤트→앨범 3단, 학부모는 **앨범 단계가 없다**(18·19 — 아이 사진이 한 화면에 펼쳐진다). 선생님 흐름만 보여주면 학부모로 합류한 사람은 자기 화면을 못 보므로 첫 장에서 갈래를 고르고 마지막 장에서 반대편으로 건너뛴다(고른 갈래는 계정에 남기지 않는다 — role은 멤버십(모임별) 속성). **초안(310px `Modal` 안 미니 화면)은 "촌스럽다"는 피드백으로 폐기**(2026-07-28) — 낡아 보인 원인 셋을 걷어냈다: ① 액자(가짜 `‹ 홈` 상단 바) 제거, 전체 화면 여백 위에 실물 카드만 띄우고 어느 화면인지는 실제 화면의 제목·서브텍스트로 알린다 ② 카드 전체를 감싼 점멸 링(핫스팟 — 가장 낡은 튜토리얼 패턴)을 카드 모서리 작은 탭 물결 하나로 축소 ③ 일곱 겹(제목·설명·`1/3` 카운터·링크·버튼 2개…)을 **무대/문구/액션 1개** 세 겹으로 줄이고 진행은 상단 세그먼트 바 3칸이 맡는다(한 화면 한 가지). 전환 애니메이션 `step-in`·탭 물결 `tap-ripple`은 tailwind.config keyframes. **노출은 첫 로그인 1회 자동**(홈 위에서 연다 — `postLoginDestination`은 언제나 홈이고 별도 화면으로 튕기지 않는다. 끝까지 보든 ✕로 나가든 '봤다'로 기록, 초대 링크로 02-1이 열려 있으면 미룬다) + 설정 '치즈모아 둘러보기' + 홈 빈 상태. ⚠ **첫 사용 온보딩 슬라이드(00)는 일단 노출하지 않는다**(같은 결정) — 로그인 후 목적지·설정 링크 등 진입점을 모두 내렸고 화면·라우트(`/onboarding`)는 되살릴 수 있게 남겼다. 노출 판정 플래그(`lib/onboarding.ts`)는 **둘이 공유한다**(따로 세면 슬라이드를 되살릴 때 이미 본 사람에게 다시 뜬다).

## 소스 오브 트루스 (작업 전 관련 문서를 읽는다)

화면·기능·데이터 결정은 모두 여기서 나온다.
- `docs/feature-spec.md` — 기능 명세(도메인 모델, 상태머신, 기능 F1~F7, 추적표)
- `docs/screen-spec.md` — 화면 명세(IA, 화면별 카드, 디자인 토큰, 26개 와이어프레임 매핑)
- `docs/api-spec.md` — API 계약(리소스 스키마, 엔드포인트 req/res, 화면 매핑). **단, 실연동 단계에선 실 BE 스웨거가 우선**(`https://api.cheese-moa.com/v3/api-docs`) — 문서와 다르면 FE를 BE에 맞추고, 문서 갱신은 CHMO-196에서 한다
- `docs/mvp-explanation.md` — 서비스 배경/취지
- `docs/parent-model-proposal.md` — **학부모 접근 모델 전환 정책 원천**(법률 자문 후속 — 무로그인 뷰어 폐기, 멤버십 role·승인제·인물 매핑. Q1~Q6·§7 전부 BE 협의 확정) · `docs/parent-model-api-draft.md` — 그 API 계약 초안(**목 선행 구현의 기준** — 실 BE 배포 후 실채집으로 재확인, CHMO-444~449)
- `docs/legal/` — **약관·개인정보 처리방침·얼굴 특징정보 안내 정본**(CHMO-478, 변호사 검토 전 초안): `[[결정필요]]`(팀 결정)·`[[검토필요]]`(변호사 확정) 마커가 미확정 지점. 근거 리서치는 노션(약관·처리방침 필수 내용 정리 / 이용약관 작성 리서치 / 사진 보안 리서치 / AWS Rekognition Opt-Out). 앱 적용판 `src/legal/*.ts`와 반드시 함께 수정
- `docs/admin-spec.md` — **관리자(어드민) 페이지 명세**(CHMO-375 확정): 내부 운영자용 백오피스. 1차는 조회 전용(대시보드·모임 목록·모임 상세) · 같은 리포 `/admin` 라우트(+lazy) · 유저 role 인증(`/admin/*`에서만 검사, 기존 서비스 API는 role을 안 본다) · 인프라 지표는 Grafana 담당이라 제외. 나중에 별도 리포로 격리하기 위한 규칙 3개(관리자 코드는 `src/admin/`에만·공유는 바닥 인프라뿐·BE 경로 `/admin/*` 분리)를 구현 시 지킨다. **화면 설계(CHMO-376) 완료 — §3-0**: 피그마 페이지 `어드민 (Admin)`(`287:3`, 모바일 와이어프레임 페이지와 분리)에 A1 대시보드(`289:3`)·A2 모임 목록(`289:45`)·A3 모임 상세(`289:87`) + 공용 컴포넌트(사이드바·지표 카드·상태 배지·로고 심볼). 데스크탑 1440×1024(사이드바 240 + 상단바 56 + 콘텐츠), 팔레트는 **중립 그레이 + 치즈 옐로우 포인트**(토큰은 `치즈모아 토큰` 컬렉션의 `admin/` 그룹 — 모바일 cream/brown을 쓰지 않는다)
- `docs/design/screen-system.dc.html` — **디자인 시안(공용 컴포넌트 스펙)**: Header/Button/Badge/Toggle/카드류/PhotoTile/오버레이/Toast의 색·크기·규칙. 토큰 원천은 `tailwind.config.js`(충돌 시 코드가 이김) · `docs/design/logo-v3.dc.html` — 로고 시안(체다 심볼)

## 확정 MVP 모델 (코드로 추론 불가 — 반드시 준수)

- 정보구조: 모임(Group) > 이벤트(Event) > 앨범(Album). 앨범 `type`: `person`/`common`/`uncertain`/`eyes_closed`/`blurry`.
- 제작자 인증: **소셜 로그인 4종(카카오·구글·네이버·애플)으로 완전 대체 확정**(CHMO-359, 2026-07-20 — 미배포·사용자 0명이라 마이그레이션·계정 연결 없음). 닉네임+4자리 PIN은 BE 엔드포인트 제거 전까지만 병행. **소셜 표시명은 사람 이름 기반**(2026-07-25 확정 — 네이버 검수 반려(이름 활용처 증빙)가 계기): 구글(`name`)·애플(성+이름)은 기구현, 네이버는 별명→이름 전환(BE CHMO-439 — **실서버 배포 후에** 네이버 재검수 신청, 배포 전 신청 금지), 카카오는 실명 스코프가 비즈니스 앱 검수 필요라 카톡 닉네임 유지. 설정 화면 라벨도 '이름'(CHMO-440 — 데이터 필드는 BE 계약대로 `nickname`, 레거시 PIN 로그인·가입 화면은 '닉네임' 유지). **권한 등급 없음** — 로그인한 모임 멤버는 전원 동일(업로드·검수·공개 가능).
- 합류는 **role 무관 승인제**(CHMO-475, 2026-07-28 BE 배포): joinKey는 "누가 신청할 수 있나(=어떤 role로)"만 정하고, "누가 들어오나"는 그 모임의 ACTIVE 선생님이 승인으로 정한다 — 선생님 키 합류도 즉시 ACTIVE가 아니라 PENDING 신청이다. **모임 생성자만 예외**로 즉시 TEACHER/ACTIVE. 승인 전 모임 API(상세·이벤트·앨범)는 전부 `SPACE403`(FE `PENDING_APPROVAL`). 신청 목록엔 선생님·학부모가 섞여 오고(role로 구분·선생님은 childNames 빈 배열), 승인/거절은 기존 API 그대로(`PATCH /join-requests/:id` — 거절은 행 삭제라 재신청 가능).
- 모임 진입점 2개(분리): ① **선생님 초대** = 제작자 합류(모임 단위, 모임 비밀번호+참여 링크) ② **학부모 공개** = 무로그인 뷰어(**모임 단위** 공유 링크 + **학부모 전용 비밀번호**(모임 비밀번호와 별개, 모임 생성 시 자동), 조회·다운로드만). 비번 입력 후 **공개된 이벤트 목록**에서 선택.
- 공개(publish) 단위: **이벤트별**(검수 → 공개 요약(14) → 공개하기). 학부모 접근(공유) 단위: **모임별**(공개된 이벤트만 목록 노출). 상세 [[viewer-share-group-level]].
- 검수: **사진 단위 `reviewed`**(앨범 [검토 완료] = 일괄 처리, 앨범 자체엔 검토 상태 없음 — 실 BE는 CHMO-395로 매핑(AlbumPhoto) 단위, 목은 사진 단위 근사). 뷰어 노출 = 사진 `reviewed && published`(서버 필터링, CHMO-324). · **공개 게이트 = 전량 검토 완료(하드)**(CHMO-487·488, 2026-07-28 정책 변경 — **재공개 게이트 CHMO-324·265 폐지·`?force=true` 폐기**): 인물·공통 앨범에 미검토가 1장이라도 남으면 공개할 수 없다(서버는 항상 `PUBLISH409`, 14는 [공개하기]를 잠그고 막고 있는 앨범을 이름·남은 장수로 안내). 판정 범위가 인물·공통뿐인 이유는 특수 앨범엔 검토 UI가 없어(CHMO-357) 포함하면 영영 공개 불가한 이벤트가 생기기 때문 — 목 게이트도 여기에 맞췄다(`unreviewedGatePhotoCount`, 종전 목은 이벤트 전 사진을 세서 force가 그 차이를 삼키고 있었다). **검토 완료는 비가역**(해제 폐지 — 되돌리려면 삭제뿐)이라 09 [검토 완료]에 확인 다이얼로그를 되살렸다(CHMO-413 반전). `pendingPublishCount`는 화면이 더는 읽지 않는다(BE가 걷어낼 때까지 매퍼만 통과 — 값은 구조적으로 항상 0) · **공개 후 사진 추가·재분석은 폐지**(CHMO-486 — 업로드·분류는 **이벤트당 1회**). 삭제·이동은 그대로 가능하고 옮긴 사진은 `reviewed`·`published`를 유지해 학부모 화면에 **즉시** 반영되므로, published 이벤트의 09-1 이동은 확인 다이얼로그를 거친다(CHMO-488 — 미공개는 종전대로 즉시).
- 타깃: 기관(B2B/유치원) 우선. 일반(B2C) 전용 화면은 후순위.

## 하드 제약

- **BE/AI 코드는 건드리지 않는다.** 이번 범위는 프론트엔드뿐.
- **API 계약 기준은 실 BE다**(오리진 `https://api.cheese-moa.com` — Cloudflare 경유 HTTPS(CHMO-260), EC2 직통 IP는 보안그룹으로 차단, 스웨거 `/v3/api-docs`) — `docs/api-spec.md`와 안 맞는 부분은 **FE를 BE에 맞춘다**. BE 규약: 성공/실패 모두 `{isSuccess, code, message, result}` 봉투 · 숫자 ID(int64) · 경로에 `/api/v1` 프리픽스 없음(vite 프록시가 rewrite) · 시각이 오프셋 없는 UTC(CHMO-205 전까지 FE 보정). 봉투·에러 코드·시각 차이는 `src/api/client.ts`·`src/api/errors.ts`가, 리소스 필드명(`groupId`≠`id`·`eventDate`·`thumbnailUrl`·`personName`)·대문자 enum·서버가 주지 않는 파생값(특수 앨범 표시명·`visibleToViewer`·원본 `url`)은 `src/api/mappers.ts`+도메인 모듈이 흡수한다(CHMO-192) — 화면 코드는 봉투도 BE 필드명도 모른다. 매퍼의 절반은 단순 개명이라 **CHMO-201(BE DTO 필드명 정렬) 확정 후 걷어낼 수 있다** — 그 전엔 움직이는 과녁이라 유지한다. **리프레시 토큰(CHMO-193) 완료** — accessToken(만료 1시간) 401 시 `src/api/client.ts`가 refreshToken으로 1회 자동 재발급(회전 저장·in-flight promise 공유) 후 원 요청 재시도, 실패(TOKEN401) 시 두 토큰 삭제·로그인 복귀. 로그아웃은 `POST /auth/logout`(auth 미첨부, body `{refreshToken}`)으로 서버 무효화 후 로컬 삭제(`src/lib/auth.ts`가 두 토큰 저장 소유). **계약 테스트(CHMO-219) 완료** — 실 BE 응답 픽스처로 봉투·시각 보정·401 재발급·매퍼를 고정했다(`npm run test`). **에러 코드는 실서버에서 채집된 것만 목이 흉내 낸다**(COMMON401·AUTH400/401·TOKEN401·JOIN403·SPACE404·MOMENT404·ALBUM404·PHOTO400/404·VALID400·PUBLISH409 — SPACE404 = 모임 404, 2026-07-16 채집, CHMO-285 · PUBLISH409 = 미검토 공개, 2026-07-22 채집, CHMO-265. PUBLISH400은 BE 소스 대조만 · **SPACE403 = 승인 전 모임 접근, 2026-07-28 BE 완료 공지(CHMO-475 AC2 — 실서버 채집은 학부모 전환 배포 후 CHMO-449)**). 미채집 케이스(닉네임 중복·이미 멤버)는 FE 의미 코드를 그대로 두고 `// BE 코드 미확인` 주석을 단다 — `errors.ts`가 미지 코드를 통과시키므로 화면은 동작하고, 추측한 코드를 BE 진실 테이블에 굳히지 않는다. 남은 몫: CHMO-196(문서 갱신 — 목이 구현한 계약이 곧 갱신본).
- **BE 결함 4건은 2026-07-10 전부 완료 — 이 제약은 해제됐다**(CHMO-194 착수 중 발견해 티켓을 냈던 것들): 사진 추가 시 기존 사진·앨범·검수 상태 삭제(CHMO-215 — 증분 재분석으로 전환) · 공개 이벤트에 사진 추가 시 `published` 해제(CHMO-216) · 요청당 50장 상한이라 200~300장 경로 없음(CHMO-217) · 분석 실패가 `EMPTY`로 회귀해 미시작과 구분 불가(CHMO-218). 다만 **2026-07-28 정책 변경으로 사진 추가·재분석 경로 자체가 폐지됐다**(CHMO-486 — 업로드·분류는 이벤트당 1회. 08 [+ 사진 추가] 제거·06-U는 `photoCount > 0`이면 진입 차단·목은 presign·등록 양쪽에 게이트. 서버 게이트는 CHMO-485). 단 주의 두 가지: ① Jira '완료'일 뿐 **실 BE 배포·동작은 FE가 검증하지 않았다**, 이 경로를 건드리는 스토리는 실 BE로 먼저 확인한다. ② **업로드 상한은 BE 계약과 화면이 서로 다른 값이다**(CHMO-497) — BE는 요청당 **500장**(2026-07-28 실측: presign·등록 501개 → `VALID400 "한 번에 500장까지 업로드할 수 있습니다."` — CHMO-482 배포 확인. 그 전 상한 50은 2026-07-24 실측분), 웹 화면은 **100장**에서 캡한다. 상수도 갈라 뒀다(`src/lib/upload.ts`): `MAX_UPLOAD_BATCH = 500`은 **BE가 받아 주는 수**(목·계약 테스트가 쓴다), `MAX_UPLOAD_PICK = 100`은 **웹이 스스로 거는 수**(06-U만 쓴다 — 브라우저는 고른 파일을 전부 디코드해 미리보기를 만들어야 해서 장수가 곧 CPU·메모리 부담이고, 500장 규모는 앱의 네이티브 업로드가 맡는다). BE 상한이 더 올라가도 웹은 100을 지키고, 반대로 `MAX_UPLOAD_PICK > MAX_UPLOAD_BATCH`가 되면 BE가 배치 전체를 400으로 거절한다(`upload.test.ts`가 이 부등식을 고정). 06-U는 선택 시점 100장 캡으로 대응한다(CHMO-397). 등록이 곧 분석 시작(analyzing 중 등록 400)이라 분할 업로드는 불가하고, **1회 정책이라 이어 올리기도 없다**(CHMO-430은 철회된 선행 티켓). ⚠ 목은 크기 검증을 1회 게이트·analyzing 체크 **뒤에** 두는데 실 BE는 `@Size`가 **먼저**다(501장이 SPACE403 대신 VALID400) — 화면이 100에서 캡해 도달 불가라 방치 중.
- FE 개발 기본은 여전히 **MSW 목**(`VITE_ENABLE_MSW=true`) — 실 BE 확인 시 `false`로 끄면 프록시 경유. 실 BE 테스트 계정: `FE연동테스트`/PIN `0709`.
- MVP 제외: 결제/요금제, 다운로드 한도, 휴지통, 인물 병합, 학부모 로그인.

## 기술 스택

React 18 + Vite 5 + TypeScript(모바일웹, 기준 프레임 390×844) · 라우팅 react-router v6 · 스타일링 **Tailwind CSS v3**(토큰은 `tailwind.config.js` + `src/index.css`) · 데이터패칭 커스텀 `useApi` 훅 · API 목 레이어 **MSW**(`VITE_ENABLE_MSW=true`일 때만 활성 — 전 엔드포인트를 **실 BE 계약대로** 구현, CHMO-195). MSW를 끄면 `/api/v1` 요청이 vite dev 프록시로 실 BE에 전달된다(`VITE_API_ORIGIN`, 기본 `https://api.cheese-moa.com` — CHMO-191·260). 테스트는 **Vitest**(`vitest.config.ts`, node 환경 — jsdom·RTL 없음): 범위는 `src/api/`·`src/lib/` **계약 테스트**와 `src/mocks/serializers.test.ts` **이음매 테스트**뿐이고 화면 테스트는 의도적으로 제외한다(CHMO-219·195).

### 코드 구조 (`src/`)
- `main.tsx` 진입(+MSW 부트스트랩) · `router.tsx` 전 라우트 정의 · `index.css` Tailwind+디자인 토큰.
- `components/` PhoneShell(390×844 프레임 — **뷰포트 고정 `h-dvh`**: 문서 스크롤이 없고 스크롤은 화면별 콘텐츠 영역(`overflow-y-auto`)이 소유, 하단 고정 요소는 safe-area 합성 간격 `pb-safe-6/7/9`(tailwind.config spacing), 중앙 정렬형은 `justify-center` 대신 `my-auto`(넘칠 때 위 잘림 방지), CHMO-396 · sm+ 코너 클리핑은 `overflow-clip`: hidden이면 sticky가 프레임에 갇힌다, CHMO-369)·BrandHero·AuthCredentialsForm(인증 화면 공용, returnTo 복귀 지원)·SocialLoginButtons(소셜 로그인 버튼 4종 — 01·01-1·01-2 공용, 브랜드 고정색, CHMO-359)·JoinGroupModal(02-1 참여 모달 — 홈·초대 링크 공용)·GroupShareSheets(InviteSheet·ParentShareSheet — 05 위 초대/학부모 공유 바텀시트, CHMO-112)·PhotoLightbox(사진 크게 보기 공용 라이트박스 — 09 검수·16 뷰어, iOS 사진 앱풍 라이트 크롬(흰 배경·상단 ✕/카운터·하단 아이콘 툴바)·좌우 스와이프·개별 저장 + info/actions 슬롯, CHMO-242) · `components/ui/` **공용 컴포넌트 세트**(CHMO-107: Header·Button(+ButtonLink)·Badge·Toggle·EmptyState·카드류·PhotoTile/Grid·Modal·BottomSheet(끌어내려 닫기 제스처 — 포인터 이벤트라 터치·마우스 공용, 수직/수평 축 판별로 내부 가로 스크롤과 공존·`touch-action: pan-x`로 터치 네이티브 팬 보존, CHMO-345)·ConfirmDialog·Toast + TextField(CHMO-110) + ErrorState·PinField(CHMO-118: ErrorState = useApi 실패 공용, 401 `unauthorizedTo` 복귀·404 `notFoundTo` 돌아가기 CTA / PinField = 4자리 PIN 입력 공용) + LoadState(CHMO-181 — useApi 로딩/에러 폴백 공용: loading이면 로딩 문구, error면 ErrorState, 둘 다 아니면 null) + icons(SF Symbols풍 라인 아이콘 IconDownload·IconTrash·IconFolderMove·IconClose — currentColor, 라이트박스·09 선택모드·16 다운로드 버튼 공용, CHMO-242) — 데모 `/dev/components`, DEV 전용) · `guards/` 제작자/뷰어 라우트 가드 · `pages/`(+`pages/viewer/`) 화면 — 인증 01·01-1·01-2(CHMO-110)·01-C 소셜 콜백(SocialCallbackPage — `/auth/callback` 코드 교환, 가드 밖, CHMO-359)·모임 진입 02·03·02-1(CHMO-111)·모임 허브 05(CHMO-112, 초대는 시트라 별도 페이지 없음)·20 초대 관리(InviteManagePage + 20-1 ChildLinkSheet, CHMO-447)·18/19 학부모 조회(ParentGroupPage·ParentEventPhotosPage — `/parent/groups/…`, CHMO-448)·이벤트 06-M(05 위 모달)·06-E/분석중(EventDetailPage 상태 분기)·06-U(PhotoUploadPage, CHMO-113 — 카운트·토글은 일반 흐름, CTA는 스크롤 밖 하단 고정: CHMO-369의 화면 내 sticky는 실기기 WebKit에서 상·하단 모두 바가 떠 사진이 비쳐 철회(CHMO-424)·미리보기 축소 썸네일(원본 업로드는 그대로, CHMO-369)·업로드 100장 상한 UX(CHMO-397·497 — "한 번에 최대 100장" 사전 고지 상시 노출, 파일 선택 시 남은 슬롯까지만 선택 상태로 담고 초과분은 해제 상태+토스트, 캡 도달 후 신규 선택 탭은 토스트로 차단, 미선택 잔여분은 **올라가지 않는다는 사실을 그대로 고지**(CHMO-486 — 1회 정책이라 "다음에 이어 올리세요"는 거짓 안내) — 기존 overBatchLimit 경고·CTA 비활성은 회귀 방어선으로만 잔존. 상한 원천은 `MAX_UPLOAD_PICK`(화면 상수)이고 BE 계약 상한 `MAX_UPLOAD_BATCH`(500)와 별개다)·미리보기는 **원본 장당 1회 디코드**(CHMO-497 — 타일에 원본 objectURL을 꽂지 않고 썸네일이 올 때까지 PhotoTile 플레이스홀더, 준비 소요는 DEV에서만 화면에 표기)·등록 응답 유실 안전망(등록 단계 실패 시 상세를 되물어 실제로 시작됐으면 에러 대신 인계, CHMO-486))·08 앨범 그리드(EventDetailPage 안 EventAlbumGrid + ⚙ 이벤트 설정 = RenameModal(이름 수정)+이벤트 삭제, CHMO-114·278)·09 앨범 상세(AlbumDetailPage 선택모드·삭제, CHMO-114)·설정(SettingsPage — GET/PATCH /me + 로그아웃, CHMO-118 + 약관·정책 링크 3종, CHMO-478)·약관 전문(LegalDocPage — `/legal/{terms,privacy,biometric}` 가드 밖 공용 렌더러, 문서 데이터는 `src/legal/`, CHMO-478)·404(NotFoundPage) 포함 전 화면 실 UI.
- `api/` **API 계층**(CHMO-191·192·193) — `client.ts`(fetch 래퍼: 실 BE 봉투 `result` 언랩·시각 'Z' 보정 + 제작자 401 시 refreshToken 1회 자동 재발급 후 재시도(`/auth/refresh` 경로 소유 — transport 인프라) + 뮤테이션 401 공용 `redirectIfUnauthorized`)·`errors.ts`(BE 에러 코드→FE 의미 코드 매핑, 미지 코드는 통과)·**도메인 모듈** `auth.ts`·`groups.ts`·`events.ts`·`albums.ts`·`viewer.ts`(엔드포인트 함수 — 경로·바디·응답 변환 소유, 화면은 이것만 호출)·`mappers.ts`(BE DTO→FE 타입 변환 — 내부 전용, 화면에서 import 금지) · `lib/` `auth.ts`(제작자 토큰 — accessToken+refreshToken 저장·삭제 단일 소유, CHMO-193)·`viewer.ts`(뷰어 토큰 + 모임명 캐시 — BE는 모임명을 unlock 응답에만 준다)·`clipboard.ts`(복사 헬퍼)·`download.ts`(blob 저장 헬퍼 — 교차 출처 download 무시·서비스워커 우회 함정 회피(CHMO-242) + `cache:'no-store'`로 `<img>`가 오염시킨 CORS 헤더 없는 캐시 재사용 차단(CHMO-326 — S3는 Origin 없는 요청에 ACAO·Vary를 안 줌))·`pin.ts`(4자리 PIN 규칙 단일 원천 — UI·목 공유)·`albumLabels.ts`(특수 앨범 표시명 단일 원천 — UI·목 공유, CHMO-192)·`uncertainCauses.ts`(분류 사유 코드→문구 단일 원천 — 문구는 FE 소유, 미지 코드 무시·빈 결과는 범용 문구 수렴, CHMO-412)·`albumSort.ts`(앨범 표시 정렬 단일 원천 — 인물 가나다(이름 없음 뒤)→공통→분류어려움, 검토 상태 무관 고정 · 08·14·15 공용, 09-1 추천은 유사도 순이라 제외. 실 BE 응답은 미검토 우선+albumId(CHMO-336)라 화면이 정렬을 소유하고 목도 실 BE 순서로 응답, CHMO-411)·`upload.ts`(업로드 제약 단일 원천 — 확장자 화이트리스트·20MB·50장, UI·목 공유, CHMO-194)·`concurrency.ts`(S3 PUT 동시 실행 수 제한)·`previewThumb.ts`(06-U 업로드 미리보기 축소 썸네일 — 원본 수십 장 타일 디코드 프리즈 방지, 실패 시 원본 폴백, CHMO-369. **디코드는 장당 1회**(CHMO-497): 1순위 `createImageBitmap(file, {resizeWidth, resizeQuality})`로 디코드 단계에서 축소해 원본 해상도 비트맵을 메모리에 안 올리고 — resize 옵션을 무시하는 브라우저도 뒤이은 캔버스 축소가 같은 결과를 만들어 **능력 감지가 필요 없다** — 이 API가 못 읽는 포맷(HEIC 등)만 `<img>`+캔버스 폴백) · `hooks/` `useApi.ts`(**`useApi(key, fetcher)`** — key가 요청 정체성, fetcher는 도메인 함수에 signal만 전달)·`useAlive.ts`(언마운트 후 늦은 응답 차단)·`useMutation.ts`(뮤테이션 공용 스켈레톤 — unmount 가드+401 리다이렉트+에러 메시지 정규화 소유, handle\*엔 성공/실패 후속만 남긴다; busy/성공/에러표시는 호출부 소유라 회귀 없음, CHMO-181)·`useDragScrollX.ts`(가로 스크롤 목록 마우스 끌기 스크롤 — 터치는 네이티브 팬 유지·드래그 후 click 삼킴, 09-1 추천 목록, CHMO-345) · `types/api.ts`(API 계약 타입 — `ID = number`, 라우트 파라미터는 도메인 함수가 `ID | string`으로 받아 경로에 그대로 쓴다).
- `test/` **테스트 하네스**(CHMO-219) — `setup.ts`(node용 localStorage + 테스트 간 토큰 격리)·`http.ts`(fetch 스텁·응답 빌더)·`fixtures/be.ts`(**실 BE 응답 원문** — 2026-07-10 채집분 + 스웨거 스키마 기준분 + 2026-07-16 SPACE404, 시크릿은 치환). 테스트는 대상 옆에 둔다(`src/api/*.test.ts`·`src/lib/*.test.ts`·`src/mocks/serializers.test.ts`). **MSW를 HTTP 서버로 쓰지 않는다** — 목이 목을 검증하는 꼴이다. 두 테스트가 지키는 건 서로 다르다: `contracts.test.ts` = "매퍼가 **실 BE**와 말이 통하는가"(BE 픽스처), `mocks/serializers.test.ts` = "목과 매퍼가 서로 말이 통하는가"(tsc는 이 둘을 잇지 못한다 — 목이 `id`를 주고 매퍼가 `albumId`를 읽어도 빌드는 통과한다).
- `mocks/` **MSW 목 레이어**(CHMO-108·109, BE 계약 이행 CHMO-195) — `db.ts`(인메모리 스토어·상태머신·다대다 — `personNameOf`는 인물만 이름을 주고 특수 앨범은 null, BE와 동일. 분류 로직 자체는 증분("앨범 없는 사진만")이지만 **2회차 등록이 게이트로 막혀 도달하지 않는다**, CHMO-486) · `fixtures.ts`(시드 — 호출마다 새 객체) · `persist.ts`(가입 계정 localStorage 보존) · `handlers/`(auth·groups·events(+검수요약/공개)·albums(검수)·share(학부모 뷰어) + `shared.ts` 봉투 헬퍼(`ok`/`created`/`errorResponse`)·검증 헬퍼(`toId` 경로/본문 id 정규화 포함)·`serializers.ts`(**BE DTO 직렬화** — 함수명이 BE DTO를 따른다(`toAlbumSummary`≠`toAlbumDetail`), 목록/상세가 다른 필드를 주는 것까지 BE 그대로. 뷰어 필터링 포함)). 시드 ID는 숫자(모임 1~3·이벤트 1~4·앨범 1~12·사진 101/201/301대, 신규 발급 1001~ — CHMO-191). 업로드 3단계도 BE 계약대로 구현(CHMO-194): presign은 사진을 만들지 않고 `originals/events/{id}/{uuid}.{ext}` 키만 발급, 가짜 S3 PUT(`/mock-s3/*`)이 `db.uploadedKeys`에 기록, 등록(`POST /events/:id/photos`)이 사진 생성 + 분석 시작을 맡는다(PUT 안 한 키는 404). 시드 로그인: 이현정/1234 · 학부모 공유 시연: `/share/shr_grp1`, 비번 7421.

## 작업 방식

- **애매하면 추론하지 말고 사용자에게 묻는다.** (사용자 명시 선호)
- 사용자·문서는 한국어 — **한국어로 응답**한다.
- 화면 작업 시 와이어프레임을 Figma MCP로 확인: file `pcgDOk6iZYtuUEhKhaPWPM`, page `211:1311`. 화면별 노드 id는 `docs/screen-spec.md` 매핑표 참조.
- **브랜치·커밋 규칙**: `docs/convention.md` 준수 — Git Flow(`main`/`develop`/`feature`/`release`/`hotfix`), 커밋 메시지는 `[CHMO-###] type: 메세지` 형식.
- **스토리 작업 흐름**: 착수는 `/start-story CHMO-###`(Jira SP·상태 세팅 + `develop` 기준 `feature/CHMO-###-*` 브랜치), 마무리는 `/finish-story`(커밋·푸시·`develop` 대상 PR 생성까지). **머지·브랜치 정리·Jira '완료' 전환은 사용자가 수동으로** 한다(스킬은 체크리스트만 안내). `main`·`develop` 직접 커밋/푸시는 가드 훅이 차단한다. 담당자·상태는 Jira에서 직접 지정한다. 백로그는 Jira 프로젝트 `CHMO`(에픽 CHMO-96~105 / 스토리 CHMO-106~. 실연동 후속: 192 엔드포인트 계층 추출·193 리프레시 토큰(완료)·194 업로드 3단계·195 목 이행·196 API 문서 갱신).

## 미확정 결정 (임의로 정하지 말 것 · 상세 `docs/screen-spec.md` §5)

홈 관리자 배지 표시 여부 · 품질 제외 사진 라우팅(눈감음/흔들림 앨범) · 공통(common) 앨범 정의. (학부모 공유 = 모임 단위로 **확정**됨.)

## 명령어

- `npm run dev` — 개발 서버(Vite, 기본 5173) · `npm run build` — 타입체크(`tsc --noEmit`)+프로덕션 빌드 · `npm run preview` — 빌드 결과 미리보기.
- `npm run typecheck` · `npm run lint` — 검사 · `npm run test`(=`vitest run`)/`test:watch` — **api 계층 계약 테스트**(CHMO-219).
- Prettier: `npm run format`(쓰기)/`npm run format:check`(검사). **주의**: 리포에 기존 CRLF/포맷 잔재가 있어 `format:check`는 클린 체크아웃에서도 실패한다 — 통과/실패 신호로 쓰지 말 것. `format` 전체 실행도 무관 파일까지 재포맷하므로 커밋 전엔 스토리 파일만 스테이징한다.

**워크플로우 스킬**(`.claude/skills/`):
- `/start-story CHMO-###` — 스토리 시작: SP 추천·설정, 상태 '진행 중', `feature/CHMO-###-<slug>` 브랜치 생성.
- `/test-guide` — 현재 브랜치 변경분 기준 테스트 가이드 생성: 자동 검증(typecheck·lint·build) 실행 + "진입 경로 → 조작 → 기대 결과" 수동 체크리스트.
- `/finish-story` — 커밋·푸시·PR 생성(테스트 가이드 포함) + Jira PR 코멘트(best-effort). 머지·브랜치 정리·Jira '완료'는 사용자가 수동으로(스킬은 체크리스트만 안내).
- `/bug-briefing` — 버그·코드리뷰 결함을 "무슨 코드 → 언제 터지나 → 왜 그렇게 됐나 → 어떻게 고치나" 4단 형식 + 코드 위치 링크 + 요약 표로 브리핑.

**가드 훅**(`.claude/settings.json` → `.claude/hooks/`): `main` 직접 commit/push 차단, commit/push 전 typecheck·lint·test(스크립트 정의 시) 실행.
